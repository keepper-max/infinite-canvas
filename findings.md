# AI 漫剧生产画布 2.0：事实与决策

## 支付宝电脑网站支付正式接入（2026-09-24）

- 线上权威仓库为 `keepper-max/infinite-canvas` 的本地发布副本，当前基线提交为 `eb29db3`；实验仓库存在大量未提交改动，不参与本轮。
- 支付宝正式应用已上线，AppID、商户 ID、支付宝公钥和本地应用私钥均已准备；应用私钥可按 PKCS8 导入，并与同目录应用公钥构成有效 RSA2048 密钥对。验证过程未展示密钥内容。
- 官方电脑网站支付主流程要求 `alipay.trade.page.pay`、POST `notify_url` 异步通知与 `alipay.trade.query` 主动查单并行；同步 `return_url` 不能作为支付成功依据。
- 支付通知验签后必须核对 app_id、out_trade_no、total_amount、seller_id 与交易状态；重复通知和主动查询必须共用一次性入账事务。
- 当前不实现银行卡退款，因此不需要应用网关或 `alipay.trade.refund.depositback.completed` 消息订阅；接口内容 AES 加密、授权回调和 openid 也不启用。
- 生产支付继续由服务端开关控制，代码与配置验证完成前保持关闭。
- 正式仓库已有 `billing_plans`、`payment_orders`、`payment_callback_receipts` 占位表和支付路由，但服务层固定返回 `PAYMENTS_DISABLED`，前端也只显示“支付宝充值尚未开放”。
- 现有积分账户已支持批次、到期、负余额抵扣、不可变流水和幂等键；支付入账应复用 `CreditService` 的锁与批次语义，不能另写第二套余额更新。
- 现有 `payment_orders` 缺少购买积分快照、通知/查询时间、失败原因和关闭时间；需用新的增量迁移补列，不能修改历史迁移。
- API 使用 Hono，正式接入应通过独立支付宝服务注入 `OperationsService`，并为通知路由保留原始表单字段；不能套用要求浏览器 Origin 的普通 JSON 写接口语义。
- `billing_plans` 目前没有任何种子数据，积分页也没有订单创建或历史入口；需在新迁移中加入已确认的 10/50/100 元套餐，并由支付能力开关控制展示。
- 支付回调路由位于统一 `/api/*` 来源校验中；支付宝服务端不会携带浏览器 Origin，必须只对精确通知路由豁免 Origin，并依靠 RSA2 验签和业务字段校验，不能整体放宽 API 防护。
- 当前积分页已经使用统一账户、批次和流水展示，适合在同页增加支付宝充值区和订单状态；支付到账流水需使用 `purchase` 批次并默认 12 个月有效。
- 采用项目已验证过的 `alipay-sdk` 4.14.0：`pageExecute(..., "GET")` 生成电脑网站支付链接，`checkNotifySignV2` 校验表单通知；主动查询可使用 SDK 的服务端请求能力并限制返回字段，不把原始响应写日志。
- 当前支付输入允许客户端提交 `provider`，正式接入后应改为服务端固定 `alipay`，避免客户端选择未授权支付渠道。
- 支付套餐可沿用已确认的 10 元/1000、50 元/5000、100 元/10000 积分；计划快照必须写入订单，后续修改套餐不能改变旧订单应到账积分。
- 主动查单至少提供用户本人订单同步和管理员异常订单同步；自动关闭的分钟数属于行为边界，本轮代码改为环境变量必填配置，生产启用前由用户明确填写，不静默假定默认值。
- 现有 API 集成测试使用可选 `TEST_DATABASE_URL` 和增量迁移，可新增真实 PostgreSQL 支付幂等测试；应用层测试需要增加支付宝服务端通知不受浏览器 Origin 校验阻断的精确用例。
- `alipay-sdk@4.14.0` 的默认依赖树解析到存在高危重定向凭据泄漏问题的 `urllib<=4.9.0`；支付接入不能带着该依赖上线，需要锁定已修复的 urllib 版本或改用官方 v3 cURL 协议实现后再继续。
- `urllib@4.9.1` 已包含该漏洞修复，且满足支付宝 SDK 的 `^4` 依赖范围；使用项目级 override 锁定后生产依赖审计为 0 项漏洞。
- 已实现服务端固定支付宝渠道和套餐快照、官方收银台 URL 白名单、精确异步通知 Origin 豁免、RSA2 验签、AppID/商户号/金额核对、回调收据防重及购买积分账本幂等；同步返回页仅触发主动查单，不直接判定成功。
- 用户积分页沿用现有黑金界面，支付宝作为首位充值方式并使用官方 Logo/推荐标识；付款前必须勾选简明协议。按用户此前要求，普通界面不展示供应商名称、成本公式、汇率或密钥。
- 原简明协议仍含运营主体和客服占位符，且详细写出供应商成本公式；站内版本按既定产品要求改为守守画布通用称谓、客服渠道和“以页面提示及流水为准”。正式开放前仍需补足准确运营主体/客服信息。
- 生产启用仍缺三个明确选择：订单关闭分钟数、小额实付验收金额、协议运营主体与客服渠道。代码因此不提供隐式超时默认值，支付与合规开关继续为 false。

## AI 文本工作台专项（2026-09-11）

- 新入口定位为“AI 文本工作台”，不是提示词库的替代品：工作台负责多轮创作，提示词库负责收藏与检索。
- 现有 `requestImageQuestion` 已能通过服务端托管模型任务调用文本模型，但把整段对话拼成单个 prompt，且没有独立的会话/消息 PostgreSQL 表。
- 现有模型目录已提供 64 个文本模型；新页面应读取同一全局目录，不新增浏览器 API Key 配置。
- 文本对话按“项目 + 创建者”隔离；项目成员权限负责项目边界，但同项目其他成员不能修改或删除个人对话。
- 本地 `docker-compose.local.yml` 当前让 Web 依赖无健康检查的 `model-proxy`，组合启动会失败；本轮继续使用既有主 Compose 部署路径，不把该覆盖文件混入验证环境。
- 现有 `generation_jobs`、BullMQ Worker 和文本产物可复用；会话消息必须由服务端按用户和项目隔离，不能只存 localForage。
- 导航、路由与图像/视频工作台均为独立页面结构；新增 `/text` 页面最符合现有产品边界。

## 项目云端单一数据源专项（2026-09-11）

- 用户要求补齐项目创建、重命名和删除服务端接口，消除前端 IndexedDB 与 PostgreSQL 的双权威，并使用最新镜像完成登录→画布→真实生成→刷新恢复验收。
- 删除属于高风险数据操作；本专项采用可恢复的软删除，不直接物理删除既有画布、任务或资产，不修改 ECS、不提交、不推送。
- 根因已确认：服务端只有项目列表与读取接口，前端新建、改名、删除和导入全部直接写 localForage；云端保存随后使用本地 nanoid 请求 UUID 项目而失败。
- `projects` 当前没有软删除字段；所有项目访问需要统一排除已删除项目，不能只修改列表，否则任务详情、成片任务和素材接口仍可能通过遗留成员关系访问。
- 安全语义定为：所有者可软删除，owner/admin/editor 可改名；删除时取消仍在运行的生成/成片任务，保留项目、画布、资产、事件和成员关系用于恢复；若无可用项目则在同一事务创建新的默认项目。
- 前端项目卡、项目列表、项目页快捷新建/删除/重命名、ZIP 导入均需改成异步平台 API；AuthGate 加载成功后必须以服务端项目列表替换本地项目壳，不能只追加。
- 0012 增量迁移、项目生命周期 API、前端云端单一来源和软删除访问隔离已完成，并在隔离 PostgreSQL 16 与保留旧数据卷的最新本地镜像上通过。
- 本地编排此前继承了外网 `TRUSTED_ORIGINS`，导致 localhost 注册被拒绝；本轮只调整运行中本地 API 的来源白名单和 Cookie 属性，未改生产配置。
- 隔离账号的注册、创建/改名/删除、画布保存及新 Session 恢复已通过；安全注入本地模型密钥后，真实 GPT-5.5 任务完成至 100%，全新登录会话可恢复同一画布与任务。

## 第 6 部分启动审计（2026-09-10）

- 第 6 部分必须在现有通用节点和服务端任务契约上增量扩展，不能把 Codex Skill 目录或其内部标签直接发送给视频模型。
- Seedance 漫剧属于多片段 sequence workflow：项目层保存故事、场景、镜头与连续性状态；每次只编译当前镜头的生成提示词。
- Skill 输出需要同时保留内部结构化作者状态与可发送的自然语言提示词；Director's Read 内部字段必须转译成可见/可听载体，不能原样进入最终提示词。
- 每个参考资产必须显式绑定唯一角色：身份、环境、首帧、尾帧、动作、音频等；不能根据上传顺序或媒体类型猜测引用职责。
- 2.0 Skill 的创作方法可复用，但其模型 ID、时长、分辨率和参考数量不能套用到 Seedance 2.5；运行参数继续由服务端模型能力表决定。
- 编译器必须落实镜头范围防火墙：已完成事件不重演、未来事件不提前、每个任务只有一个当前动作和一个可验证终点。
- 漫剧连续性需要区分角色/场景等 canonical references 与上一条已验收视频的 transient state；跨场景默认重新锚定，不能把未验收或失败结果当作下一镜头父级。
- 参考角色在画布连接上直接表达，最终 Provider 请求仍由现有模型能力表转换；Skill 不应硬编码任何第三方渠道的字段名或数值限制。
- 节点注册表已支持开放字符串类型和自定义 Content/Panel，因此漫剧节点可以作为内置业务定义增量注册，无需扩张底层 `CanvasNodeType` 枚举或更换渲染内核。
- 服务端画布契约接受开放节点类型、`workflowKind`、类型化端口和任意已脱敏 metadata；适合直接保存角色卡、分镜表和 Skill 结果。
- 任务创建契约已有 `parameters`、引用资产版本和幂等键，资产版本也已有 provenance；第 6 部分可在兼容字段内先记录 Skill/输入追溯，同时补充前端业务契约与服务端校验。
- 当前 Worker 生成媒体资产时 provenance 只写模型和模式；需要从不可变任务输入快照继续携带 `skillId`、`skillVersion`、输入哈希与上游资产版本，才能完成漫剧结果追溯。
- 当前插件 AI 文本调用未绑定节点上下文；漫剧节点若直接复用它只能得到结果，无法把后台任务稳定关联到具体节点。第 6 部分应给宿主 AI 调用增加可选任务追踪上下文，并保持普通插件兼容。
- 真实视频服务已经允许 `projectId/nodeId/nodeRevision/idempotencyKey` 上下文并按模式映射首帧、尾帧和多参考；第 6 部分重点是让漫剧节点产生确定的模式与有序角色映射，而不是另写 Provider。
- 当前 Agent `connect_nodes` 只生成旧式无端口边，漫剧模板不能复用该简化操作；模板创建器需要直接输出完整端口、资源类型、角色和顺序。
- 仓库没有现成的业务工作流模板入口，需增加自有漫剧模板注册/实例化模块，并从画布工具栏显式创建；不能把模板伪装成插件脚本。
- 最小可维护实现是给现有 `CanvasNodeDefinition` 增加可信内置 `execute` 与端口/执行描述：远程插件仍只获得现有受限能力，漫剧定义由本地注册，不执行用户脚本。
- 默认模板应直接实例化带 `workflowKind`、稳定 definitionId/version 和完整类型化边的节点，避免经过只支持旧式边的 Agent 简化操作。
- 运行器放在画布宿主层，统一提供单节点、所选、从当前向后和整套拓扑执行；节点定义只负责把上游快照编译成当前节点结果。

## 项目云端单一数据源专项（2026-09-10）

- 用户要求补齐项目创建、重命名和删除服务端接口，消除前端 IndexedDB 与 PostgreSQL 的双权威，并使用最新镜像完成登录→画布→真实生成→刷新恢复验收。
- 删除属于高风险数据操作；本专项默认采用可恢复的软删除，并在服务端事务内处理当前项目/默认项目兜底，不直接物理删除既有画布、任务或资产。
- 不提交、不推送、不部署 ECS；本轮只修改工作区、重建本地隔离环境并验收。

## 当前仓库事实

- 工作仓库：`H:\CodexStorage\infinite-canvas-upgrade`。
- GitHub：`keepper-max/infinite-canvas`；上游为 `basketikun/infinite-canvas`。
- 当前已集成 Infinite Canvas 画布、分组/解组、媒体参数、视频任务 ID 恢复、代理、WebDAV、全局托管模型和 Seedance 工作流。
- 前端为 Vite + React + TypeScript + Zustand；画布主业务仍主要保存在浏览器 localForage/IndexedDB。
- 底层内置节点目前为图片、文本、配置、视频、音频、分组；插件节点使用开放字符串类型。
- 当前全局模型和 Seedance 适配已经存在，后续应迁入统一服务端模型注册表，而不是重写丢弃。
- 第 0 部分本地基线（2026-09-08）：`main` 位于 `d31c74e253232c4a6314b7461b7e2f0d9c948ee8`，与 `origin/main` 无前后差异；工作区只有本轮新增的规划文件未跟踪。
- 当前根 `docker-compose.yml` 只有 `app` 一个服务，运行镜像由 Bun 同时提供 Vite 静态产物和同源模型代理；尚无 PostgreSQL、Redis、Worker、MinIO/OSS 服务。
- 当前服务端敏感配置仅登记变量名 `TOKEN360_API_KEY`、`TOKEN360_UPSTREAM`、`TOKEN360_CATALOG_URL`；审计文档不得记录其值。
- 现有 `CanvasConnection` 只有起止节点 ID，尚无端口、数据类型或引用角色；第 0 部分契约必须保持向后兼容映射。
- 现有视频调用区分普通 OpenAI 格式、Gemini 格式、脚本模型和服务端托管 JSON 模式；新能力表需要覆盖并收敛这些分支，而不是直接删除。
- 旧版工作区 `H:\代码相关\项目\漫剧工作流` 保留了 Next.js/Drizzle/PostgreSQL 的完整后端实现，已使用 `argon2`、`pg`、`drizzle-orm`，并包含注册、登录、Session、项目权限及画布 API；第 1 部分将复用其已验证数据语义，但适配为当前仓库的独立 API 服务。
- 旧 `projects` 已有 `owner_id`，`project_members` 尚缺复合主键/唯一约束；旧库没有独立 `canvases` 表，画布节点直接绑定项目。兼容迁移必须增量补表/约束，不能重建现有 19 张表。
- 旧 SQL 迁移实际已为 `project_members(project_id,user_id)` 和画布节点/边建立复合主键，只有旧 Drizzle 声明没有完整表达这些约束；新 schema 要与数据库实际约束对齐。
- 旧认证已采用 Argon2id、随机 Session Token 的 SHA-256 哈希和 HttpOnly/Lax Cookie，可复用安全语义；需要补强密码长度、统一错误信封、Origin 校验、Session 轮换/过期清理和默认草稿幂等创建。
- 当前前端路由仍直接开放首页和 `/canvas/:id`，项目完全由 localForage 保存。第 1 部分只接入用户边界和云端项目壳；节点/连线内容迁移仍留在第 2 部分。
- 前端已有 React Router、Ant Design、React Query 和 `UserLayout`，认证可以作为路由门卫接入，不需要替换现有布局；画布详情页会隐藏顶部栏，退出入口需同时覆盖普通顶部栏和画布工具栏或提供统一会话失效跳转。
- 第 1 部分依赖版本已通过当前注册表核验：Hono 4.13.7、Node adapter 2.1.1、Drizzle ORM 0.45.2、pg 8.23.0、argon2 0.45.1、Zod 4.5.4、tsx 4.23.13。
- 第 1 部分完成后，平台 API 已成为账号与项目边界；画布节点、连线和媒体内容仍以 IndexedDB 为权威，必须由第 2、3 部分分别迁移，不能把项目壳同步误认为云端画布持久化。
- ECS 使用旧式 Docker builder 且无法稳定直连 Docker Hub；镜像阶段已按 API、模型代理、前端顺序组织，PostgreSQL 镜像支持通过 `POSTGRES_IMAGE` 覆盖，生产部署时应使用主机可达的可信镜像源。
- 第 0 部分 PostgreSQL 备份已在隔离容器成功恢复，并能应用第 1 部分增量迁移；原 19 张业务表和数据未被重建或覆盖。
- 第 2 部分审计确认：画布详情页用组件内 `nodes`、`connections`、`viewport` 作为运行态，并通过两个 effect 写回 Zustand；Zustand 再以 400ms 防抖写入统一 localForage 键 `infinite-canvas:canvas_store`。
- 当前 `CanvasNodeData` 和 metadata 包含运行态 URL、storageKey、远端任务 ID、插件字段及多媒体引用；第 2 部分只能保存 JSON 结构和待转存引用，不能把 Blob、`blob:` URL 或密钥配置当作云端资产上传。
- 旧工作区已有 `canvas_nodes`、`canvas_edges`、`canvas_snapshots` 及画布 API，可借鉴事务替换和组合主键语义；新实现必须叠加独立 `canvases.revision`、端口/角色字段和显式冲突响应，不能原样复制旧的无并发控制保存逻辑。
- 现有 WebDAV 同步会合并项目及媒体文件，属于用户主动配置的另一条同步链；第 2 部分不得破坏它，但平台云端画布必须优先于 WebDAV/localForage，并把二者降级为迁移/导出来源。
- 第 2 部分最终采用 800ms 防抖和串行保存：服务端 revision 控制并发，浏览器缓存最近成功快照与未同步草稿；任何 409 冲突都要求用户明确选择。
- 旧 IndexedDB 不自动删除。迁移前保留本地原数据，只上传清洗后的节点、连线、视口和设置；媒体 Data URL/Blob、API Key/Token、渠道、代理与 WebDAV 配置均排除。
- ECS 隔离恢复旧数据库后，0007 增量迁移、真实 PostgreSQL 集成、同源读写、版本冲突与 API 重启恢复均通过，生产容器未变更。

### ECS 运行基线（2026-09-08）

- 主机运行 Ubuntu 22.04 系列内核、Docker 29.1.3 和 Docker Compose 2.40.3；系统盘约 40 GiB，审计时已用约 21 GiB。
- 对外服务是 `infinite-canvas` 容器，镜像 `infinite-canvas:global`，端口 `3000`，重启策略 `unless-stopped`，根页面返回 HTTP 200。
- 当前线上镜像 ID 为 `sha256:a13b4d47de32276972cb9481a148e4e7019d14770d4a201a6625c34f81a734b8`；其 `/healthz` 未暴露 JSON 健康响应，说明部署镜像早于本地当前代理实现或使用了不同启动层。
- 旧 `jingjie-studio` Compose 项目位于 `/opt/jingjie-studio`；Web 与 Worker 已停止，PostgreSQL 16、Redis 7、MinIO 仍在运行并只绑定回环地址。
- 持久卷仍存在：`jingjie-studio_postgres_data`、`jingjie-studio_minio_data` 及 Redis 匿名卷。它们是后续迁移来源，不得直接覆盖或删除。
- PostgreSQL 备份中确认有 19 张业务表，包括用户、Session、项目成员、画布节点/边/快照、资产、任务/事件/产物、积分、模型配置和提示词等；第 1 部分必须先读取旧 Drizzle/SQL 结构并编写兼容迁移，不能新建空库替代。
- 线上应用容器环境清单只记录到变量名 `TOKEN360_API_KEY`；任何 inspect 备份都按敏感文件处理。

### 第 0 部分备份

- ECS 备份：`/opt/jingjie-backups/part0-20260908T141423Z`，包含当前生产镜像、容器 inspect、旧源码/配置、PostgreSQL 自定义格式转储、MinIO 数据和恢复说明；全部校验和已通过。
- 本地 Git 备份：`H:\CodexStorage\backups\infinite-canvas-upgrade\part0-20260908T141423Z`，包含完整 Git bundle 和非敏感容器配置；bundle 验证通过。
- 备份没有触发容器切换、数据库写入、数据恢复或线上发布。

## 蔓悦实测结论

- 蔓悦与 Infinite Canvas 属于高度相近的自研 DOM 节点 + SVG 连线架构，不是 React Flow。
- 其产品优势主要在漫剧业务层：项目关联、分镜表、角色卡及状态、版本 A/B、批量首帧/视频、任务详情、对象存储、时间线、字幕和合并成片。
- 其画布交互还包含对齐/排列、锁定、搜索、专注模式和媒体性能模式。
- 蔓悦后端源码未公开，只能依据公开前端包和实际页面行为；不得复制其私有实现。
- 直接访问其带尾斜杠画布路由曾返回 403，说明我们的生产 Nginx 必须配置 SPA 路由回退。

## 核心架构决策

| 决策 | 原因 |
|---|---|
| 保留 Infinite Canvas 底座 | 当前交互和扩展系统可用，重换底座只会增加迁移风险 |
| 新建独立平台 API 与 Worker | 当前 Vite 前端不适合承担鉴权、密钥、任务和云数据职责 |
| PostgreSQL 作为云端权威 | 支持多用户隔离、版本、任务恢复和可追溯性 |
| Redis/BullMQ 执行生成任务 | 页面关闭后继续、失败重试、并发与幂等控制 |
| 开发用 MinIO、生产优先 OSS | 视频占用大，避免 ECS 系统盘同时承担长期素材 |
| 通用节点 + `workflowKind` | 防止为几十个漫剧用途复制底层组件 |
| 类型化端口和边 | 在请求模型前阻止首尾帧、多参考等模式传错参数 |
| 资产版本不可变 | 保证旧任务可复现，切换主版本不破坏历史 |
| Skill 节点输出结构化 JSON | Seedance Skill 先编译提示词，再由视频节点调用模型 |
| 内部模型 ID 与显示名分离 | 用户只看模型名，Provider、地址和密钥留在服务端 |

## 关键风险

- IndexedDB 到 PostgreSQL 是数据权威切换，必须提供显式迁移、备份和失败回退，不能静默上传。
- 旧浏览器配置可能含 API Key；迁移时必须明确排除渠道、密钥和代理配置。
- 当前画布主页面文件较大，产品化改造需要按状态、渲染、交互和执行边界渐进拆分，避免一次大型重写。
- 模型公开目录不等于完整能力声明；必须叠加人工维护的能力覆盖表。
- 用户自定义脚本和第三方插件可访问敏感数据；首版只允许可信来源，沙箱后续单独实施。
- FFmpeg 必须在 Worker 运行，并限制并发、临时空间和清理策略；不能阻塞 Web 服务。

## 第 3 部分补充发现（2026-09-09）

- 当前图片与音视频仅保存在浏览器 IndexedDB（`image_files`、`media_files`），页面刷新依赖重新生成 blob URL；尚无云端资产权威数据。
- 当前资产 Store 仅支持 `text | image | video`，直接删除会清理本地文件，没有不可变版本、主版本、来源追踪、回收站或用户隔离。
- 资产页的上传、编辑、导入、下载全部连接本地 Store；第 3 部分需要在保留旧数据读取能力的同时切换到项目级 API。
- 画布数据已有 `storageKey` 字段，但没有 `assetId` / `assetVersionId`，后续需要用确切版本标识绑定长期媒体引用。
- 对象存储采用一套 S3 兼容适配器：开发环境连接 MinIO，生产环境通过可配置 endpoint 连接 OSS；浏览器只获得短期签名地址，永久记录只保存服务端生成的对象键。
- 旧数据库中的直连资产字段可无损迁移为不可变 v1，同时保留旧列用于回滚；新版本只写 `asset_versions`，主版本切换仅更新指针。
- 画布保存事务会同步确切的资产版本引用，并拒绝跨项目版本；因此后续任务和生成节点可绑定 `AssetVersion.id`，不会因主版本切换而漂移。
- ECS 隔离环境已验证旧库恢复、0008 增量迁移、真实 PostgreSQL 版本链、MinIO 签名上传下载、画布版本绑定、跨用户隔离、回收站恢复和 API 重启恢复；生产容器未改动。

## 第 4 部分补充发现（2026-09-09）

- 当前托管模型代理不是独立 TypeScript 包，而是根目录 `token360-proxy.mjs`，由 Dockerfile 的 Bun 容器直接运行；平台 API 尚未包含模型注册、任务队列或 SSE。
- 旧 PostgreSQL 已有基础 `generation_jobs`、`job_events`、`job_artifacts`，但缺少工作流/节点运行、幂等、尝试、事件序号、取消、心跳、输入快照和资产版本外键；第 4 部分必须用兼容 ALTER/补表迁移，不能重建旧任务表。
- 前端图片、文本、视频和音频仍各自在浏览器调用 Provider；切换到后台任务需要先建立统一任务 DTO 和服务端编译器，再逐个替换调用入口，避免一次重写画布主文件。
- 平台边界要求 PostgreSQL 先写事件再发布 Redis，Redis 只用于队列与短期通知；断线恢复必须以任务和事件表为准。
- Token360 现有说明确认统一使用公开模型名，并分别通过聊天、图片和视频端点调用；模型可选参数随模型变化，因此公开目录必须与人工能力覆盖表合并，不能仅按名称或粗模态开放。
- BullMQ 官方说明其任务语义是至少一次，Worker 崩溃或锁失效可能再次执行；数据库幂等键和阶段性副作用防重是必需条件，不能只依赖 Redis jobId。
- BullMQ 官方建议生产者在 Redis 断线时快速失败、Worker 持续重连，并在部署时优雅关闭；当前 API 与 Worker 需要使用不同 Redis 连接策略。
- BullMQ 的自动重试次数包含首次执行，取消活动任务需要 AbortSignal 协作；数据库仍应保留 `cancel_requested`，Worker 确认停止后再进入终态。
- 新画布节点 ID 是文本且以项目组成复合主键，而旧 `generation_jobs.node_id` 是 UUID；新任务链应使用 `node_key text` 绑定当前画布节点，旧 `node_id` 只保留兼容读取。
- 当前画布详情页已能取得路由 `projectId` 和执行节点 `nodeId`，可以把这两个上下文显式传给统一任务客户端，无需引入全局隐式项目变量。
- 当前文本、图片、视频、音频调用均集中在 `web/src/services/api/`；托管模型可在这些服务入口分流到平台任务 API，用户自带渠道与插件继续走原有浏览器路径，从而减少回归面。
- 模型目录同步必须是“发现而不自动开放”：未知模型会记录到 `model_catalog`，但只有具备人工能力覆盖、启用且健康的模型才返回前端，避免目录名称误判造成错误参数或计费风险。
- Seedance 视频任务在 Worker 重试时必须优先查询已有 `provider_job_id`，避免上游已经接收任务后因本地超时再次提交并产生重复费用。
- 任务最长运行时间按首次 `started_at` 计算，而不是每次重试重置；因此确认的 30 分钟是整项任务上限。
- Token360 官方 API 文档确认视频工作流由 `frame_images[].frame_type`（首帧/尾帧）和 `input_references[]`（多模态参考）推断；`aspect_ratio` 是正式字段，`omni_reference_task_type` 只应在多参考场景发送。
- Token360 视频查询完成后不保证任务详情直接带下载 URL；规范下载路径是 `GET /v1/videos/{id}/content`。Worker 已使用二进制模式兜底转存，避免把临时 Provider URL当成长期资产。
- Token360 图片接口在同一 `POST /v1/images/generations` 支持文生图和带 `images[]` 的图生图，因此服务端能力映射将通用 `references` 转成 `images`。

## 资源索引

- 总计划：`task_plan.md`
- 执行包目录：`plans/ai-drama-canvas-2/`
- 历史迁移规划备份：`plans/archive/upstream-v018-migration/`
- 当前进度：`progress.md`
- 当前画布类型：`web/src/types/canvas.ts`
- 当前画布主流程：`web/src/pages/canvas/project.tsx`
- 当前浏览器持久化：`web/src/stores/canvas/use-canvas-store.ts`
- 当前插件宿主：`web/src/pages/canvas/hooks/use-plugin-host.tsx`

## 第 7 部分补充发现（2026-09-10）

- 第 7 部分要求补齐配音、音效、背景音乐、字幕、简化时间线、FFmpeg 合成与成片资产闭环。
- 当前服务端已有 PostgreSQL 资产模型、BullMQ 生成队列、对象存储边界与音频 TTS Provider，可复用现有鉴权、项目权限和资产版本机制。
- 当前画布已支持 `timeline` 资源连线和插件 `compose` 能力，但漫剧节点目录中尚未提供音轨、字幕、时间线与成片输出业务节点。
- 当前生成任务与模型目录强绑定；FFmpeg 合成不属于模型调用，应建立独立合成任务与队列，避免污染模型任务状态机。
- 当前对象存储接口只有签名下载和写入，没有供 Worker 直接读取二进制的内部方法；第 7 部分需要补充安全的 `get` 边界。
- 当前 API/Worker 生产镜像尚未安装 FFmpeg；应只在 API/Worker 共用的运行阶段安装系统 FFmpeg，不放入 Web 镜像。
- 合成 Worker 初始并发按第 7 部分既定方案设为 1；输入先从对象存储解析到隔离临时目录，成功或失败后均清理。
- FFmpeg 必须以参数数组启动，禁止拼接用户输入为 Shell 命令；字幕路径、样式和时间轴由结构化数据生成并转义。
- Token360 当前视频接口文档明确要求可选参数按具体模型的 `parameter_schema`/`supported_parameters` 决定，示例包含 8 秒任务；因此不能把 Seedance 2.5 错误限制为仅 5/10 秒。
- 生产端浏览器渠道已默认关闭并执行旧配置脱敏迁移；全局 Provider Key 只由 API/Worker 环境变量读取。
- 本机隔离 Docker 验证证明服务端任务闭环可用：默认项目、动态模型目录、BullMQ、SSE、MinIO 资产版本、幂等、三次尝试与手动重试、四种视频模式均通过。
- 运行中取消存在过一次真实竞态：Provider 提交返回后的无条件 `running` 写入会覆盖 `cancel_requested`。正确边界是提交、轮询前后和持久化前都重新读数据库状态，取消终态只允许由 `cancel_requested` 幂等转换。
- 成片合成适合使用独立于模型任务的 BullMQ 队列和数据库状态机；API 只保存结构化时间线，Worker 才读取不可变素材并调用 FFmpeg。
- FFmpeg 以参数数组和 `shell: false` 启动；用户字幕只写入隔离 SRT 文件，不能进入 Shell 命令。临时目录在成功、失败和取消后统一清理。
- H.264 `yuv420p` 输出宽高必须为偶数；该限制需要同时放在服务端契约和画布输入控件，避免任务排队后才失败。
- 仅安装 FFmpeg 不足以保证中文字幕质量；运行镜像还需要中文字体。为避免软件源大型索引不稳定，镜像按 Debian 官方 SHA-256 固定校验并安装文泉驿正黑字体包。

## 第 8 部分补充发现（2026-09-10）

- 当前数据库已有项目成员角色与生成任务积分预留字段，但没有短信验证、积分账户/流水、套餐、支付订单/回调、团队或管理员身份数据结构。
- 当前 API 已有基于可信 Origin 的写请求防护、HttpOnly Session Cookie、项目成员隔离和存活/就绪检查；尚未统一添加浏览器安全响应头，也没有运营能力开关或管理员 API。
- 仓库明确要求新增超时、重试、限流和并发边界前取得用户对默认值的确认；第 8 部分可先建立可配置且默认关闭的运营适配器，不能静默启用真实短信、支付或扣费。
- 第 8 部分的生产部署属于高风险发布：必须在任何迁移/切换前读取线上真实容器与路径，备份环境配置、数据库、代码提交和容器清单，禁止假设 ECS 当前目录或覆盖旧库。
- 实际文档路径是 `docs/content/docs/progress/todo*.mdx` 与 `pending-test*.mdx`；仓库没有 `.gitattributes`，新增 Linux 运维脚本时需要显式保证 LF，不能依赖仓库属性自动转换。
- 当前项目权限查询只验证 `project_members` 是否存在，没有区分可编辑与只读角色；团队项目映射若直接同步 `viewer` 会造成越权写入，必须先把所有写操作收口到明确的编辑权限检查。

## 第 9 部分补充发现（2026-09-10）

- 第 8 部分已具备备份、备份校验和健康检查脚本，但尚无统一的脱敏预检、不可变镜像切换、发布清单和脚本化回滚入口。
- 当前 Compose 把 Web/API 镜像固定为 `:local`，无法在不改文件的情况下按 Git 提交切换镜像；应改为环境变量覆盖并保留本地默认值。
- 当前 GitHub 镜像工作流只构建 Dockerfile 默认 Web 目标，且镜像命名仍指向上游组织；发布闭环需要分别构建当前仓库所有者名下的 Web 与 API 镜像。
- 未提供生产必填变量时 `docker compose config` 会在启动前拒绝解析，这是正确的安全行为；第 9 部分预检需要复用该能力且不得打印插值后的配置。

## 第 5 部分补充发现（2026-09-10）

- 现有画布已经具备内容化图片、视频、音频和文本节点，并已有框选、多选、复制、删除、分组、撤销重做、小地图、节点搜索与聚焦；第 5 部分应补齐产品缺口，不重写稳定底座。
- 节点类型已经保留 `locked` 字段，但拖动、缩放、删除、分组和自动排列尚未统一执行锁定语义，属于“数据字段存在、交互未落地”。
- 连线数据已经支持端口、资源类型、角色和顺序，但当前界面仍只显示左右通用圆点，创建连线时也未写入这些语义字段；需要在检查器中开放角色编辑，并由节点资源类型推断兼容性。
- 当前只渲染视口附近节点，已具备基础虚拟化；性能模式应在此基础上进一步延迟非选中媒体，而不是引入第二套画布渲染器。
- 资产侧栏当前只支持点击插入；浏览器拖放入口已存在，可通过自定义 MIME 载荷复用同一插入函数，实现拖到指定画布位置。
- 项目页约 3600 行，第 5 部分优先把纯布局算法与检查器拆出独立文件，避免在产品化阶段继续堆积交互逻辑。

## 第 6 部分补充发现（2026-09-10）

- 节点注册表可以直接承载可信业务节点，不需要扩展核心 `CanvasNodeType` 枚举；`workflowKind`、定义版本、端口和执行器构成稳定业务边界。
- 默认漫剧模板使用 15 个可自由重连的节点和显式引用角色，视频模式由实际首帧、尾帧和多参考输入推导，不依赖节点名称或上传顺序。
- Seedance Skill 在平台内作为提示词编译节点运行：内部保存结构化镜头状态和连续性字段，视频 Provider 只收到编译后的自然语言与其能力表允许的参数。
- 生成任务的 trace 与 Provider 参数严格分离；Skill 版本、输入哈希、输入快照、用户修改标记和语义资产类型进入任务/资产来源，不发送给上游模型。
- 角色三视图完成后把确切 `assetVersionId` 回写到角色卡；首帧、尾帧和视频节点也绑定输入资产版本，切换资产主版本不会改写旧任务引用。
- 批量执行按有向图拓扑顺序运行，支持当前、选中、当前及后续和全部四种范围；同输入成功节点跳过，上游错误只阻断依赖分支。

## 模型提交无限等待专项

- 本机任务 `e810274e-d663-468b-8f9e-1aecce66ed3d` 使用 `catalog.image.gpt-image-2`，在提交后精确 60 秒以 `PROVIDER_TIMEOUT` 失败。
- 运行中的本机 API/Worker 均注入 `PROVIDER_SUBMIT_TIMEOUT_MS=60000`；仓库默认虽为 300000，但仍会自动中止，和用户要求不符。
- 同一时间画布保存多次返回 422，失败节点因此未成为云端权威状态；需要单独读取验证错误并修复。
- ECS 当前 Web 仍为提交 `7807a73`，最新修复 `7fdecde` 尚未部署；本次失败请求实际来自 localhost，不是 ECS。
- 只移除 Provider 的定时信号不够：同步图片/文本请求挂起时，原有取消接口只写数据库，无法中断 Worker 中正在等待的 HTTP；修复需要由 Worker 按既有 5 秒轮询间隔监测 `cancel_requested` 并触发请求信号。
- 前端停止按钮原先只中止浏览器轮询，没有调用平台任务取消接口；平台托管图片、文本、音频和视频需要在其等待信号终止时同时请求后台取消。
- 平台托管视频前端原有约 50 分钟轮询上限，服务端原有 30 分钟总运行上限；两处都属于自动终止路径，需要在默认配置中关闭。
- 画布节点支持自由缩放并会产生小数尺寸，服务端 `width`/`height` 使用整数校验会稳定返回 422；位置本来已接受小数，尺寸应同样接受范围内有限数值。
- `canvas_nodes.width/height` 仍是兼容旧结构的整数辅助列；精确尺寸已保存在节点 JSON。正确的无迁移修复是在协议层接受有限小数、辅助列取整、读取时继续以 JSON 精确值为准。
- 普通 AbortSignal 还会用于请求替换和页面生命周期，不能一律解释为后台取消；只有「停止」按钮传入显式手动取消原因时才调用任务取消 API，刷新或切页只结束本地等待，后台任务继续。
# 当前任务发现：动态视频参数（2026-09-12）

- Token360 公开目录返回 `normalizedApiParameterSchema.fields`、`effectiveDefaultParams`、`supported_parameters`、输入输出类型与参考素材上限。
- Seedance 2.5 当前目录列出：时长 -1/4-30 秒、480p/720p/1080p、自适应及六种比例、标准/高码率、mp4/mov、音频、水印；多模态参考最多 30 图/10 视频/10 音频。
- 服务端 `model-gateway.ts` 已有 `modes`、`acceptedParameters`、`requiredParametersByMode`、`limits`、`parameterMap` 和提交前编译验证。
- `/api/models` 当前未把模式级必填规则和完整 UI 参数 schema 暴露给前端。
- 前端模型同步只保存 `acceptedParameters`；`VideoSettingsPanel` 固定渲染 480/720/1080、比例、时长和两个旧模式。
- 托管视频请求仍把旧的 `frames/reference` 加参考图数量推断为 `t2v/i2v/flf2v/multiref`，导致用户选择与真实请求模式存在间接关系。

## 当前技术方向
- 公共模型响应增加经过白名单清理的参数 schema、默认值、生成方式和参考素材限制。
- 新的前端配置使用四种明确模式：`t2v`、`i2v`、`flf2v`、`multiref`。
- 旧值增量兼容：`reference -> multiref`；`frames` 根据已连接图片数迁移。
- 生成方式切换只改变可见和可提交参数，不删除其他模式曾保存的草稿配置。

---
# 2026-09-12 本地动态视频参数未显示

- 根因不是前端实现未生效，而是 `localhost:3000` 仍运行旧 Web 镜像；工作区源码完成后没有执行本地镜像重建。
- 当前有效编排是 `canvas-part5-qa` 的基础 `docker-compose.yml`；已停止的 `model-proxy` 来自旧编排覆盖文件，是孤立容器且不在当前 API 直连链路中。
- 最新 Web 页面已从服务端模型能力返回值渲染四种生成方式，并显示 Seedance 2.5 的动态参数；旧截图中的 W/H 固定尺寸和“首尾帧模式 / 全能参考模式”已消失。

# 2026-09-12 全视频模型参数适配

- Token360 公开目录当前返回 101 个条目，其中按类型和模型名识别出 25 个视频相关条目；主要系列包括 Seedance、Veo、MiniMax H3、Wan 3、Vidu Q1、Berry、CogVideoX 和 Happy Horse。
- 当前服务端已经读取 `normalizedApiParameterSchema.fields`，但只公开 7 个通用参数；若其他模型声明帧率、镜头、负面提示词、种子等参数，前端会直接丢失。
- 当前前端只认识四种统一模式和固定参数键；方向正确，但需要从目录字段的 `visible_when`、输入参考限制和枚举中推导“某模式下显示哪些参数”。
- 错误记录：第一次抽样 Schema 按不存在的 `modelName` 精确查找，得到空结果；目录显示名实际来自其他字段，下一步先读取条目真实键名再抽样，不重复该查法。
- 公开目录实际有 22 个 `VIDEO_GENERATION` 模型；先前 25 个结果误把名称含 `image` 的图片模型计入，已按 `modelType` 收窄。
- 模式差异可由传输字段推导：`frame_images.max_items=0/1/2` 分别代表无图生、仅首帧、首尾帧；`input_references` 的图片/视频/音频上限决定多模态参考是否可用。目录中部分旧模型 `max_items` 为空，但 API 文档明确支持首尾帧，需要兼容字段存在即支持的语义。
- 额外可见参数至少包括 `style`、`prompt_expansion`/`prompt_extend`、`seed`、`size`、`quality`、`movement_amplitude`；当前实现会丢弃这些参数。
- 目录差异示例：Veo 只支持 4/6/8 秒和 16:9/9:16；Vidu Q1 固定 5 秒/1080p并提供运动幅度；CogVideoX 同时声明尺寸与分辨率；Berry/Wan 支持智能时长 `-1` 和最长 30 秒。
- 当前服务端提交编译以 `acceptedParameters + parameterMap` 白名单过滤，适合作为安全边界；应扩展为目录驱动的任意安全可见标量参数，而不是继续新增硬编码分支。
- Zustand 配置目前只保存 7 个固定视频字段，画布元数据也逐项复制这些字段；要支持所有模型且可刷新恢复，需要增加通用 `videoParameters` 字典，同时保留旧字段兼容已有项目。
- 目录模式推导还存在两个实际错误：`frame_images.max_items=1` 的模型不应显示首尾帧模式；`input_references` 明确把三类上限设为 0 的模型不应显示多模态参考。字段存在但限制为空时才按兼容语义开放。
- 前端公共参数定义缺少说明文字；可安全增加目录 description，用中文参数名映射 + 原说明辅助用户理解未知新参数。
- Web 子项目没有单元测试运行器，前端参数编译将通过类型检查、生产构建和浏览器模型切换验收；服务端契约由现有 Node Test 覆盖。
- 模型额外参数必须按模型 ID 隔离保存，否则从 Berry 切到 Vidu 时同名字段可能错误复用；已采用 `videoModelParameters[modelId]` 结构。
- 修正了默认值优先级：用户未设置某个扩展参数时应使用目录默认值，尤其 `prompt_extend=true`，不能因布尔值没有枚举而误落为 false。
- 视频工作台参考图仍被旧常量限制为 7 张，界面文案却写 9 张；这既自相矛盾，也无法表达 Seedance 2.5 的 30 张、Veo 的 3 张和单帧模型的 1 张限制。
- 参考输入上限必须结合生成方式：文生为 0、首帧为 1、首尾帧为 2、多模态参考使用目录 `maxImages`；画布连接输入由服务端再次校验。
- 对当前 22 个视频模型的实时 Schema 扫描确认：所有 111 个可见字段均为 `model_parameter`，类型只包含 boolean/integer/string，且字段结构没有条件显示扩展键。因此“生成方式变化”主要影响参考素材槽，标量参数按模型变化即可。
- 实时能力编译已覆盖 22/22：纯文生模型只保留 T2V，单帧模型只增加 I2V，双帧模型增加 FLF2V，有非零或未声明旧式参考上限的模型增加多模态参考。

# Virtual Portrait 角色资产库

- 用户截图明确指出：视频 API 通过 `asset://{assetId}` 引用 Virtual Portrait，不能使用分组 ID 或资产记录 ID。
- 首帧/尾帧场景放入 `frame_images[].image_url.url`；参考图与多模态参考放入 `input_references[].image_url.url`。
- First-frame I2V 只使用一个首帧资产；多个 Virtual Portrait 角色应使用 Reference images 或 Multimodal reference 模式。
- 官方流程：`POST /v1/asset-groups` 以 `groupKind=VIRTUAL_PORTRAIT` 建组；`POST /v1/assets` 用 multipart 的 `groupId`、`file`、`name` 上传；随后轮询 `GET /v1/assets/{assetId}` 或按 `groupId` 列表直到 `active`。
- Virtual Portrait 无需真人验证，但必须属于 active 分组且资产自身为 active；普通 `ua_` 用户上传资产不能冒充 Virtual Portrait，唯一可用于视频引用的是 `ta_` 前缀 `assetId`。
- 官方列出 First-frame I2V、首尾帧、参考图、多模态参考、编辑视频、续写视频；实际可用性仍必须以具体模型 `GET /v1/models/{model_id}` 的参数 schema 和当前 Provider/SKU 为准。
- Token360 登录态 Assets 页面不能由公开抓取工具读取；公开文档已提供本次实现所需的 API 合约，后续不依赖登录页面结构。
- 现有平台资产是项目级 PostgreSQL `assets` + 不可变 `asset_versions`，支持类型、状态、软删除和云端下载；当前没有供应商资产分组、供应商 `assetId` 或处理状态字段。
- 现有视频请求已在服务端统一生成 `frame_images` 与 `input_references`，适合在这一可信边界把 Virtual Portrait 引用映射成 `asset://`，无需浏览器直接拼 Token360 请求。
- 当前画布资产入口主要基于本地 `useAssetStore` 与通用云端资产 API，类型只覆盖文本、图片和视频；Virtual Portrait 应作为独立角色资产视图，同时仍能插入画布并参与现有图片连线。
# 2026-09-14 Virtual Portrait 补充审计

- 真实开发仓库为 `H:\CodexStorage\infinite-canvas-upgrade`；桌面线程 `cwd` 是另一套旧项目，不能作为本次改动目标。
- `createApp` 当前通过可选依赖注入资产、任务、模型等服务，适合新增独立 `VirtualPortraitService` 并在路由层复用现有登录与项目权限。
- 画布左栏目前只有“画布 / 资产 / 提示词”三个页签；普通资产完全来自浏览器本地 `useAssetStore`，上传走客户端存储服务，不具备项目级跨设备 Virtual Portrait 同步能力。
- 生成引用当前只接受 `assetVersionId`、标准 URL 或 data URL。Virtual Portrait 应新增站内 `virtualPortraitId` 引用，由服务端校验归属和 active 状态后解析成 `asset://ta_...`；不能允许客户端直接提交任意 `asset://`。
- Token360 视频 body 的最终映射层已经正确写入 `frame_images[].image_url.url` 与 `input_references[].image_url.url`，因此不应在 provider 层再做第二套 Virtual Portrait 特判。

## 最终实现结论

- 一个项目只创建一个服务端托管的 `VIRTUAL_PORTRAIT` 分组；角色条目保存 Token360 `ta_` Asset ID，并关联现有不可变源图版本。
- 角色库只展示当前项目条目，预览地址每次由对象存储重新签发；处理中的条目在角色页打开时继续同步 Token360 状态。
- 画布角色节点同时保存源图版本与站内角色 UUID：普通生图继续用源图版本，托管视频优先用角色 UUID 并在 Worker 内解析成 `asset://ta_...`。
- 本地移除采用归档语义，不删除源图和未公开文档语义下的 Token360 上游资产；数据库回滚脚本同样保留源素材与版本。

## 账号后台与消耗对账启动审计（2026-09-14）

- 现有后台能力仅为 `/operations` 中的管理员概览、失败任务和模型状态；没有独立管理布局、账号列表或账号操作。
- `users` 当前只有邮箱、密码哈希、`is_admin` 和创建时间；Session 为服务端 Cookie 会话，适合通过删除用户全部 Session 实现强制下线。
- `generation_jobs` 已保存用户、画布项目、模型、能力、视频 `provider_job_id` 和任务状态，但没有首次推理 Trace 或实际计费字段。
- 当前 Provider 丢弃推理响应头；视频轮询只使用资源 ID，需把首次提交响应头与轮询资源 ID 分离。
- Token360 官方账单接口使用首次推理 `X-Trace-ID` 查询；调用方可主动传入 Trace。账单可能短暂 404，视频轮询 Trace 不能替代首次提交 Trace。
- 当前自动生成尝试数为 1，但手动重试原地复用任务；本任务将手动重试改为新任务，确保一任务一 Trace 一账单。
- 管理后台视觉方向采用与现有产品一致的克制、信息密集型运营控制台，不引入独立品牌色或与画布冲突的视觉语言。
- API 与 Worker 都会启动时执行增量迁移；生成 Worker 已持有 PostgreSQL、Redis、Provider 与对象存储连接，适合在同一进程增加独立账单对账队列消费者。
- BullMQ 当前只有生成和合成队列；账单对账必须使用不同队列与 Job ID 前缀，避免和生成任务冲突，也不能复用模型任务的 attempts 语义。
- 当前 Worker 在 Provider 首次提交与视频轮询之间保持完整生命周期；最可靠的 Trace 捕获点是 `Token360Provider.create(request, correlationId)`，轮询 `get()` 不得更新 Trace。
- 账号状态必须在仓库的 Session 查询层统一收口，否则只在登录接口检查会让旧 Cookie 继续调用 API。
- 用户素材下载当前依赖项目成员权限；管理员只读排障不能复用普通下载 API 冒充成员，应由管理员服务签发受审计的专用只读下载地址。
- `/api/auth/me` 的用户类型目前只有 `id/email`；需要把数据库 `is_admin` 与 `ADMIN_EMAILS` 合并为有效管理员标记后返回，前端才能可靠隐藏 `/admin` 入口。
- 现有顶栏导航是面向所有用户的静态数组；管理后台应使用独立布局，并只在有效管理员会话中追加入口，而不是把后台栏目塞进普通导航数组。
- 迁移系统按文件校验哈希且 API/Worker 并发启动有 advisory lock，新增迁移必须只追加 `0016`，不能改写已应用迁移。
- Token360 账单响应的金额字段位于 `data`，包括基础、最终、总额、钱包、代金券、单价和币种；计量位于 `data.usage`，视频/音频时长使用 `seconds`，最终金额以账单数据为准。
- 对账状态 `mismatch` 同时承担“Trace 不一致”提示与最终异常状态；只有仍有 `billing_next_check_at` 的 mismatch 才能进入自动对账，最终 mismatch 必须停止自动扫描，避免重复请求账单接口。
- 管理后台最终采用独立 `/admin` 布局；普通用户既不显示入口，服务端每个 `/api/admin/*` 方法也再次验证管理员身份，前端隐藏不能替代后端权限。
# 2026-09-16 视频结果回传诊断

- 当日 5 个成功视频的生成后转存耗时为 282–1201 秒，文件仅 8–25MB，折算约 14–59KB/s。
- ECS 入站带宽上限 1000Mbps，数据库、MinIO 和磁盘均无瓶颈；同一结果新连接可数秒下载完成，根因为旧媒体连接间歇性降速。
- 现有下载器只检测连续 60 秒无数据；低速连接持续收到零星数据时不会重连，并占用生成 Worker 并发。
- Token360 媒体直链与 `/v1/videos/{id}/content?format=binary` 均支持 Range，可用于断点分块和双路由切换。
- 转存队列与生成队列使用相同本地任务 ID 但位于不同 BullMQ 队列，可独立并发和取消；恢复逻辑必须按任务状态选择队列，避免把已生成结果再次提交给模型。
- “正在写入资产库”必须在对象存储写入前发出，而不是在持久化完成后补发；已按真实阶段调整事件时序。

# 2026-09-21 海马云渠道接入初始发现

- 官方开发工具包要求优先阅读 `llms.txt` 和 `rh-api-contract.md`，模型 endpoint 与 schema 以 `model-registry.public.json` 为事实来源。
- 默认 API 基址为 `https://www.runninghub.cn/openapi/v2`，凭证环境变量示例为 `RH_API_KEY`；本地媒体必须先上传，再提交模型任务。
- 标准生命周期为：上传媒体 → 提交模型 endpoint → 保存 `taskId` → 轮询 `/query` → 处理 `SUCCESS` / `FAILED` / `CANCEL` → 解析输出。
- 官方公开注册表当前含 383 个模型；其中 291 个具备画布当前可安全映射的提示词与媒体输入契约并启用，其余 3D、上传/续写/分析等专用端点仅在管理员全量目录展示并保持未启用。
- 全局渠道选择保存在 PostgreSQL `platform_settings`，普通用户 `/api/models` 只读取当前渠道；任务表继续保存创建时的 provider，切换渠道不会改变已创建任务。
- 海马云没有复用 Token360 账单接口，相关任务按外部渠道显示无法进行 Token360 对账，避免产生伪账单。
- 公开价格表只能用于成本感知，不能替代最终账单或直接向用户承诺价格。

# 2026-09-21 海马云 Seedance 2.5 与消耗入账初始发现

- 海马云 Seedance 2.5 提供独立文生、图生和多模态视频 endpoint，不能直接依赖当前尚未收录它们的公开模型注册表。
- 用户提供的真实终态响应包含 `thirdPartyConsumeMoney`、`tokenUsage.usage.completion_tokens`、`tokenUsage.usage.total_tokens`、`billingSeconds`、`consumeCoins` 和 `taskCostTime`，可作为单次任务实际用量来源。
- 当前结论需修正：海马云任务能够提供实际用量；但金额币种若未随响应返回，不能自行标成 USD 或 CNY。
- 生成结果与消耗入账必须解耦：先完成结果转存，再幂等记录用量；用量缺失或格式异常只影响对账状态。
- 官方实时文档确认三条标准接口：`text-to-video`、`image-to-video`、`multimodal-video`；图生支持首帧/首尾帧，多模态最多 30 图、10 视频、10 音频，单段音视频 2–30 秒且参考总长不超过 30 秒。
- 三类示例共同返回异步任务并通过 `/query` 取终态；现有海马云上传和轮询实现可以复用，主要缺口是模型目录补充、终态 usage 解析与入账。
- 多模态接口使用 `imageUrls`/`videoUrls`/`audioUrls` 数组；图生使用 `firstFrameUrl`/`lastFrameUrl`；文生不携带媒体字段。公共参数包含 `resolution`、`duration`、`generateAudio`、`watermark`、`ratio`、`bitrateMode`、`seed`、`outputFormat`，另有路由特有参数。
- 当前公开 `model-registry.public.json` 仍未收录 Bytedance Seedance 2.5 三个 endpoint，因此仅刷新注册表不会出现模型；需要服务端补充目录，并在将来官方注册表收录后按 endpoint 去重、优先采用官方条目。
- `ProviderResult` 已有 `usage` 字段，JobExecutor 也会把终态 usage 写入 `billing_meter_usage`；缺口集中在 RunningHub Provider 未解析 usage，以及 BillingService 对非 Token360 一律标记 unavailable。
- 视频转存是独立队列，恢复时会再次查询终态；为避免崩溃窗口丢用量，普通执行和转存执行都应记录海马云 usage，最终在任务状态写成 completed/failed/cancelled 后幂等落一条 `generation_usage`。
- 现有 `generation_usage` 已能承载 Token、视频秒数、实际金额、Provider 请求 ID 和原始 usage JSON，无需修改已上线表结构；海马云未返回币种时保留 `currency=null`，管理后台应显示“原始单位”而不是误报“金额未返回”。

# 2026-09-21 海马云国际区 GPT Image 2.5 初始发现

- 用户截图确认国际站模型页面 ID 为 `213310000000800375`，接口路径为 `/rhart-image-g-2.5-official-token/sunburst/text-to-image`，模型名为 `gpt-image-2.5/sunburst/text-to-image/stable-token`。
- RunningHub 国际站采用 `https://www.runninghub.ai/openapi/v2`，同样通过 Bearer 鉴权提交任务并用 `/query` 查询异步结果；国际站凭证必须与中国区 `RH_API_KEY` 隔离。
- 官方国际站模型目录还包含 `gpt-image-2.5/sunburst/image-to-image/stable-token`，支持参考图输入；具体字段必须从模型详情契约核实，不能沿用旧版 GPT Image 2 参数猜测。
- 推荐内部使用独立 Provider 标识承载国际区密钥、基址、任务和消耗归属，管理员界面仍归入“海马云”并按中国区/国际区分组。
- 用户提供的海外 Key 文件仅作为配置来源；检查和使用过程中不得输出密钥正文或把密钥加入 Git。
- 国际区当前只引入已核实的两条 GPT Image 2.5 endpoint，不复用中国区动态注册表，避免把区域专属路由误导入另一套凭证。
- 文生图和参考图编辑共享提示词、画幅、分辨率、质量和透明背景语义；参考图编辑最多接受 16 张图片。
- 现有消费表可以复用；账单唯一标识必须包含 Provider 区域前缀，否则中外站相同 `taskId` 存在冲突风险。

# 2026-09-22 账号积分消费系统初始要求

- 用户要求每个账号拥有积分系统，兑换基准为 1 元人民币 = 100 积分。
- 用户侧最终消耗按供应商实际成本乘以 1.2 后换算积分，即人民币成本 × 120 并取整数；示例成本 10 元应扣 1200 积分。
- 实际成本来自现有 Token360/供应商用量回传，不应维护另一套估算价格作为最终扣款依据。
- 需要参考 `C:\Users\biaoc\Desktop\积分服务协议.docx` 确定用户展示、有效期、退款、扣费和争议处理语义。
- 工作区依赖定位工具返回已下线提示；后续使用本机可用的只读 DOCX 解析方式。

## 协议约束

- 积分是平台内部服务计量单位，不可转让、提现或跨账号使用；流水和余额必须归属单一用户账号。
- 购买积分有效期为到账后 12 个月；活动赠送积分可有独立有效期，因此账本需要按批次记录来源、到账量、剩余量和到期时间，扣费应支持按最早到期批次消耗。
- 付款后 7 日内且套餐消耗不超过 5%可申请按已用比例退款；因此购买批次必须保留原始积分、已用积分、支付单号和退款状态，不能只维护总余额。
- 系统故障或重复扣款应退还/补足积分；管理员修正必须使用不可删除的冲正流水，不能修改或删除原消费流水。
- 平台需公示各功能积分消耗说明和调整规则；前台至少要展示当前余额、任务实际扣分、扣费状态与流水明细。
- 协议仍含网站名称、运营主体、统一社会信用代码、注册地址、客服邮箱等上线占位符；本次系统实现不擅自替换这些法律主体信息。

## 读取问题

- 本机无 Pandoc 与 `python-docx`；`python-docx` 导入报 `ModuleNotFoundError`。已改用只读 OOXML 解析完整提取正文，原 DOCX 未修改。

## 现有实现审计

- 数据库已存在 `credit_accounts`、`credit_ledger`、`billing_plans` 和 `payment_orders` 占位结构；积分账户接口会懒创建账户并返回最近 100 条流水，但 `capabilities.credits=false` 且前端仅在“平台状态”显示占位余额。
- 现有 `credit_ledger` 已有全局唯一幂等键，适合用任务 ID 防重复扣款；但缺少购买/赠送批次、到期时间、批次剩余量及流水与批次分摊关系，无法满足协议的有效期和退款核算。
- Token360 账单由 `BillingService.reconcileJob` 写入 `generation_usage`；海马云终态用量也进入同一表。积分扣费应在实际用量成功持久化后执行，并与用量入账处于同一数据库事务，避免“账单已结算但积分未扣”或重复扣款。
- 当前 `BillingService` 多次独立执行 SQL，尚未使用事务；新增扣费时需要把用量 upsert、积分流水和任务计费状态收敛进事务。
- 当前余额是 `bigint`，适合整数积分；用户未指定小数成本换算后的取整规则。
- Token360 已有账单可能以 USD 返回，而用户定义的是人民币兑换积分；不能把 USD 数值直接当人民币，需要管理员可配置的 USD/CNY 换算率或用户明确固定汇率。

## 推荐实现决策

- 复用现有积分账户与流水，新增积分批次和流水分摊；购买/赠送/管理员发放均形成独立批次，消费按最早到期优先。
- 固定业务公式为 `人民币实际成本 × 100 × 1.2`，最终积分不足 1 点向上取整；公式和取整规则在用户积分页与协议中显著展示。
- CNY 账单直接计算；USD 账单使用超级管理员维护的 USD/CNY 结算汇率。汇率未配置、币种未知或实际金额未最终确认时只标记“待计费”，不得猜测或扣款。
- 任务实际用量先落库，再立即尝试独立积分结算事务；失败时保留待计费状态并由 Worker 扫描恢复。积分流水幂等键使用 `generation:<jobId>`，因此崩溃恢复和重复对账不会重复扣分。
- 实际成本只能在任务终态后确定，因此先实现后结算：余额可以因单次任务实际成本超出剩余积分而变成负数，随后阻止继续创建托管生成任务，直到管理员发放或未来支付充值补足；正在执行的任务继续完成。
- 管理员可发放购买、赠送或人工补偿积分，并填写说明与到期时间；所有调整进入审计和不可删除流水，不提供直接改余额或删除流水。
- 支付仍保持关闭，本轮不接支付宝回调；用户积分页展示余额、待计费和消费明细，管理员账号详情展示余额、批次、积分发放和任务扣分。
- 新增独立 `/credits` 用户页面并加入顶部导航；原“平台状态”中的积分占位不再作为主要入口。
- 迁移只为上线后的新账单启用积分结算；历史 `generation_usage` 统一标记 `historical`，不自动追溯扣分，避免已有账号上线后突然产生负余额。

## 前端与后台现状

- 路由和导航目前没有积分页，只有 `/operations` 平台状态卡片显示禁用余额。
- 管理员用户详情已有实际金额、任务和消耗流水，适合增加积分账户卡、发放入口及每笔用量的积分字段。
- 管理后台已有用户操作审计与数据库事务辅助，可复用到积分发放、补偿和定价设置。

## 协议修改的法务依据与风险

- 《民法典》第496—498条要求格式条款遵循公平原则，并对与消费者有重大利害关系的扣费、退款、责任限制等条款履行提示说明义务；歧义通常作不利于提供方的解释。
- 《消费者权益保护法实施条例》第10条要求显著标明服务内容、价格和计价方法；第22条要求预付款服务书面约定服务内容、费用、退款方式和违约责任，未按约提供时消费者可要求履行或退款。
- 《合同行政监督管理办法》第6—9条进一步要求以单独告知、加粗或弹窗等显著方式提示重大利害条款，不得用格式条款不合理免责或借助技术手段强制交易。
- 原协议把积分一概表述为“不代表运营方任何负债”和“已消费不退”，但同时约定购买套餐、余额有效期与比例退款，存在表述冲突；修订版应改为“内部计量单位，不影响消费者依法享有的退款、纠错等权利”。
- 风险等级为中等：涉及面向个人消费者的预付式服务与格式条款。可先形成修订稿，但上线前仍建议由持牌律师或人工法务结合真实运营主体、ICP备案主体和支付方案复核。

## 法务检索问题

- 首次使用 Windows 通配路径直接传给 `rg`，系统未展开通配符并返回路径错误；已改为对知识库目录使用 `-g` 文件过滤完成检索。
# 本轮关键发现

- 线上项目是本仓库 `keepper-max/infinite-canvas`，`H:\代码相关\项目\漫剧工作流` 为另一实验仓库；当前工作树包含上一轮未提交的积分余额/扣费代码。
- 海马云 `usage.third_party_consume_money` 已进入 `generation_usage`，但币种由 `usage.currency` 决定，缺失时目前无法扣费。按用户确认应视为 CNY。
- Token360 当前从 `platform_settings.credit_pricing.usdCnyRate` 读取管理员设置的汇率；用户最新明确改为在后台填写兑换比例，因此不接第三方报价。每笔已扣费账单仍保存所用比例。
- 支付宝审核未完成，本轮不得开启收款；当前真实项目还没有支付宝接口实现，需保持支付占位边界。

## 资产重复与回收站调查（2026-09-23）

- `PostgresAssetService.trash` 仅写 `assets.status='trashed'`/`trashed_at` 和 `trash_items`，没有对象存储删除；资产页回收站只有“恢复”，无永久删除入口。
- 上传每次用随机 `storage_key` 并创建新资产或版本；已有 SHA-256 用于上传校验而非去重。本任务暂不执行生产数据删除或调整保留期限。
- 线上只读统计：611 项资产中 609 活跃、2 项回收；608 个版本约 2.6 GB，回收版本仅约 8.9 MB。按同项目 SHA-256/字节去重，91 组、307 个额外版本，估计重复媒体约 656 MB；其中 `migration` 来源 415 个/约 902 MB。磁盘 40 GB 已用 33 GB、剩余 4.8 GB，不能将总磁盘压力全归于回收站。
- 重复版本按来源：本地迁移 294 项/594 MB、普通上传 11 项/24 MB、生成 2 项/38 MB；未见同一 `localAssetId` 的多次迁移，也未见同一个生成任务/存储键的重复版本。相同字节不等于可删资产：名称、来源、画布引用、任务记录可能不同。
- `asset_links` 目前 136 个版本/约 1.38 GB；`asset_versions.storage_key` 唯一，无法直接令不同版本共享同一对象键，安全去重需要调整存储模型与引用校验。
- 真正的系统盘大头在 `/var/lib/containerd` 约 23 GB；MinIO 卷约 2.6 GB，PostgreSQL 约 153 MB，旧发布备份约 683 MB；`docker system df` 虚拟镜像大小 24.4 GB，不能把它直接当成可删除空间。禁止对在线容器、回滚镜像或当前对象做无差别清理。
- 建议：先盘点镜像依赖和回滚基线、保留可回滚发布后定向清旧镜像；资产新增按项目+哈希复用物理对象但保留独立资产身份；回收站提供带引用检查与二次确认的永久删除，后台清理采用先标记/重试、再核对对象引用、最后删对象的可恢复流程。具体保留期限与立即清空范围需用户决定。

## 双路径清理与共享引用设计（2026-09-23）

- 用户明确手动永久删除和期限自动清理均需支持；自动保留天数尚待答复。
- `asset_versions.id` 被画布 `asset_links`、生成 `job_artifacts`、生成任务 `output_asset_version_ids`、合成任务与时间线、虚拟形象来源等引用；直接合并版本 ID 会混淆任务产物归属及不可变来源。正确层次是版本身份独立、底层对象按内容哈希共享。
- 当前 `asset_versions.storage_key` 唯一，意味着共享同一存储键需要增量迁移或引入 `asset_blobs` 表；删除共享对象前须核验所有版本/缩略图/任务引用并处理并发。
- Worker 当前负责生成/转存/合成，尚无资产回收定时队列；手动删除和自动过期应复用同一服务端清理流程，不可前端直接删存储对象。
- 最终范围按用户决定收窄：不做重复资产合并；回收站保留 14 天。手动与自动路径复用服务端清理队列，任务和费用记录保留，画布/虚拟角色引用会阻止删除。
- 清理事务先持久化全部原图/缩略图存储键，再解除任务产物引用并删除资产记录；对象删除失败时队列保持 pending，由 Worker 下次检查重试。
# 2026-09-24 支付订单套餐验收

- 用户确认订单有效期为 10 分钟。
- 正式套餐采用订单定价：9.9 元/100 积分、99.9 元/1000 积分，不再宣称固定“1 元兑换 100 积分”。
- 经营主体为“守密人（大连）科技有限公司”，售后联系方式为微信 `JPdai8888`。
- 0.01 元验收订单应为 1 积分，套餐元数据标记 `adminOnly` 和 `experimental`；服务端根据已认证用户管理员身份强制拦截普通用户，前端仅在超级管理员打开专用查询参数时展示。

# 2026-09-24 正式套餐与后台发布

- 用户确认 0.01 元真实支付验收通过；正式套餐调整为 9.9 元/1000 积分、99 元/10000 积分。
- `payment_orders` 已保存 `amount_cents`、`credits` 和套餐名称快照，后续调价不会改变历史订单金额与到账积分。
- 管理后台当前只有订单查看与主动同步，没有套餐写接口；应新增服务端超级管理员鉴权、输入校验与审计，而不能只做前端表单。
- 套餐采用草稿/已发布（`enabled`）状态，不物理删除；实验套餐在迁移中下架，且管理 API 拒绝编辑 `metadata.experimental=true` 的记录。
