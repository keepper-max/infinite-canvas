# AI 漫剧生产画布 2.0 执行总计划

## 目标

在 `keepper-max/infinite-canvas` 当前主线基础上，保留 Infinite Canvas 交互、Token360 全局模型、Seedance Skill、插件与 Codex Agent，补齐云端账号、画布持久化、资产版本、任务队列、漫剧生产节点和 FFmpeg 成片闭环。

## 当前阶段

专项任务“AI 文本工作台”已完成：已在现有服务端模型网关与 PostgreSQL 权威数据基础上增加可恢复的多轮文本创作入口。

## 专项任务：AI 文本工作台

### 阶段 A：契约与复用审计

- [x] 核对路由、导航、文本模型调用、项目资产和提示词库入口
- [x] 定稿会话/消息持久化契约与迁移
- **状态：** `complete`

### 阶段 B：服务端会话闭环

- [x] 增量迁移、会话/消息 API、权限边界与软删除
- [x] 文本生成任务绑定会话并在完成后物化助手消息
- **状态：** `complete`

### 阶段 C：文本工作台界面

- [x] 多轮对话、模型选择、快捷模式、停止/重试
- [x] 保存到提示词资产、发送到画布/生图/视频工作台
- **状态：** `complete`

### 阶段 D：验证与本地镜像

- [x] TypeScript、API 测试、生产构建
- [x] Docker 重建与浏览器端到端恢复验证
- **状态：** `complete`

## 执行口令

- `执行第 N 部分`：只执行对应任务包；开始前重读本文件、`findings.md`、`progress.md` 和任务包文件。
- `继续执行`：从当前任务包尚未完成的第一项继续，不自动跨越高风险确认点。
- `查询进度`：只读取规划与验证记录，不修改代码或服务器。
- `提交修改`：完成当前任务包验证后再创建提交；未收到该口令不 commit、不 push。
- `部署 ECS`：先备份线上配置和数据库，再部署已经验证的版本；不默认包含在普通“执行”中。

## 不可变原则

1. 不更换 Infinite Canvas 底座，不复制蔓悦私有代码、品牌、素材或文案。
2. 服务端是云端数据权威；IndexedDB 仅作本地缓存和旧数据迁移来源。
3. API Key 仅存在服务器环境变量或密钥存储中，不进入浏览器、日志、diff 和 Git。
4. 底层节点保持通用，漫剧用途通过 `workflowKind` 和节点注册表扩展。
5. 连线按端口类型和引用角色校验；模型请求按能力表过滤参数。
6. 生成结果不可覆盖，只创建新资产版本；删除默认进入回收站。
7. 每部分必须完成窄范围测试、整链路复查和进度记录后才算完成。
8. 生产部署、数据迁移和破坏性操作必须先备份并保留回滚路径。

## 任务包

### 第 0 部分：基线冻结与契约定稿

- 文件：`plans/ai-drama-canvas-2/00-baseline-and-contracts.md`
- 目标：确认当前 Git/ECS/数据基线，定稿节点、端口、任务、资产和模型能力契约。
- 状态：`complete`

### 第 1 部分：平台 API、账号与默认草稿空间

- 文件：`plans/ai-drama-canvas-2/01-platform-api-and-auth.md`
- 目标：建立 API 服务、PostgreSQL、注册登录、Session、用户隔离和默认草稿项目。
- 依赖：第 0 部分。
- 状态：`complete`

### 第 2 部分：云端画布持久化与旧数据迁移

- 文件：`plans/ai-drama-canvas-2/02-canvas-persistence.md`
- 目标：保存节点、端口连线、视口和快照；刷新恢复；安全迁移 IndexedDB 旧画布。
- 依赖：第 1 部分。
- 状态：`complete`

### 第 3 部分：OSS 资产与不可变版本系统

- 文件：`plans/ai-drama-canvas-2/03-assets-and-versions.md`
- 目标：统一图片、视频、音频资产，建立版本、来源、主版本、回收站和 OSS 转存。
- 依赖：第 1、2 部分。
- 状态：`complete`

### 第 4 部分：任务系统与全局模型网关

- 文件：`plans/ai-drama-canvas-2/04-jobs-and-model-gateway.md`
- 目标：Redis/BullMQ Worker、SSE、重试取消、幂等、动态模型目录和能力过滤。
- 依赖：第 1、3 部分。
- 状态：`complete`

### 第 5 部分：画布产品化升级

- 文件：`plans/ai-drama-canvas-2/05-canvas-productization.md`
- 目标：内容化节点、锁定、对齐、等距、背板、搜索、自动整理、性能模式、版本入口。
- 依赖：第 2、3 部分；可与第 4 部分后半段并行开发，但集成验收需第 4 部分完成。
- 状态：`complete`

### 第 6 部分：漫剧节点、分镜、角色与 Seedance Skill

- 文件：`plans/ai-drama-canvas-2/06-drama-workflow.md`
- 目标：剧本拆解、角色卡、场景、分镜表、首尾帧、多参考和 Seedance Skill 结构化输出。
- 依赖：第 3、4、5 部分。
- 状态：`complete`

### 第 7 部分：声音、字幕、时间线与 FFmpeg 成片

- 文件：`plans/ai-drama-canvas-2/07-composition.md`
- 目标：配音、音效、音乐、字幕、时间线和 Worker 内 FFmpeg 合成导出。
- 依赖：第 4、6 部分。
- 状态：`complete`

### 第 8 部分：运营接口预留、全链路验收与生产部署

- 文件：`plans/ai-drama-canvas-2/08-release-and-operations.md`
- 目标：短信/积分/支付接口占位，安全与恢复测试，ECS 备份、部署、监控和回滚演练。
- 依赖：第 1 至第 7 部分。
- 状态：`in_progress`（本地实现完成；生产备份、灰度切换、外部冒烟与回滚演练等待独立部署指令）

### 第 9 部分：生产发布编排与外部验收

- 文件：`plans/ai-drama-canvas-2/09-production-cutover.md`
- 目标：把生产预检、不可变镜像、备份校验、分阶段切换、健康检查和回滚整理为可重复执行的发布流程。
- 依赖：第 8 部分本地实现完成。
- 状态：`in_progress`（本地发布编排与验证完成；ECS 备份、分阶段切换、外部冒烟和回滚演练等待独立部署指令）

## 里程碑

- M1（第 0–2 部分）：登录后直接进入草稿画布，刷新后云端恢复。
- M2（第 3–4 部分）：真实生成任务在后台运行，产物进入 OSS 和版本库。
- M3（第 5–6 部分）：从故事、角色、场景、分镜到 Seedance 视频形成可用漫剧闭环。
- M4（第 7–8 部分）：声音、字幕、成片导出及可回滚的生产部署完成。

## 全局完成标准

- 多用户数据隔离；浏览器不含 API Key。
- 80 个以上媒体节点可操作，刷新后节点、连线、版本和任务不丢失。
- 重复点击不重复提交；关闭页面或重启服务后任务可恢复。
- 模型不支持的字段不会发送；普通用户看到中文错误，管理员可查看脱敏原始错误。
- 生成产物在临时 URL 过期前转存 OSS，并可追溯到输入、模型、Skill 和任务版本。
- 一集漫剧能够完成分镜批量生成、视频生成、声音字幕处理和 FFmpeg 合成导出。

## 错误记录

| 错误 | 次数 | 处理方式 |
|---|---:|---|
| 批量补丁同时删除并新增同一路径被拒绝 | 1 | 原文件未变化；先备份旧规划，再分批删除与新增 |
| 第 0 部分写入事实时两次匹配了错误原文 | 2 | 读取文件实际内容后用精确最小补丁完成；没有覆盖原文件 |
| 首次从 pg_restore 清单提取表名时取到了 owner 列 | 1 | 改为读取表名列并复核，确认 19 张业务表 |
| 同批补丁删除并新增工作流文件被拒绝 | 1 | 原文件未变化；拆成独立删除和新增补丁 |
| 英文待测试标题大小写不匹配导致文档补丁失败 | 1 | 核对实际标题后拆分为精确小补丁 |
| 部署保护测试先触发公网配置检查 | 1 | 把提交绑定的确认口令移动到所有部署参数之前，确保最先阻断 |
| Web 镜像脱离 Compose 执行 nginx -t 无法解析 api | 1 | 保持生产服务发现配置不变；测试时注入等价 api 主机名后复测 |
| 首次同时更新三份规划文件时 progress.md 模板标题与实际文件不一致 | 2 | 两次补丁均整体未写入；读取实际标题后改为分别追加，避免覆盖历史记录 |
| 前端格式化后类型检查发现通用 Workspace 的动态 role 被推断为 unknown | 1 | 在工作区壳边界增加字符串类型判断，保持默认 owner 回退 |
| 最新本地编排沿用外网来源白名单，注册首次返回 UNTRUSTED_ORIGIN | 1 | 仅为本地 API 运行环境补充 localhost/127.0.0.1 来源并关闭 Secure Cookie，生产配置未改动 |
| 端到端脚本按通用 `id`/`name` 读取项目摘要 | 1 | 核对现有契约后改用 `projectId`/`projectTitle`，接口本身正常 |
| 真实文本任务在 Worker 提交阶段失败 | 1 | 定位为本地未配置模型密钥；未把用户密钥写入命令、日志或仓库，也未重复调用上游 |

## 专项任务：项目云端单一数据源与端到端验收

### 阶段 A：主流程与副作用审计

- [x] 核对项目、成员、画布、任务与资产删除约束
- [x] 确认软删除及默认项目兜底策略
- **状态：** `complete`

### 阶段 B：服务端项目生命周期

- [x] 增量迁移与回滚脚本
- [x] 创建、重命名、软删除 API 与权限/边界测试
- **状态：** `complete`

### 阶段 C：前端单一云端数据源

- [x] 项目操作改走平台 API
- [x] IndexedDB 仅保留缓存和旧数据迁移来源
- [x] 处理保存失败、当前项目切换与默认项目兜底
- **状态：** `complete`

### 阶段 D：本地验证与最新镜像重建

- [ ] TypeScript、API 测试、Web/API 构建
- [ ] 使用最新源码重建并启动本地 Compose
- **状态：** `in_progress`

### 阶段 E：浏览器端到端验收

- [ ] 注册/登录与默认项目
- [ ] 项目创建、重命名、删除与刷新恢复
- [ ] 画布保存、真实生成、任务完成与刷新恢复
- **状态：** `pending`

## 专项任务：取消模型提交自动超时并修复画布 422

### 阶段 A：超时与取消链路审计
- [x] 定位本机失败任务与实际运行参数
- [x] 核对 Provider、Worker、手动取消和服务关闭信号
- **状态：** `complete`

### 阶段 B：关闭自动提交超时
- [x] 配置允许关闭提交超时并默认关闭
- [x] 仅保留用户手动停止与进程关闭中止
- **状态：** `complete`

### 阶段 C：画布 422 修复
- [x] 复现并读取服务端拒绝原因
- [x] 在正确的数据边界做最小修复
- **状态：** `complete`

### 阶段 D：本地重建与验证
- [x] 相关类型检查、测试与生产构建
- [x] 重建 API/Worker/Web 并验证配置与健康状态
- [x] 避免重复计费，以本地 API 往返和取消信号测试代替真实模型调用
- **状态：** `complete`
# 当前任务：模型/生成方式驱动的视频参数面板（2026-09-12）

## 目标
以 Seedance 2.5 为首个完整适配模型，将服务端模型目录、生成方式、前端参数控件、参考素材槽和最终请求编译连成同一条能力驱动链路，并让其他视频模型复用。

## 当前阶段
阶段 6：本地最新镜像与动态界面验收完成，等待真实模型任务验收

## 阶段
- [x] 阶段 1：核对现有模型目录、节点配置和请求编译链路
- [x] 阶段 2：扩展公共模型能力契约与前端类型
- [x] 阶段 3：实现模型/模式驱动的视频参数面板和参考素材约束
- [x] 阶段 4：统一视频工作台与画布节点提交映射
- [x] 阶段 5：补充回归测试、变更记录和待测试说明
- [x] 阶段 6：使用最新源码重建本地 Web/API/Worker，并验证动态参数界面
- [ ] 阶段 7：按不同生成方式各提交一次真实模型任务并核对上游参数

# 当前任务：全视频模型参数适配（2026-09-12）

## 目标
读取 Token360 当前视频模型目录及每个模型的参数 Schema，使所有模型按自身生成方式显示、校验和提交参数；未知新模型也能通过目录 Schema 自动接入。

## 当前阶段
阶段 5：完成

## 阶段
- [x] 阶段 1：抓取并归档当前视频模型、生成方式和参数差异
- [x] 阶段 2：补齐通用参数 Schema 规范化与能力推导
- [x] 阶段 3：扩展前端动态控件、参考槽和请求编译
- [x] 阶段 4：增加逐模型/逐模式契约测试与边界处理
- [x] 阶段 5：类型检查、测试、生产构建和本地界面验证

## 决策边界
- 模型目录是能力权威来源，已知模型仅用于修正目录缺失或命名不一致，不复制整份硬编码配置。
- 浏览器不直接访问 Token360，也不接触全局 API Key。
- 不向上游发送目录未声明或当前生成方式不支持的参数。
- 本轮不自动 commit、push 或部署 ECS。

## 决策
- 服务端模型目录是能力和默认值的权威来源，浏览器不直接请求上游目录。
- 前端保存规范化参数名；服务端负责映射上游字段并拒绝不支持组合。
- 先完整适配 Seedance 2.5，再通过同一 schema 渲染其他视频模型。
- 本轮不改变超时、重试、并发和大小上限。

## 错误记录
- API 类型检查首次发现模型枚举读取仍为 `unknown`；已把枚举解析函数显式收窄为数组后再继续验证。
- Web 类型检查首次发现可选图片角色与 Agent 未知输入没有完成类型收窄；已改为显式构造图片数组并把通过白名单的模式转换为字符串。

---
