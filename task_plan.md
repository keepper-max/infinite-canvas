# AI 漫剧生产画布 2.0 执行总计划

## 本轮：积分激活码、双渠道结算与 ECS 发布

- [ ] 核对未提交积分改动、海马云计量币种、支付宝开关和发布基线
- [ ] 落地积分激活码及本地发码器，支付宝保持关闭
- [ ] Token360 USD 按管理员填写的 USD/CNY 比例结算并冻结到逐笔账单；海马云 CNY 直接计分
- [ ] 相关测试、增量迁移备份、提交推送及 ECS 分阶段部署

不将实验仓库 `H:\代码相关\项目\漫剧工作流` 的改动混入此项目。

## 目标

在 `keepper-max/infinite-canvas` 当前主线基础上，保留 Infinite Canvas 交互、Token360 全局模型、Seedance Skill、插件与 Codex Agent，补齐云端账号、画布持久化、资产版本、任务队列、漫剧生产节点和 FFmpeg 成片闭环。

## 当前阶段

专项任务“AI 文本工作台”已完成：已在现有服务端模型网关与 PostgreSQL 权威数据基础上增加可恢复的多轮文本创作入口。

## 专项任务：AI 文本工作台

### 阶段 A：契约与复用审计

- [x] 核对路由、导航、文本模型调用、项目资产和提示词库入口
- [x] 定稿会话/消息持久化契约与迁移
- **状态：** `complete`

### 阶段 B：服务端会话闭环

- [x] 增量迁移、会话/消息 API、权限边界与软删除
- [x] 文本生成任务绑定会话并在完成后物化助手消息
- **状态：** `complete`

### 阶段 C：文本工作台界面

- [x] 多轮对话、模型选择、快捷模式、停止/重试
- [x] 保存到提示词资产、发送到画布/生图/视频工作台
- **状态：** `complete`

### 阶段 D：验证与本地镜像

- [x] TypeScript、API 测试、生产构建
- [x] Docker 重建与浏览器端到端恢复验证
- **状态：** `complete`

## 执行口令

- `执行第 N 部分`：只执行对应任务包；开始前重读本文件、`findings.md`、`progress.md` 和任务包文件。
- `继续执行`：从当前任务包尚未完成的第一项继续，不自动跨越高风险确认点。
- `查询进度`：只读取规划与验证记录，不修改代码或服务器。
- `提交修改`：完成当前任务包验证后再创建提交；未收到该口令不 commit、不 push。
- `部署 ECS`：先备份线上配置和数据库，再部署已经验证的版本；不默认包含在普通“执行”中。

## 不可变原则

1. 不更换 Infinite Canvas 底座，不复制蔓悦私有代码、品牌、素材或文案。
2. 服务端是云端数据权威；IndexedDB 仅作本地缓存和旧数据迁移来源。
3. API Key 仅存在服务器环境变量或密钥存储中，不进入浏览器、日志、diff 和 Git。
4. 底层节点保持通用，漫剧用途通过 `workflowKind` 和节点注册表扩展。
5. 连线按端口类型和引用角色校验；模型请求按能力表过滤参数。
6. 生成结果不可覆盖，只创建新资产版本；删除默认进入回收站。
7. 每部分必须完成窄范围测试、整链路复查和进度记录后才算完成。
8. 生产部署、数据迁移和破坏性操作必须先备份并保留回滚路径。

## 任务包

### 第 0 部分：基线冻结与契约定稿

- 文件：`plans/ai-drama-canvas-2/00-baseline-and-contracts.md`
- 目标：确认当前 Git/ECS/数据基线，定稿节点、端口、任务、资产和模型能力契约。
- 状态：`complete`

### 第 1 部分：平台 API、账号与默认草稿空间

- 文件：`plans/ai-drama-canvas-2/01-platform-api-and-auth.md`
- 目标：建立 API 服务、PostgreSQL、注册登录、Session、用户隔离和默认草稿项目。
- 依赖：第 0 部分。
- 状态：`complete`

### 第 2 部分：云端画布持久化与旧数据迁移

- 文件：`plans/ai-drama-canvas-2/02-canvas-persistence.md`
- 目标：保存节点、端口连线、视口和快照；刷新恢复；安全迁移 IndexedDB 旧画布。
- 依赖：第 1 部分。
- 状态：`complete`

### 第 3 部分：OSS 资产与不可变版本系统

- 文件：`plans/ai-drama-canvas-2/03-assets-and-versions.md`
- 目标：统一图片、视频、音频资产，建立版本、来源、主版本、回收站和 OSS 转存。
- 依赖：第 1、2 部分。
- 状态：`complete`

### 第 4 部分：任务系统与全局模型网关

- 文件：`plans/ai-drama-canvas-2/04-jobs-and-model-gateway.md`
- 目标：Redis/BullMQ Worker、SSE、重试取消、幂等、动态模型目录和能力过滤。
- 依赖：第 1、3 部分。
- 状态：`complete`

### 第 5 部分：画布产品化升级

- 文件：`plans/ai-drama-canvas-2/05-canvas-productization.md`
- 目标：内容化节点、锁定、对齐、等距、背板、搜索、自动整理、性能模式、版本入口。
- 依赖：第 2、3 部分；可与第 4 部分后半段并行开发，但集成验收需第 4 部分完成。
- 状态：`complete`

### 第 6 部分：漫剧节点、分镜、角色与 Seedance Skill

- 文件：`plans/ai-drama-canvas-2/06-drama-workflow.md`
- 目标：剧本拆解、角色卡、场景、分镜表、首尾帧、多参考和 Seedance Skill 结构化输出。
- 依赖：第 3、4、5 部分。
- 状态：`complete`

### 第 7 部分：声音、字幕、时间线与 FFmpeg 成片

- 文件：`plans/ai-drama-canvas-2/07-composition.md`
- 目标：配音、音效、音乐、字幕、时间线和 Worker 内 FFmpeg 合成导出。
- 依赖：第 4、6 部分。
- 状态：`complete`

### 第 8 部分：运营接口预留、全链路验收与生产部署

- 文件：`plans/ai-drama-canvas-2/08-release-and-operations.md`
- 目标：短信/积分/支付接口占位，安全与恢复测试，ECS 备份、部署、监控和回滚演练。
- 依赖：第 1 至第 7 部分。
- 状态：`in_progress`（本地实现完成；生产备份、灰度切换、外部冒烟与回滚演练等待独立部署指令）

### 第 9 部分：生产发布编排与外部验收

- 文件：`plans/ai-drama-canvas-2/09-production-cutover.md`
- 目标：把生产预检、不可变镜像、备份校验、分阶段切换、健康检查和回滚整理为可重复执行的发布流程。
- 依赖：第 8 部分本地实现完成。
- 状态：`in_progress`（本地发布编排与验证完成；ECS 备份、分阶段切换、外部冒烟和回滚演练等待独立部署指令）

## 里程碑

- M1（第 0–2 部分）：登录后直接进入草稿画布，刷新后云端恢复。
- M2（第 3–4 部分）：真实生成任务在后台运行，产物进入 OSS 和版本库。
- M3（第 5–6 部分）：从故事、角色、场景、分镜到 Seedance 视频形成可用漫剧闭环。
- M4（第 7–8 部分）：声音、字幕、成片导出及可回滚的生产部署完成。

## 全局完成标准

- 多用户数据隔离；浏览器不含 API Key。
- 80 个以上媒体节点可操作，刷新后节点、连线、版本和任务不丢失。
- 重复点击不重复提交；关闭页面或重启服务后任务可恢复。
- 模型不支持的字段不会发送；普通用户看到中文错误，管理员可查看脱敏原始错误。
- 生成产物在临时 URL 过期前转存 OSS，并可追溯到输入、模型、Skill 和任务版本。
- 一集漫剧能够完成分镜批量生成、视频生成、声音字幕处理和 FFmpeg 合成导出。

## 错误记录

| 错误 | 次数 | 处理方式 |
|---|---:|---|
| 批量补丁同时删除并新增同一路径被拒绝 | 1 | 原文件未变化；先备份旧规划，再分批删除与新增 |
| 第 0 部分写入事实时两次匹配了错误原文 | 2 | 读取文件实际内容后用精确最小补丁完成；没有覆盖原文件 |
| 首次从 pg_restore 清单提取表名时取到了 owner 列 | 1 | 改为读取表名列并复核，确认 19 张业务表 |
| 同批补丁删除并新增工作流文件被拒绝 | 1 | 原文件未变化；拆成独立删除和新增补丁 |
| 英文待测试标题大小写不匹配导致文档补丁失败 | 1 | 核对实际标题后拆分为精确小补丁 |
| 部署保护测试先触发公网配置检查 | 1 | 把提交绑定的确认口令移动到所有部署参数之前，确保最先阻断 |
| Web 镜像脱离 Compose 执行 nginx -t 无法解析 api | 1 | 保持生产服务发现配置不变；测试时注入等价 api 主机名后复测 |
| 首次同时更新三份规划文件时 progress.md 模板标题与实际文件不一致 | 2 | 两次补丁均整体未写入；读取实际标题后改为分别追加，避免覆盖历史记录 |
| 前端格式化后类型检查发现通用 Workspace 的动态 role 被推断为 unknown | 1 | 在工作区壳边界增加字符串类型判断，保持默认 owner 回退 |
| 最新本地编排沿用外网来源白名单，注册首次返回 UNTRUSTED_ORIGIN | 1 | 仅为本地 API 运行环境补充 localhost/127.0.0.1 来源并关闭 Secure Cookie，生产配置未改动 |
| 端到端脚本按通用 `id`/`name` 读取项目摘要 | 1 | 核对现有契约后改用 `projectId`/`projectTitle`，接口本身正常 |
| 真实文本任务在 Worker 提交阶段失败 | 1 | 定位为本地未配置模型密钥；未把用户密钥写入命令、日志或仓库，也未重复调用上游 |

## 专项任务：项目云端单一数据源与端到端验收

### 阶段 A：主流程与副作用审计

- [x] 核对项目、成员、画布、任务与资产删除约束
- [x] 确认软删除及默认项目兜底策略
- **状态：** `complete`

### 阶段 B：服务端项目生命周期

- [x] 增量迁移与回滚脚本
- [x] 创建、重命名、软删除 API 与权限/边界测试
- **状态：** `complete`

### 阶段 C：前端单一云端数据源

- [x] 项目操作改走平台 API
- [x] IndexedDB 仅保留缓存和旧数据迁移来源
- [x] 处理保存失败、当前项目切换与默认项目兜底
- **状态：** `complete`

### 阶段 D：本地验证与最新镜像重建

- [ ] TypeScript、API 测试、Web/API 构建
- [ ] 使用最新源码重建并启动本地 Compose
- **状态：** `in_progress`

### 阶段 E：浏览器端到端验收

- [ ] 注册/登录与默认项目
- [ ] 项目创建、重命名、删除与刷新恢复
- [ ] 画布保存、真实生成、任务完成与刷新恢复
- **状态：** `pending`

## 专项任务：取消模型提交自动超时并修复画布 422

### 阶段 A：超时与取消链路审计
- [x] 定位本机失败任务与实际运行参数
- [x] 核对 Provider、Worker、手动取消和服务关闭信号
- **状态：** `complete`

### 阶段 B：关闭自动提交超时
- [x] 配置允许关闭提交超时并默认关闭
- [x] 仅保留用户手动停止与进程关闭中止
- **状态：** `complete`

### 阶段 C：画布 422 修复
- [x] 复现并读取服务端拒绝原因
- [x] 在正确的数据边界做最小修复
- **状态：** `complete`

### 阶段 D：本地重建与验证
- [x] 相关类型检查、测试与生产构建
- [x] 重建 API/Worker/Web 并验证配置与健康状态
- [x] 避免重复计费，以本地 API 往返和取消信号测试代替真实模型调用
- **状态：** `complete`
# 当前任务：模型/生成方式驱动的视频参数面板（2026-09-12）

## 目标
以 Seedance 2.5 为首个完整适配模型，将服务端模型目录、生成方式、前端参数控件、参考素材槽和最终请求编译连成同一条能力驱动链路，并让其他视频模型复用。

## 当前阶段
阶段 6：本地最新镜像与动态界面验收完成，等待真实模型任务验收

## 阶段
- [x] 阶段 1：核对现有模型目录、节点配置和请求编译链路
- [x] 阶段 2：扩展公共模型能力契约与前端类型
- [x] 阶段 3：实现模型/模式驱动的视频参数面板和参考素材约束
- [x] 阶段 4：统一视频工作台与画布节点提交映射
- [x] 阶段 5：补充回归测试、变更记录和待测试说明
- [x] 阶段 6：使用最新源码重建本地 Web/API/Worker，并验证动态参数界面
- [ ] 阶段 7：按不同生成方式各提交一次真实模型任务并核对上游参数

# 当前任务：全视频模型参数适配（2026-09-12）

## 目标
读取 Token360 当前视频模型目录及每个模型的参数 Schema，使所有模型按自身生成方式显示、校验和提交参数；未知新模型也能通过目录 Schema 自动接入。

## 当前阶段
阶段 5：完成

## 阶段
- [x] 阶段 1：抓取并归档当前视频模型、生成方式和参数差异
- [x] 阶段 2：补齐通用参数 Schema 规范化与能力推导
- [x] 阶段 3：扩展前端动态控件、参考槽和请求编译
- [x] 阶段 4：增加逐模型/逐模式契约测试与边界处理
- [x] 阶段 5：类型检查、测试、生产构建和本地界面验证

## 决策边界
- 模型目录是能力权威来源，已知模型仅用于修正目录缺失或命名不一致，不复制整份硬编码配置。
- 浏览器不直接访问 Token360，也不接触全局 API Key。
- 不向上游发送目录未声明或当前生成方式不支持的参数。
- 本轮不自动 commit、push 或部署 ECS。

## 决策
- 服务端模型目录是能力和默认值的权威来源，浏览器不直接请求上游目录。
- 前端保存规范化参数名；服务端负责映射上游字段并拒绝不支持组合。
- 先完整适配 Seedance 2.5，再通过同一 schema 渲染其他视频模型。
- 本轮不改变超时、重试、并发和大小上限。

## 错误记录
- API 类型检查首次发现模型枚举读取仍为 `unknown`；已把枚举解析函数显式收窄为数组后再继续验证。
- Web 类型检查首次发现可选图片角色与 Agent 未知输入没有完成类型收窄；已改为显式构造图片数组并把通过白名单的模式转换为字符串。

# 当前任务：Virtual Portrait 角色资产库

## 目标
在现有画布和云端资产体系中增加 Token360 Virtual Portrait 角色资产库，安全保存并使用 Token360 `assetId`；视频生成时按所选模式映射为 `asset://{assetId}`，浏览器不接触全局 API Key。

## 阶段
- [x] 阶段 1：核对 Token360 官方资产接口、引用格式及模式限制
- [x] 阶段 2：审计现有资产数据库、服务端 API、画布资产侧栏和视频请求编译链
- [x] 阶段 3：确定数据模型、服务端代理接口和权限/删除语义
- [x] 阶段 4：实现 Virtual Portrait 资产库 UI、创建/同步/选择/拖入画布
- [x] 阶段 5：实现视频节点引用角色和服务端 `asset://` 请求映射
- [x] 阶段 6：补测试、变更记录并完成相关验证

## 当前状态
阶段 6 已完成；功能、测试和待测试文档均已补齐。本轮未自动 commit、push 或部署 ECS。

## 决策边界
- Token360 API Key 只由服务端环境变量读取。
- `assetId` 是 Virtual Portrait 资产身份；分组 ID 和资产记录 ID 不能替代。
- 复用现有项目权限、云端资产版本和任务链路，不建立浏览器直连 Token360 的第二套数据源。
- 不擅自新增超时、自动重试、并发或文件大小默认值。

---

# 当前任务：账号管理后台与 Token360 消耗对账

## 目标
建设独立 `/admin` 管理后台，补齐账号启停、强制下线、只读业务排障、全局任务与审计；将每条生成任务 UUID 作为 Token360 首次推理 Trace，并通过官方账单接口持续对账实际消耗。

## 阶段
- [x] 阶段 1：审计鉴权、任务、Provider、数据库和现有运营页
- [x] 阶段 2：增量迁移、账号状态与管理员鉴权
- [x] 阶段 3：任务 Trace、手动重试新任务与消耗对账 Worker
- [x] 阶段 4：管理员查询/操作 API 与审计
- [x] 阶段 5：独立后台布局、账号/消耗/任务/模型/审计页面
- [x] 阶段 6：定向测试、类型检查、构建、文档与整链路复审

## 当前状态
本地实现与验证完成；等待单独的 commit、push 和生产备份/部署指令。

## 决策边界
- 账号停用立即撤销会话并阻止新请求，已进入队列或运行中的任务继续完成。
- 管理员只读查看用户业务内容，不冒充登录，不代用户修改、删除或生成。
- 本地生成任务 UUID 对齐 Token360 `X-Trace-ID`；视频资源 ID 继续单独保存。
- 手动重试创建新任务并关联原任务，不复用旧任务 ID。
- Token360 实际账单是金额权威；历史无 Trace 任务不猜测费用。
- 账单重查最长 24 小时，不触发模型重试。

## 错误记录
- 首次同时追加三份规划文件时，`findings.md` 末尾与技能模板不同，补丁被整体拒绝；核对实际文件末尾后分别追加，产品代码未受影响。
- 首次账单服务大补丁把回滚文件上下文误用于迁移文件，补丁整体拒绝；拆分迁移字段和服务新增后继续。
- 首次类型检查误从仓库根执行 `pnpm --filter`，因根目录没有 package.json 失败；随后 `pnpm --dir web` 又尝试接管 npm 安装的依赖。已停止该路径，改用各子项目现有 npm 脚本验证，不提交依赖目录变化。
# 当前任务：视频结果快速回传与断点转存（2026-09-16）

## 目标
解决 Token360 已完成后 ECS 下载链路间歇性低速导致画布长时间停留“生成中”的问题；使用低速检测、4MB Range 分块、直链/content 双路由及独立 4 并发转存队列，且不重复生成。

## 阶段
- [x] 阶段 1：生产耗时与根因核验
- [x] 阶段 2：下载器、任务队列与恢复语义实现
- [x] 阶段 3：画布阶段状态与下载进度展示
- [x] 阶段 4：定向测试、类型检查、构建与文档复审

## 当前状态
本地实现与验证完成；改动保留在工作区，未提交、推送或部署。

## 边界
- 30 秒窗口低于 64KB/s 视为低速；4MB 分块；转存并发 4。
- 恢复仅重取已有 Provider 结果，不提交新的模型请求。
- 本轮不自动提交、推送或部署。

---

# 当前任务：海马云渠道接入与管理员渠道治理（2026-09-21）

## 目标
按 RunningHub/海马云官方契约接入服务端托管模型渠道；普通用户端不展示渠道配置或切换，超级管理员在独立后台统一选择启用渠道；管理后台模型页按 Token360 / 海马云二级菜单分组展示完整目录。

## 阶段
- [x] 阶段 1：读取官方入口说明并建立接入约束
- [x] 阶段 2：审计现有 Provider、模型目录、配置与管理员路由
- [x] 阶段 3：读取完整 API 契约与模型注册表，确定首批可映射模型
- [x] 阶段 4：实现服务端渠道配置、海马云客户端与任务生命周期
- [x] 阶段 5：实现用户端隐藏、管理员渠道切换和模型二级菜单
- [x] 阶段 6：补迁移/测试/文档并完成整链路验证

## 边界
- API Key 只从服务端环境变量读取，不进入浏览器、数据库明文、日志或版本库。
- 管理员渠道切换必须由服务端鉴权，不能只做前端隐藏。
- RunningHub endpoint、字段、状态和媒体限制以官方 `model-registry.public.json` 与契约为准，不猜测。
- 本轮不自动 commit、push 或部署 ECS；未提供 API Key 前不调用真实计费任务。

---

# 当前任务：海马云 Seedance 2.5 与实际消耗入账（2026-09-21）

## 目标
把海马云 Seedance 2.5 文生、图生/首尾帧和多模态视频接入现有托管模型后台；生成终态返回的实际金额、Token 与计费秒数写入统一消耗流水，并在管理员后台按海马云渠道查询汇总。

## 阶段
- [x] 阶段 1：审计海马云任务返回、模型目录、任务终态和现有消耗入账链路
- [x] 阶段 2：补充 Seedance 2.5 三类服务端模型定义及按生成模式路由
- [x] 阶段 3：解析海马云 `usage`/金额字段并幂等写入消耗流水
- [x] 阶段 4：补管理员消耗/任务展示与渠道归属语义
- [x] 阶段 5：补契约测试、迁移/回滚、变更记录并复审

## 当前状态
本地实现与验证完成；现有表结构可复用，无需数据库迁移。本轮只修改工作区，未 commit、push、部署，也未发起真实计费任务。

## 边界
- API Key 继续只从服务端环境变量读取。
- 金额字段使用 PostgreSQL `numeric` 与字符串接口值，不用浮点数。
- 金额单位和币种以海马云明确返回为准；没有币种字段时不伪造币种。
- 上游生成成功与消耗同步分别处理；消耗解析失败不得把已成功生成改成失败。
- 不改变现有超时、重试、并发、大小限制或生产渠道选择。

## 错误记录
- 首次模型目录合并补丁因 `catalogItems` 的真实位置与预期上下文不一致而整体拒绝，产品代码未发生部分修改；改为按方法和辅助函数的准确位置拆分补丁。

---

# 当前任务：海马云国际区 GPT Image 2.5 接入（2026-09-21）

## 目标
在不影响现有海马云中国区 `.cn` 模型的前提下，使用独立海外凭证接入 RunningHub `.ai` 的 GPT Image 2.5 文生图与参考图能力，并在管理员模型与消耗视图中明确区分中国区和国际区。

## 阶段
- [x] 阶段 1：核对国际站官方入口、鉴权方式与异步任务生命周期
- [x] 阶段 2：审计现有 Provider、模型目录、渠道设置、计费和管理员展示
- [x] 阶段 3：实现国际区独立配置、Provider 路由与 GPT Image 2.5 模型契约
- [x] 阶段 4：实现管理员区域分组、任务归属和实际用量展示
- [x] 阶段 5：补测试、文档并完成整链路验证

## 当前状态
本地实现与验证完成；国际区密钥未写入项目或 Git。本轮未 commit、push、部署，也未提交真实计费任务。

## 边界
- 海外 Key 只从 `RH_GLOBAL_API_KEY` 读取，不输出、不写入代码或版本库，也不复用中国区 Key。
- 国际区基址独立使用 `https://www.runninghub.ai/openapi/v2`；中国区配置保持不变。
- 未确认的模型参数不猜测；优先读取官方详情或可验证的契约。
- 本轮不自动 commit、push 或部署 ECS，也不发起真实计费任务。

## 错误记录
- 首次同时追加三份规划文件时，`findings.md` 的实际标题与预期上下文不同，补丁被整体拒绝；产品代码未发生修改，改为按文件末尾分别追加。

---

# 当前任务：账号积分消费系统（2026-09-22）

## 目标
为每个账号建立可审计的积分余额与消费流水；按 1 元 = 100 积分、实际供应商成本 × 1.2 计算最终扣除积分，并把余额、单次消费及任务关联展示给用户和管理员。

## 阶段
- [x] 阶段 1：完整读取积分服务协议，审计现有积分、账号、生成任务和实际消耗链路
- [x] 阶段 2：确定币种、换算精度、入账时点、幂等与余额不足策略
- [x] 阶段 3：实现增量数据库、服务端积分账本和实际消耗扣费
- [x] 阶段 4：实现用户积分余额/流水和管理员账户积分管理界面
- [x] 阶段 5：补迁移、测试、文档并复审端到端计费链路
- [x] 阶段 6：生成带修订痕迹的积分服务协议并完成 OOXML/正文验证

## 当前状态
本地实现与验证完成；迁移已在隔离 PostgreSQL 16 执行，积分幂等、批次顺序、负余额抵扣、API/Web 类型检查和全量测试均已验证。本轮尚未 commit、push 或部署。

## 边界
- 供应商实际金额先按明确币种换算成人民币；币种未知时不得猜测或扣费。
- 积分使用整数账本，金额和换算中间值使用 PostgreSQL `numeric`，不得用浮点数。
- 同一生成任务只能产生一次最终消费扣款，重复对账必须幂等。
- 计费、余额和管理员调账均需审计，不能直接覆盖余额或删除流水。
- 本轮不自动 commit、push 或部署 ECS。
- 原协议不覆盖；修订稿输出到 `H:\CodexStorage`，并保留可审阅的修订痕迹。

## 错误记录
- 工作区依赖定位工具已下线，未获得 bundled runtime；改为先核查本机已有的 DOCX 读取能力，不重复调用失效入口。
- Windows 通配路径直接传给 `rg` 未展开；改用目录搜索加 `-g` 文件过滤完成法务知识库检索。
- 首轮 API 类型检查发现 `DomainError` 的状态码类型不接受 402；积分不足改为项目已支持的 409 业务冲突状态。
- 首轮 Web 类型检查发现当前编译目标不支持 BigInt 字面量；保留精确整数比较，改用 `BigInt(0)` 构造形式。
- DOCX 修订脚本首次插入新段落时，原段落属性含未绑定命名空间前缀，安全解析器拒绝；未生成输出文件，改为使用与正文一致的最小标准段落属性后重新执行。
- 协议验证脚本最初检查字面量“USD”，但修订稿采用更完整的“非人民币成本”表述；文件结构和前六项检查均正常，改为校验实际条款措辞。
- 一次管理员页面定位用的正则缺少闭合分组；未改代码，改用简单关键词搜索继续。

## 当前任务：资产重复与回收站占用排查（2026-09-23）

- [x] 追踪资产创建、版本、引用、回收站和对象存储删除链路。
- [x] 只读核查线上重复与容量规模，区分重复记录和重复文件。
- [x] 评估安全去重与彻底清理方案、影响和回滚边界。
- [x] 用户确定只做回收站永久删除与 14 天自动清理；不实施重复资产合并，也不清除现有线上数据。

## 当前任务续：双路径清理与同内容引用设计（2026-09-23）

- [x] 核对画布、任务、版本与存储键的引用边界及后台任务入口。
- [x] 定义手动永久删除和期限自动清理的共同安全条件与失败恢复。
- [x] 定义同内容版本/底层文件共享模型，避免改写历史任务归属。
- [x] 已按 14 天保留期实现；任务和费用记录保留，画布/虚拟角色有引用时阻止永久删除。本轮不清理生产数据。

### 验证错误

- 首次从仓库根目录执行 npm workspace 命令失败：根目录没有 `package.json`，本项目 Web 与 API 分别管理依赖；改为进入各自目录运行。

### 当前结果

- 手动永久删除、14 天定时清理、引用保护、对象存储失败重试均已实现。
- API/Web 类型检查、差异检查及带 PostgreSQL 16 的 85 项测试全部通过。
- 未 commit、push、部署或清理线上资产。
