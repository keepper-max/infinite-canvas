# AI 漫剧生产画布 2.0：进度记录

## 账号管理后台与 Token360 消耗对账（2026-09-14）

- **状态：** 本地实现与验证完成，尚未 commit、push 或部署。
- **数据链路：** 新任务 UUID 同时作为首次请求 Trace；视频资源 ID 独立保存；手动重试创建新任务并关联旧任务；终态任务由 Worker 按退避规则同步实际账单。
- **后台：** 新增独立 `/admin`，包含概览、账号、消耗、任务、模型和审计；支持账号启停、强制下线、管理员设置、只读项目排障和手动对账。
- **验证：** API 类型检查、构建通过；56 项 API 测试中 55 通过、1 项 PostgreSQL 集成测试因未配置隔离数据库跳过；Web 类型检查与生产构建通过；`git diff --check` 通过。当前 Docker Desktop 未运行，因此未执行临时 PostgreSQL 容器迁移演练。

## AI 文本工作台（2026-09-11）

- **状态：** complete
- **服务端：** 新增 PostgreSQL 会话/消息表、项目成员权限校验、会话增删改查、消息恢复，以及与现有 Redis 生成任务的绑定和结果物化。
- **前端：** 新增独立文本工作台、五种创作模式、全量文本模型选择、停止/重试、历史恢复，并支持把结果保存为提示词素材或发送到画布、生图、视频工作台。
- **验证：** Web/API TypeScript 与生产构建通过，API 测试 28/28 通过（1 项隔离 PostgreSQL 测试按设计跳过）；本地 0013 迁移成功，真实 GPT-5.5 文本任务完成，刷新后恢复同一会话和消息，发送到生图工作台后提示词正确带入。
- **Docker 记录：** 首次组合本地覆盖文件因 `model-proxy` 缺少健康检查而阻断 Web；旧数据卷未动，已清理未启动临时容器并恢复主 Compose，Web/API/Worker 与 PostgreSQL/Redis/MinIO 全部健康。

## 项目云端单一数据源与端到端验收（2026-09-10）

- **状态：** complete
- **已开始：** 重读总计划、仓库规则、既有发现和验证记录。
- **执行边界：** 项目删除采用软删除；不修改 ECS，不产生 Git 提交，不把密钥写入代码或日志。
- **当前阶段：** 审计项目/成员/画布/任务数据流和现有前端项目操作。
- **审计结论：** 采用 `projects.deleted_at` 增量迁移；所有项目访问入口排除软删除记录；后台活动任务在删除事务内切换到取消请求；前端项目生命周期统一改走平台 API。
- **阶段 A 完成：** 已追踪项目、成员、画布、素材、生成任务、成片任务与团队关联的访问链，确认软删除副作用和权限边界。
- **阶段 B 进行中：** 已新增 0012 增量迁移/回滚脚本、项目 POST/PATCH/DELETE 路由、仓库事务方法，并开始补单元与 PostgreSQL 集成测试。
- **错误记录：** 前端格式化后类型检查发现 `Workspace` 的动态 `role` 不能直接赋给字符串字段；已增加显式字符串判断，未改变接口语义。
- **阶段 B 完成：** 0012 迁移在隔离 PostgreSQL 16 上通过；28/28 API 测试通过，覆盖创建、改名、软删除、删除后访问阻断和默认工作区兜底。
- **阶段 C 完成：** 项目页、项目卡、批量删除、快捷新建和 ZIP 导入全部等待平台 API 成功后更新；服务端列表成为活动项目唯一来源，旧本地项目转存为隐藏迁移副本。
- **阶段 D 完成：** API/Web 类型检查与生产构建通过；最新 API、Worker、Web 镜像已在 `canvas-part5-qa` 原数据卷上重建，0012 迁移、服务健康和 Worker 内 FFmpeg 均通过。
- **数据保护：** 重建前 PostgreSQL 自定义格式备份保存到 `H:\CodexStorage\infinite-canvas-backups\canvas-part5-qa-pre-project-lifecycle-20260911-100114.dump`，`pg_restore` 清单可读取；未重置 PostgreSQL、Redis 或 MinIO 数据卷。
- **端到端结果：** 隔离账号注册、默认项目、项目创建/改名/软删除、画布保存、新登录会话恢复节点/视口/版本全部通过；真实 GPT-5.5 任务已进入 Redis Worker，随后因本地未配置 `TOKEN360_API_KEY` 以 `PROVIDER_AUTH_FAILED` 失败。
- **最终验收：** `.env` 密钥已同时加载到 API/Worker；真实 GPT-5.5 任务按 `queued → submitting → completed` 完成至 100%，全新登录会话恢复同一画布 revision 1 和 completed 任务。密钥未进入代码、日志、备份或 Git。

## 当前状态

- 当前任务包：第 9 部分发布编排与本地验证已完成。
- 下一动作：确认生产容量阈值；收到独立的 `部署 ECS` 指令后执行备份、分阶段切换、外部冒烟和回滚演练。
- 是否允许自动进入下一任务包：否；每个任务包完成后先汇报验证结果。
- 是否允许提交或推送：否；等待用户明确说“commit、push”。
- 是否允许生产部署：否；等待用户明确说“部署 ECS”。

### 第 9 部分执行记录

- 已确认原总计划只定义到第 8 部分；为避免把 `执行第9部分` 误当作生产授权，新增独立的生产发布编排任务包，ECS 修改仍受 `部署 ECS` 口令保护。
- 只读 Compose 检查因本机未设置生产必填变量而拒绝解析；这是预期安全结果，未启动、停止或修改任何容器。
- 已确认现有发布工具缺少脱敏预检、不可变镜像发布清单和脚本化回滚，GitHub 工作流也尚未分别产出 Web 与 API/Worker 镜像。
- 已新增脱敏生产预检、提交号绑定的显式部署确认、备份校验、Web/API 独立镜像、API/Worker 优先切换、失败自动恢复和按发布清单回滚入口。
- Compose 支持通过 `WEB_IMAGE` / `API_IMAGE` 切换不可变镜像；GitHub Actions 改为分别构建并发布当前仓库所有者名下的 Web/API 多架构镜像。
- API 类型检查、26 项单元测试、API 生产构建、前端类型检查与生产构建、脚本语法、Compose 配置解析和未授权失败保护均通过；PostgreSQL 集成测试因本轮未启动隔离数据库按设计跳过 1 项。
- 本机 Docker 已实际构建 `api` 和 `web` 两个目标；API 镜像确认包含 FFmpeg 与迁移 CLI。Web 镜像在注入等价 Compose 服务发现后通过 Nginx 配置检查。
- 最终 `git diff --check`、Shell 语法、Compose 配置解析、部署/预检失败保护与密钥扫描均通过；没有修改 ECS、提交 Git 或推送 GitHub。

### 第 4 部分执行记录

- 用户已确认运行默认值：Worker 并发 2、自动重试 2 次（最多 3 次尝试）、Provider 提交超时 60 秒、视频轮询 5 秒、任务最长运行 30 分钟；取消先进入 `cancel_requested`，待 Worker 停止或 Provider 确认后进入 `cancelled`，全部允许由环境变量覆盖。
- 已安装并锁定 `bullmq@6.3.4` 与 `ioredis@6.0.0`，准备在现有平台 API 包内增加独立 Worker 入口。
- 已复核当前云端 schema：画布节点使用 `(project_id, text id)` 复合主键，旧任务表的 UUID `node_id` 不能直接复用；兼容迁移将新增文本型 `node_key`，保留旧列供回滚。

- 已重读总计划、第 4 部分任务包、资产任务契约和模型网关契约。
- 已开始审计当前浏览器生成、服务端模型代理、旧任务表及 Compose 服务边界。
- 已确认根目录 Bun 代理是当前托管模型入口，平台 API/Worker 需要渐进接管；旧任务表采用增量扩展，不删除历史数据。
- 已核对 Token360 模型说明和 BullMQ 官方生产建议，确认能力白名单、数据库+队列双重幂等、API/Worker 分离 Redis 策略与协作取消是必要边界。
- 用户已确认默认运行边界：Worker 并发 2、自动重试 2 次、提交超时 60 秒、视频轮询 5 秒、单任务最长 30 分钟，并全部支持环境变量覆盖。

### 第 3 部分执行记录

- 已重读总计划、第 3 部分任务包、仓库级约束和资产/任务契约索引。
- 已确认本部分需要统一图片、视频、音频资产，建立不可变版本、主版本、来源追溯、回收站和对象存储抽象。
- 已完成旧 `assets` 兼容增量迁移、新资产版本/引用/上传/回收站表及手动回滚脚本。
- 已完成 S3 兼容对象存储抽象；本地 MinIO 与生产 OSS 共用适配器，内部 endpoint 与浏览器 endpoint 可分别配置。
- 已完成服务端签名直传、大小/类型/SHA-256 校验、缩略图、短期下载地址、用户与项目隔离。
- 已完成不可变版本、主版本切换、并排比较、从任意旧版本继续、任务/模型/Skill 来源字段和回收站恢复。
- 已完成画布保存时精确资产版本引用同步，切换主版本不会改变已固定的节点版本，跨项目版本引用被拒绝。
- 已完成项目云端资产页与旧 IndexedDB 媒体显式迁移；成功逐项记录、失败可重试，旧本地文件不删除。
- 本地 API 类型检查、14 项有效测试和构建通过；前端类型检查与生产构建通过；Compose 配置解析通过。
- ECS 隔离环境中旧库恢复、0008 迁移、真实 PostgreSQL、MinIO 签名上传下载、版本引用、用户隔离、回收站与 API 重启恢复均通过；生产容器未变更。

### 第 2 部分执行记录

- 已重读总计划、第 2 部分任务包、画布图契约和平台 API 边界。
- 已开始审计画布组件运行态、Zustand/localForage 保存链路、WebDAV 同步及旧项目 PostgreSQL 画布实现。
- 初步确定采用服务端 revision 乐观锁、事务保存、成功快照缓存和显式旧数据迁移，不在本部分上传媒体 Blob。
- 已新增结构化节点、端口连线、画布设置、快照和迁移台账的 Drizzle Schema、0007 增量迁移及手动回滚脚本。
- 已实现画布读取/事务保存、快照列表/恢复、IndexedDB 幂等迁移和项目成员隔离接口。
- 已实现前端云端优先加载、800ms 串行防抖保存、离线草稿、可见同步状态和双标签冲突选择。
- 已实现旧 IndexedDB 显式迁移向导；旧数据不删除，媒体内嵌内容、API Key/Token、渠道、代理和 WebDAV 配置不会上传。
- 本地 API 类型检查、13 项有效测试和构建通过；前端类型检查与生产构建通过。
- ECS 隔离环境中旧库恢复、0007 迁移、真实 PostgreSQL 集成、同源保存、409 冲突和 API 重启恢复全部通过；生产容器未变更。

### 第 0 部分执行记录

- 已核对本地 Git：`main` 与 `origin/main` 同步，基线提交为 `d31c74e253232c4a6314b7461b7e2f0d9c948ee8`。
- 已核对当前容器边界：仅有 `app`，没有数据库、队列、Worker 和对象存储。
- 已核对现有节点与视频请求结构，确认后续契约必须兼容旧节点和四类视频生成模式。
- 已完成契约文件、恢复说明和最终校验；第 0 部分无剩余项。
- 已完成 ECS 脱敏审计：确认当前单容器版本与旧 PostgreSQL/Redis/MinIO 共存状态。
- 已创建 ECS 可恢复备份 `/opt/jingjie-backups/part0-20260908T141423Z`，所有 SHA-256 校验通过。
- 已创建本地完整 Git bundle 备份 `H:\CodexStorage\backups\infinite-canvas-upgrade\part0-20260908T141423Z`，bundle 验证通过。
- 未重启、替换或部署任何线上容器，未修改生产数据。

### 第 1 部分执行记录

- 已重读第 0 部分契约和第 1 部分任务包。
- 已审计旧版 Drizzle Schema、SQL 迁移、认证、项目权限和画布接口。
- 已确认采用增量兼容迁移：保留旧 19 张表，新增 `canvases` 及默认草稿/最近打开字段与约束。
- 已确认前端本部分只接认证、云端项目列表和默认画布入口；画布节点云端持久化安排在第 2 部分。
- 已建立独立 Hono API、Drizzle Schema、增量 SQL 迁移和 PostgreSQL Repository。
- 已实现注册、登录、当前用户、退出、项目列表/读取、默认草稿幂等创建、Session Cookie 和同源请求校验。
- API 类型检查通过；8 个认证/权限单元测试全部通过。
- 已接入前端登录/注册页、认证路由门卫、默认项目本地壳和退出入口；根路由登录后直接进入默认画布。
- 已将容器拆为同源 Nginx Web、独立 API、独立模型代理和 PostgreSQL；Compose 配置解析通过。
- 前端 TypeScript 检查通过。
- API 与前端生产构建均通过；前端仅出现已有的大包体/动态导入构建警告，没有新增构建错误。
- 已让认证门卫同步当前用户的云端项目列表；登录后直接进入最近或默认画布，不经过强制创建项目页。
- 已补充手动回滚 SQL、迁移恢复说明、PostgreSQL 镜像源覆盖配置和变更记录。
- 已在 ECS 隔离网络中恢复第 0 部分旧 PostgreSQL 备份，增量迁移、数据库集成测试、四镜像构建、同源代理、Cookie 属性、Session 重启恢复和 SPA 回退全部通过。
- 生产 `infinite-canvas:global` 容器未重启、未替换，生产数据库未执行迁移。

## 已完成的前置工作

- 已选择 `keepper-max/infinite-canvas` 作为长期底座。
- 已选择性吸收上游 v0.18.0 的关键画布和媒体能力。
- 已加入全局托管模型、Token360 目录和 Seedance 视频模式适配。
- 已加入 Seedance 2.0 漫剧提示词模板，且没有把 2.0 数值限制套到 2.5。
- 已通过上一轮 TypeScript 检查和生产构建。
- 已登录并实测蔓悦复杂画布，记录了分镜、角色、版本、任务和成片能力差距。
- 已将新方案拆为 9 个独立任务包。

## 任务包进度

| 部分 | 内容 | 状态 | 验证摘要 |
|---:|---|---|---|
| 0 | 基线与契约 | complete | 本地/ECS 审计、双端备份及 6 份契约文档均通过验证 |
| 1 | API、账号、默认草稿 | complete | 本地检查与 ECS 旧库恢复、迁移、四镜像和认证闭环验收通过 |
| 2 | 云端画布与迁移 | complete | PostgreSQL 权威保存、快照/恢复、冲突与 IndexedDB 安全迁移均通过隔离验收 |
| 3 | OSS 资产与版本 | complete | MinIO/OSS、不可变版本、来源、缩略图、精确画布引用、回收站及旧媒体迁移通过隔离验收 |
| 4 | 任务与模型网关 | complete | Docker 隔离环境已验证 BullMQ Worker、模型网关、SSE、幂等、三次尝试/手动重试、运行中取消、四种视频模式与 MinIO 产物 |
| 5 | 画布产品化 | complete | 检查器、锁定、吸附/对齐/等距、自动布局、类型化连线、重连、专注/性能模式、资产拖入与版本入口已完成 |
| 6 | 漫剧与 Seedance Skill | complete | 15 类节点、默认模板、结构化 Skill、版本绑定、语义参考与四范围执行已通过检查 |
| 7 | 声音字幕与 FFmpeg | in_progress | 正在审计并实现 |
| 8 | 运营占位、验收与部署 | pending | 未开始 |

## 文件变更记录

- 第 6 部分：新增漫剧节点注册、默认流程模板、角色卡/分镜编辑、Seedance Skill 编译、输入哈希与 stale 传播、四范围拓扑执行；扩展任务 trace 和语义资产来源。
- 第 5 部分：新增画布生产力算法与统一检查器，补齐锁定、对齐参考线、六向对齐、等距、依赖自动布局、类型化连线、重连、专注/性能模式和资产定点拖入；同时修复 API/Worker 并发迁移竞态。
- 第 3 部分：新增 0008 数据迁移、对象存储与资产服务、资产 API、云端资产页、版本比较/来源/回收站、IndexedDB 迁移和资产版本画布引用。

- `task_plan.md`：切换为 AI 漫剧生产画布 2.0 总计划。
- `findings.md`：记录仓库事实、蔓悦实测结果、架构决策和风险。
- `progress.md`：建立可跨上下文恢复的进度表。
- `plans/ai-drama-canvas-2/*.md`：新增 9 个执行任务包。
- `plans/archive/upstream-v018-migration/`：备份上一轮选择性迁移规划记录。
- `plans/ai-drama-canvas-2/contracts/*.md`：新增基线恢复、画布图、资产任务、模型网关和平台 API 契约。

## 测试记录

| 测试 | 预期 | 实际 | 状态 |
|---|---|---|---|
| 规划文件索引检查 | 9 个任务包均存在且能从总计划定位 | 9 个存在、0 个缺失、0 个未引用 | passed |
| Git 状态检查 | 仅规划文件变化，无代码、密钥和构建产物 | 仅根规划文件和 `plans/` 为未跟踪文件；敏感模式扫描无命中 | passed |
| ECS 备份校验 | 镜像、源码配置、数据库和 MinIO 备份可读取 | SHA-256、gzip/tar、pg_restore 清单全部通过；线上容器持续运行 | passed |
| 本地备份校验 | 完整代码历史可恢复 | Git bundle 完整且 SHA-256 复核通过 | passed |
| 契约覆盖检查 | 覆盖节点、端口、边、四种视频模式、旧数据、任务、资产、模型、ER/API/SSE | 15 个必需项全部命中，6 个契约文件无缺失 | passed |
| Markdown 与敏感信息检查 | 无尾随空格、无密钥值 | 检查通过 | passed |
| API 类型检查、单元测试与构建 | 认证和权限逻辑可编译且测试通过 | 8 passed、1 个无本地数据库时跳过；构建通过 | passed |
| 前端类型检查与生产构建 | 认证入口和项目同步无类型/构建错误 | 均通过；仅有原有包体警告 | passed |
| 前端锁文件重装 | Linux/Windows 均可从锁文件完整安装 | 干净目录 `npm ci` 通过，ECS 容器安装通过 | passed |
| ECS 旧库兼容迁移 | 旧备份恢复后增量迁移且保留原表 | 恢复、迁移和真实 PostgreSQL 集成测试通过 | passed |
| ECS 同源认证闭环 | 注册、Cookie、项目、SPA 与重启恢复正常 | 全部通过，生产容器未改动 | passed |
| 第 2 部分 API 检查 | 画布契约、权限、冲突和迁移逻辑通过 | 类型检查、13 passed、1 个无本地数据库时跳过；构建通过 | passed |
| 第 2 部分前端检查 | 云端同步和迁移 UI 可编译、可生产构建 | TypeScript 与 Vite 构建通过；仅有原有包体警告 | passed |
| ECS 旧库画布迁移 | 旧库增量升级且画布跨重启恢复 | 迁移、PostgreSQL 集成、同源读写、409 冲突及 API 重启恢复均通过 | passed |
| 第 3 部分 API 检查 | 资产契约、版本、权限、缩略图和回收站可编译且测试通过 | 类型检查、14 passed、1 个无本地数据库时跳过；构建通过 | passed |
| 第 3 部分前端检查 | 云端资产、版本比较、来源和迁移 UI 可生产构建 | TypeScript 与 Vite 构建通过；仅有原有包体警告 | passed |
| 第 3 部分 Compose 与敏感值检查 | MinIO/OSS 配置可解析且本次改动不含凭据 | Compose 解析通过；变更文件敏感值扫描无命中 | passed |
| ECS 旧库资产迁移 | 旧库可升级且真实对象存储、版本引用跨重启恢复 | 0008、PostgreSQL、MinIO 签名传输、版本绑定、隔离、回收站及 API 重启均通过 | passed |
| 全仓格式检查 | 识别本次格式问题 | 仓库基线已有 186 个文件不符合当前 Prettier；未执行全仓重排 | known baseline |
| 第 5 部分前端检查 | 产品化画布改动无类型和生产构建错误 | TypeScript 与 Vite 构建通过；仅有原有包体警告 | passed |
| 第 5 部分本机 Compose | Web、API、Worker、PostgreSQL、Redis、MinIO 可同时启动 | 六个容器均运行，Web/API/数据库/Redis 健康检查通过 | passed |
| 并发迁移复验 | API 与 Worker 同时启动不再重复建表 | PostgreSQL advisory lock 串行迁移，API 健康、Worker 持续运行 | passed |
| 第 6 部分前端检查 | 漫剧节点、模板、面板和批量执行可编译并生产构建 | TypeScript 与 Vite 构建通过；仅有原有包体警告 | passed |
| 第 6 部分 API 检查 | 任务追踪、语义资产来源和旧任务行为兼容 | typecheck/build 通过，21 passed、1 个无本地数据库时跳过 | passed |
| 第 6 部分 Compose 配置 | 新代码仍可由标准六服务拓扑部署 | 使用仅当前进程有效的占位环境变量解析通过，未写入凭据 | passed |

## 错误记录

- 第 6 部分完成记录首次跨多个规划文件写入时，`progress.md` 的第 5 部分描述与假设文本略有不同，导致补丁整体安全拒绝；确认无部分写入后按文件拆分完成。
- 第 6 部分首次查看容器配置时使用了不存在的 `compose.yaml`；实际文件为 `docker-compose.yml`，未启动或修改容器，随后用文件索引定位正确文件。
- 第 6 部分直接执行 `docker compose ps` 时因仓库按设计不保存数据库/对象存储密钥而拒绝解析；随后使用仅对当前检查进程有效的占位值完成配置校验，没有写入 `.env` 或版本库。
- 第 6 部分补齐分镜与三视图版本时，大补丁因格式化后的运行时代码上下文变化被整体安全拒绝；确认没有部分写入，已按文件拆成精确补丁。
- 第 6 部分资产结构复审时再次误用了已知错误目录 `server/api/src/db/migrations`，并在同一只读搜索中写错正则转义；未修改数据。已立即改为实际目录 `server/api/db/migrations` 与字面量搜索，后续不再使用该旧路径。
- 第 6 部分读取资产迁移时先尝试了不存在的 `0007_cloud_assets.sql`；目录清单显示实际文件为 `0008_asset_versions.sql`，未产生修改。
- 第 6 部分检查工作区脚本时读取了不存在的根 `package.json`；实际前端与 API 分别位于 `web/`、`server/api/`，未修改任何文件，后续按这两个真实边界执行。
- 第 6 部分首次前端 typecheck 发现内置节点规格联合类型未声明新增的 `definitionVersion` 与 `workflowKind`；已扩展公共规格返回类型，保留旧内置节点行为。
- 第 6 部分首次写入 `findings.md` 时假设了通用标题 `# Findings`，实际标题为中文项目名；补丁安全拒绝且未产生部分修改，已改为精确标题写入。
- 第 6 部分审计时按惯例假设迁移目录为 `server/api/migrations`，实际目录不存在；未修改数据，后续先用文件索引定位真实 SQL 目录再决定是否需要迁移。
- 第 6 部分首个跨文件类型补丁因 `plugin-node-context.ts` 函数体与假设上下文不同被整体安全拒绝；确认未写入后拆成类型文件与精确宿主补丁执行。
- 第 6 部分扩展节点注册元数据时首个补丁的 hunk 分隔格式错误，被工具整体拒绝且未改文件；已修正为合法最小补丁。
- 读取迁移时误用了 `server/api/drizzle/0007_cloud_assets.sql`；实际迁移目录需重新定位。未改动文件，后续用目录枚举确认正确路径。
- 一次 `rg` 包含不存在的 `web/src/contexts` 目录；有效结果已返回，后续限定到实际目录。
- 第 4 部分首次 API typecheck 发现 3 个类型问题：Provider 状态含 `failed` 未先收敛、ioredis 6 默认导入不可构造（两处）。已改为先映射失败状态并使用具名 `Redis` 导入，等待复验。
- 第 4 部分 API 第二次 typecheck 已通过。前端托管文本、图片、音频和视频入口已开始切换到平台任务 API；视频任务会保存 `managed` 来源，刷新后继续查询后台任务。
- 已完成后台状态阶段：`pending → queued → submitting → running → downloading → persisting → completed`，失败自动进入 `retrying` 或 `failed`，取消保持两阶段语义。
- 已增加项目 SSE 订阅和 PostgreSQL 事件补发，浏览器会自动重连；每次数据库事件写入后同时发布 Redis 变更通知，数据库仍是恢复权威。
- 已增加模型目录同步：公开目录仅更新候选/健康信息，未知模型默认禁用；只有人工能力覆盖表启用的模型进入用户目录。
- 已通过 API typecheck、17 项测试（另 1 项 PostgreSQL 环境测试按设计跳过）、前端 typecheck 和生产构建；现有 Vite 大包与动态/静态混合导入警告仍是基线警告，不阻塞构建。
- `docker compose config --quiet` 与 `git diff --check` 已通过；Compose 已包含 PostgreSQL、Redis、MinIO、API、Worker 和 Web，旧浏览器模型代理从运行拓扑移除。
- 隔离 Docker 全链路验证未能启动：本机 Docker Desktop Linux Engine 管道不存在，属于运行环境未启动，不是镜像构建失败；临时 Compose 覆盖文件已删除，未创建容器、卷或改动 ECS。
- 已按 Token360 官方接口规范修正视频请求：首尾帧使用 `frame_images`，多参考使用 `input_references`，比例使用 `aspect_ratio`；任务详情无 URL 时从标准 content 端点下载二进制并转存对象存储。
- 已按官方“可选参数以模型能力 Schema 为准”的规则移除 Seedance 2.5 的错误固定时长/分辨率白名单，避免合法的 6 秒、8 秒或其他目录允许值被本地误拒。
- 已将云端生成视频的 `assetId`/`assetVersionId` 绑定回画布节点；刷新恢复时仍可定位同一不可变产物版本。
- 已增加 Worker 执行前的资产版本归属校验和临时下载地址解析，跨项目或已回收的参考素材会在调用模型前拒绝。
- 已增加生产端浏览器渠道清理：旧持久化 API Key 会在配置迁移时剔除，生产 UI 隐藏新增渠道和本地代理；仅开发环境可显式开启回退。
- 已增加数据库事件序号触发器，消除 Worker、取消和重试并发写事件时的序号竞争。
- 一次大范围前端补丁因匹配上下文漂移被安全拒绝，文件未被部分覆盖；已拆成小补丁，并发现/移除误插入到创建函数中的轮询分支。
- 前端首次校验命令误写为不存在的 `npm run type-check`，并因工作目录已是 `web` 而多读了一层 `web/package.json`；未产生代码改动，已确认正确命令是 `npm run typecheck`。
- 隔离 Docker 构建尝试连接 `dockerDesktopLinuxEngine` 失败（Docker Desktop 未运行）；没有创建或删除任何持久数据，后续需在 Docker 可用环境复验真实队列闭环。
- Prettier 已格式化全部本轮 TypeScript/TSX/YAML 改动；它无法自动识别 `nginx.conf` 与 `.env.example` 的解析器，因此对这两个纯配置文件保留手工格式并继续用 `git diff --check` 校验。
- 第 4 部分最终本地校验：API typecheck/build 通过，20 passed、1 skipped；前端 typecheck/build 通过。跳过项和完整队列闭环均依赖本机未启动的 Docker/PostgreSQL/Redis/MinIO。
- 本机 Docker Desktop Linux Engine 已恢复：Docker 29.7.2、Compose 5.5.1 可用；当前仅运行另一个项目的 PostgreSQL，未占用本项目 Web、Redis 或 MinIO 端口。
- 第 4 部分本机隔离验收准备了不消耗真实额度的模型替身；首次把进程检查与 Compose 启动合并执行时被主机命令策略拒绝，未启动容器、未改变数据，后续拆分为最小命令执行。
- 第 4 部分隔离镜像构建及全链路成功：注册自动创建默认项目，模型目录发现 Seedance 2.5，任务经 Redis/BullMQ Worker 执行，SSE 收到完成事件，MinIO 产物写入不可变资产版本，重复请求只保留一个任务。
- 首次运行中取消复验暴露状态竞态：Worker 在 Provider 返回后会把 `cancel_requested` 覆盖为 `running`。已在提交、轮询前后和转存前加入状态门禁，并把取消终态改成幂等更新。
- 修复后运行中取消从 `cancel_requested` 正常进入并稳定保持 `cancelled`；成功任务、API/Worker 重启恢复、三次自动尝试失败、手动重试成功及 `t2v`、`i2v`、`flf2v`、`multiref` 均通过。
- 一次资产版本统计查询误用了不存在的 `source_type` 列；实际列名为 `source`，未修改数据，后续按迁移定义修正查询。
- 仅重建 API 容器后，既有 Nginx 暂存了旧容器地址而短暂返回 502；重启隔离 Web 入口后恢复。完整 Compose 部署会一并重建入口，本次未影响业务数据。
- 第 5 部分首次隔离构建访问 Docker Hub 时 Nginx/Bun 元数据返回 401；未改生产配置，复用本机已验证的 Nginx 镜像后完成当前标准 Compose 构建。
- 第 5 部分首次同时启动 API 与 Worker 暴露迁移竞态，二者会同时创建迁移表；已在迁移会话加入 PostgreSQL advisory lock，重建后 API 与 Worker 同时稳定运行。

| 错误 | 次数 | 处理方式 |
|---|---:|---|
| 批量补丁格式不支持同一路径 Delete + Add | 1 | 已确认未产生修改；备份后改为分批写入 |
| 第 0 部分写入事实时匹配了错误小节/原文 | 2 | 读取实际内容后用精确补丁写入；未造成覆盖 |
| 首次数据库清单把 owner 列识别成表名 | 1 | 修正字段位置后复核，确认 19 张业务表 |
| 第 0 部分首次写入事实时匹配了错误的小节标题 | 2 | 先读取实际标题，再用最小补丁写入；未造成文件改动 |
| 第 1 部分首次 API typecheck：Drizzle 部分索引传入 string，事务泛型沿用了旧签名 | 1 | 按已安装 Drizzle 0.45 类型改用 `sql` 条件和双泛型事务类型 |
| 第 1 部分批量替换 Docker 文件时同一补丁包含 Delete + Add | 1 | 文件未变化；改为基于第 0 部分备份分两步替换，避免重复提交同一失败补丁 |
| 工作区依赖定位工具已迁移，旧入口不可用 | 1 | 未重复调用；确认现有 H 盘 `web/node_modules` 可直接执行检查 |
| ECS 首次镜像验证使用旧式 Docker builder，不支持 Dockerfile 的 `RUN --mount` | 1 | 生产未受影响；移除 BuildKit 专属缓存挂载，改用标准 Docker 语法后重跑 |
| ECS 第二次镜像验证中 Bun 解压 `mermaid` 依赖失败 | 1 | 放弃容器内 Bun 安装，改为带锁文件的 Node/npm 构建 |
| 首次生成前端 npm 锁文件遇到现有 Ant Design 6 与 Pro Components 5 peer 声明冲突 | 1 | 现有应用已用该组合通过构建；使用 `--legacy-peer-deps` 固化当前依赖树，不升级组件 |
| Windows 首次生成的 npm 锁文件含无版本可选依赖占位 | 1 | 在干净目录用 npm 10 重建锁文件，并在本地和 Linux 容器分别执行完整安装 |
| ECS 无法稳定直连 Docker Hub 拉取 Nginx/PostgreSQL | 2 | Nginx 固定到主机已缓存版本；隔离测试使用已缓存可信镜像，Compose 为 PostgreSQL 保留镜像源覆盖变量 |
| 第 3 部分隔离测试首次启动临时 MinIO 时 Docker Hub 超时 | 1 | API/API-test/Web 三镜像已构建通过；改为读取现有 MinIO 容器的本地镜像 ID后重跑，不触碰其数据卷 |
| 第 3 部分隔离测试首次恢复旧库时命中 PostgreSQL 初始化重启窗口 | 1 | 临时容器已自动清理；在首次就绪后增加稳定性复核再恢复，不修改备份或生产数据库 |
| MinIO 拒绝首个签名 PUT：请求头未纳入签名 | 1 | 按已安装 AWS SDK 官方选项将 Content-Type 与校验元数据头显式纳入签名；保留服务端完成校验 |
| 第 3 部分真实 MinIO 冒烟首次上传返回 HTTP 400 | 1 | 旧库迁移和 PostgreSQL 集成已通过；增加 MinIO/API 响应留存以定位签名差异后重跑 |
| PowerShell 读取含 `[projectId]` 的旧路由路径时按通配符解析失败 | 1 | 不重复原命令；后续改用 `Get-Content -LiteralPath` 精确读取 |
| 首次读取新 API 文件时在已进入 `server/api` 的工作目录重复拼接路径 | 1 | 改用相对当前工作目录的 `src/...` 路径读取，未修改文件 |
| 首次生成画布契约文件出现两处损坏的表达式 | 1 | 在类型检查前通读新文件并以最小补丁修正节点查找和视频模式表达式 |
| 第 2 部分首次批量文档补丁因 findings 原文不匹配而中止 | 1 | 确认未写入后拆分为精确补丁完成，未覆盖其他记录 |
| 本机 Docker Desktop 未运行，无法执行本地 PostgreSQL 容器测试 | 1 | 改在 ECS 隔离网络恢复旧库并完成真实 PostgreSQL 与镜像验收，生产未改动 |
| 完整 2.9MB 隔离验证包分块上传过慢 | 1 | 中止本地上传进程，改用已验证的第 1 部分基包加 58KB 增量覆盖包；远端临时分片仅位于隔离目录 |
| 第 3 部分首次读取契约时使用了不存在的 `asset-task-contract.md` 文件名 | 1 | 通过契约索引定位为 `assets-and-jobs-contract.md`，不重复错误路径 |
| 第 4 部分首次读取平台契约时使用了不存在的 `platform-api-contract.md` 文件名 | 1 | 改为枚举契约目录并按实际文件名读取，不重复错误路径 |
| 第 4 部分首次按假设目录读取 `server/model-proxy`、根/`server` package 和 `server/index.ts` | 1 | 通过 Dockerfile 与文件索引确认实际代理为根 `token360-proxy.mjs`，API 仅在 `server/api`，不重复假设路径 |
| 第 4 部分审计命令读取了不存在的根 `package.json`、`server/package.json` 和 `server/index.ts` | 1 | 以 Dockerfile 和 `server/api/package.json` 为实际服务边界，后续只使用已枚举路径 |

## 恢复上下文检查

- 我在哪里：第 0 至第 7 部分已完成；尚未开始第 8 部分。
- 目标是什么：把当前 Infinite Canvas 推进为可完成一集漫剧的云端生产平台。
- 下一步是什么：收到 `执行第8部分` 后再做运营接口预留、全链路验收与生产部署。
- 已知事实在哪里：`findings.md`。
- 已做工作在哪里：本文件和各任务包的完成记录。

## 第 7 部分执行记录（2026-09-10）

- 已完成：重读任务包与仓库规则，盘点资产、队列、音频 Provider、画布连线和数据库迁移边界。
- 已确认：采用独立合成任务/事件记录，Worker 内独立 FFmpeg 队列，输出写入不可变资产版本。
- 进行中：核对服务端注入点、对象存储下载接口、前端漫剧节点执行分发和时间线编辑入口。
- 已确认：生产 API/Worker 镜像缺少 FFmpeg；对象存储缺少 Worker 内部读取方法；现有项目 SSE 只聚合生成任务事件。
- 已确认：现有迁移实际最新为 `0009_jobs_and_model_gateway.sql`，第 7 部分使用新增 `0010` 兼容迁移。
- 待完成：迁移、合成服务、Worker、API、节点与模板、测试、文档收尾。
- 已完成：新增 `0010` 兼容迁移、结构化时间线契约、独立合成队列/API 骨架、FFmpeg Worker 执行器和对象存储内部读取边界。
- 进行中：把配音、音效、音乐、字幕、时间线和成片输出节点接入漫剧模板与画布执行器。
- 已完成：新增 6 类成片节点、时间线编辑器、字幕时间编辑、音轨音量/起点、镜头时长/转场和后台成片提交；默认漫剧模板已连到成片输出。
- 已验证：API 与前端 TypeScript 检查通过，`git diff --check` 通过（仅有仓库既有换行提示）。

## 第 7 部分错误记录

- 首次更新进度文档时误在旧工作区匹配 `progress.md` 标题，补丁校验失败且未产生修改；已定位到本任务实际仓库 `H:\CodexStorage\infinite-canvas-upgrade` 后继续。
- 首次按假设名称读取第 6 部分迁移 `0009_ai_drama_workflow.sql` 时文件不存在；已停止猜测，后续按迁移目录实际清单定位。
- 第 7 部分首次 API typecheck 发现 Zod 对嵌套对象默认值要求完整字段；已补齐输出尺寸、帧率与字幕字号默认对象。
- 第 7 部分首次前端 typecheck 发现画布内容资源枚举尚无 `timeline`，且音轨角色条件表达式被推宽为普通字符串；时间线节点改以文本摘要参与画布引用，并显式收窄音轨角色类型。
- 本机没有全局 FFmpeg 命令；生产运行镜像已改为安装 FFmpeg，需通过 Docker 镜像内验证，不污染主机环境。
- 首次直接执行 `docker compose config --quiet` 因仓库安全设计要求必填数据库和对象存储环境变量而退出；未创建容器，后续只使用临时占位值解析配置且不会写入文件。
- 第 7 部分首次 API 镜像构建在 Debian 安全仓库返回临时 502 时中止；应用编译阶段已通过，未创建容器或改动数据。FFmpeg 安装步骤已增加包管理器重试后重新验证。
- 第二次 FFmpeg 镜像构建下载了约 125 MB 后，Docker Desktop 到 Debian 官方镜像仍连续返回 502；缓存未形成可运行镜像。已把 Debian 软件源切到与目标阿里云 ECS 网络更匹配的阿里云镜像，并保留签名校验和下载重试。
- 第 7 部分最终实现：独立时间线/合成任务数据库、BullMQ 单并发合成队列、FFmpeg 拼接/转码/混音/字幕/封面、取消/重试/清理、OSS 不可变成片资产及来源追踪全部接通。
- 漫剧模板已加入配音、音效、背景音乐、字幕、时间线和成片输出节点；支持角色声音绑定、对白批量 TTS、镜头/音轨/字幕编辑、后台进度显示和取消任务。
- 容器内真实 FFmpeg 冒烟已通过：两段画面合并为 2 秒 H.264 视频、AAC 音轨、SRT 中文字幕滤镜和 JPEG 封面均成功生成。
- 最终验证：API typecheck/build 通过，24 passed、1 skipped；前端 typecheck/build 通过；`git diff --check` 通过。前端仍只有既有的大包和静态/动态混合导入警告。
- API 镜像的 FFmpeg 运行层已构建成功；首次把 Noto CJK 合并进同一软件源安装时，大型索引持续超时。已改为复用通过验证的 FFmpeg 层，并直接下载经过 Debian 官方 SHA-256 核验的小型文泉驿中文字体包。
- Prettier 已格式化本轮 TypeScript/TSX/YAML；对 SQL 测试迁移无法推断解析器而退出，该 SQL 保持人工格式并由数据库/差异检查验证。
- 最终 API 镜像重建通过，`fonts-wqy-zenhei` 的下载内容与 Debian 官方 SHA-256 一致，容器能识别文泉驿正黑；字幕滤镜显式选择该字体，避免中文回退为缺字字体。

## 第 8 部分执行记录（2026-09-10）

- 已开始：重读发布任务包、仓库约束和既有执行记录；第 8 部分状态切换为 `in_progress`。
- 执行边界：运营能力只建立接口与数据结构；生产部署前必须先备份线上配置、数据库与当前镜像/提交，保留明确回滚路径。
- 已完成第一批：新增短信、积分、套餐、支付、团队和管理员审计的兼容迁移；新增默认关闭的运营接口、团队成员/项目接口、只读管理员概览与失败任务入口。
- 已增加 API/Nginx 安全响应头，继续沿用现有可信 Origin、HttpOnly Cookie 和项目权限校验；未新增未经确认的限流、并发或超时默认值。
- 错误记录：只读审计时尝试读取不存在的 `server/api/src/db/index.ts`；实际入口为 `server/api/src/db/client.ts`，未产生修改。
- 错误记录：首次 API typecheck 发现运营配置类型已收窄但服务仍引用旧的三项启用字段；已把未接适配器的短信、积分和支付能力固定报告为关闭，复验 typecheck 通过。
- 前端边界确认：现有顶栏由统一 `navigationTools` 驱动，适合增加一个运营入口；普通用户可查看接口开关、积分占位和团队，只有服务端判定的管理员才能看到系统概览与脱敏失败记录。
- 错误记录：审计 API 客户端时尝试读取不存在的 `web/src/services/api/auth.ts`；认证方法实际集中在 `web/src/services/api/platform.ts`，未产生修改。
- 已完成运营页面：顶栏新增“平台状态”，展示短信/积分/支付预留状态、零余额积分账户、团队创建，以及仅管理员可见的用户/项目/素材/任务概览和脱敏失败记录。
- API 与前端首轮 TypeScript 检查均通过；尚未执行数据库集成、生产构建和 Docker 全链路。
- 一次性 PostgreSQL 16 环境完成 0006–0011 全部增量迁移，27/27 测试通过；覆盖零余额积分账户、默认关闭能力、团队创建/成员权限/项目归属和管理员概览。测试容器与网络已按精确名称清理，未触碰既有容器或数据。
- 错误记录：收尾审计按预设路径读取 `.gitattributes`、`docs/todo.md` 和 `docs/testing/pending-test.zh-CN.md` 时文件不存在；已通过仓库文件索引定位实际进度文档为 `docs/content/docs/progress/*.mdx`，仓库当前没有 `.gitattributes`，未产生文件修改。
- 已确认团队角色不能只停留在团队表：项目读写仍只按“是否为成员”判断；第 8 部分将补齐团队成员同步和只读角色写操作拦截，避免 `viewer` 获得隐式编辑权限。
- 错误记录：权限收口后的首次 API typecheck 发现成片序列化返回值中的项目 ID 被推断为 `unknown`；已在权限调用边界显式转为字符串，单元测试仍为 26 passed、1 skipped。
- 已完成权限闭环：团队成员会同步到关联项目，管理员/编辑者获得编辑权限，`viewer` 只能读取；画布、素材、生成任务和成片任务的所有写入口都执行服务端编辑权限校验，并记录团队变更审计日志。
- 已完成安全与运维边界：增加素材类别/MIME 匹配校验、管理员模型健康目录、生产备份/校验/健康检查脚本和可回滚发布手册；限流、日志轮转和告警阈值因尚未确认容量参数而保持未启用并进入待办。
- 已验证：真实 PostgreSQL 16 执行 0006–0011 迁移后 27/27 测试通过；API 测试镜像内 27/27 通过；生产 Web 镜像构建成功。临时 PostgreSQL 容器已按精确名称清理。
- 错误记录：为减少格式噪音回填翻译文件时，第一次动态 `apply_patch` 缺少结束行而失败，且英文翻译文件已被本轮暂时删除；随后立即从当前 `HEAD` 内容恢复并只加入运营导航键，复验类型检查通过。
- 错误记录：团队管理页面的首个合并补丁因页面已被格式化、目标单行不再匹配而整体中止，未写入部分变更；随后按实际 JSX 结构拆成三个最小补丁完成。
- 错误记录：团队项目防重复归属与错误脱敏的两个合并补丁因格式化后的多行文本不匹配而整体中止；按实际代码分段后完成，未留下半成品。
- 已补 80 节点/79 连线的 PostgreSQL 保存与刷新恢复测试；生产 API（含 FFmpeg/中文字体）和 Web 镜像均构建成功，Compose 必填配置解析通过。构建仅保留既有前端大包与静态/动态混合导入警告。
- 最终复审发现管理员失败列表错误地假设生成任务与合成任务使用同名错误字段；已按各自真实字段统一别名，并把空失败列表查询加入真实 PostgreSQL 测试，避免上线后首次失败才暴露 SQL 错误。
- 错误记录：首次合并修正管理员失败查询与测试时把两个文件的上下文放进同一个更新段，补丁整体校验失败且未修改文件；拆成两个精确补丁后完成。
- 第 8 部分发布前最终验证：API 与前端 typecheck 通过，API/前端生产构建通过，真实 PostgreSQL 16 环境 27/27 测试通过，包含 80 节点恢复、团队编辑/只读权限、管理员授权拒绝、模型目录与失败列表查询；`git diff --check` 通过，改动扫描未发现密钥字面量。
- 当前剩余高风险步骤只有生产侧：确认限流/日志/告警容量阈值，以及收到独立 `部署 ECS` 指令后执行线上只读核查、备份校验、灰度切换、外部冒烟和回滚演练。当前工作区未 commit、未 push、未修改 ECS。
# AI 文本工作台专项

- 已开始：核对现有导航、路由、文本模型调用和提示词库边界。
- 当前决策：复用全局模型网关和后台任务，不引入第二套模型调用路径；新增 PostgreSQL 会话/消息作为刷新恢复权威。

## 模型提交无限等待与画布保存修复

- 已开始：用户确认关闭模型提交自动超时，等待上游最终回传，只允许手动停止或服务关闭中止。
- 已定位：本机 GPT Image 2 任务因运行容器仍使用 60 秒提交超时而失败；同期画布保存返回 422。
- 错误记录：首次同时追加三份专项记录时，`findings.md` 的预期尾部标题不存在，补丁整体未写入；读取真实文件尾部后按精确上下文完成。
- 执行边界：本轮修改和本机验证；不自动提交、推送或部署 ECS。
- 错误记录：首次重建命令所在的新进程未继承 Docker CLI 路径，容器未发生变化；改为解析 Docker Desktop 的绝对 CLI 路径后继续，不重复原命令。
- 错误记录：第二次重建时批量导入容器环境意外覆盖了当前进程的 Windows `PATH`，导致 Docker 凭据助手不可用，镜像构建在拉取元数据阶段停止且运行容器未变化；后续仅导入 Compose 明确需要的变量，不再导入系统变量。
- 错误记录：重建后的首次 HTTP 验收访问 localhost:3000 被拒绝；测试账号未创建。先核对 Web 容器端口发布和 Nginx 启动状态，再修正运行编排。
- 已完成：Provider 提交超时与任务总运行时限默认设为 0；API/Worker 本地运行环境已确认两个值均为 0。同步请求由 Worker 监测手动取消后中断，异步任务持续轮询到上游终态。
- 已完成：停止按钮使用显式手动取消原因并调用后台取消；请求替换、切页或刷新产生的普通中断不会取消云端任务。
- 错误记录：首次小数尺寸 E2E 已越过 422，但 PostgreSQL 整数辅助列拒绝小数而返回内部错误；节点 JSON 原本可保留精度，修复为辅助列取整后重新构建。
- 错误记录：API 重建后 Web Nginx 暂存旧容器地址导致一次 502；重启本地 Web 代理刷新服务发现后恢复，未触碰生产环境。
- 已验证：临时本地账号完成注册、默认项目读取、带小数节点保存及重新读取，尺寸 `340.5 × 191.25` 与 revision 1 均正确。
- 最终验证：API 33 passed、1 skipped，API/Web typecheck 与生产构建通过；本地 Web/API/Worker/PostgreSQL/Redis 容器健康，未调用真实模型、未产生模型费用。
# 当前任务进度：动态视频参数（2026-09-12）

- **状态：** 进行中
- 已核对 Token360 公开模型目录中的 Seedance 2.5 参数 schema。
- 已定位服务端模型能力编译器和前端固定参数面板之间的断层。
- 下一步：完成公共类型、模型同步与动态视频控件实现。
- 已完成服务端安全参数 schema、模型动态上限、参数映射以及已知模型能力刷新。
- 已完成视频工作台和画布共用的动态面板；新增四种明确生成方式及模式说明。
- 已完成托管视频请求的模式显式映射，文生视频不再携带参考素材，首帧/首尾帧按角色提交。
- 已补充模型目录与请求映射测试代码、CHANGELOG 和人工验收清单。
- 错误记录：一次读取根目录 `package.json` 和一次读取缺少扩展名的 `auth-gate.ts` 失败；改为读取各子项目清单及实际 `.tsx` 文件，未重复失败。
- 验证记录：API 首次类型检查发现目录枚举值未完成类型收窄，已修正解析边界。
- 验证记录：Web 首次类型检查发现参考图角色和 Agent 模式的类型边界，已完成显式收窄后等待复验。
- 自动化验证：API TypeScript 通过；API 测试 35 项通过、1 项按设计跳过；Web TypeScript 通过；`git diff --check` 通过；变更中未发现凭据模式。
- 已完成生产 Web/API 镜像构建；仅保留既有前端大包和静态/动态混合导入警告。
- 已定位用户本地未见改动的原因：3000 端口仍由 9 小时前的旧 Web 镜像提供，源码改动尚未进入容器；旧的 `model-proxy` 是已退出的孤立容器，不参与当前服务链路。
- 已使用最新工作区源码无卷重建本地 Web/API/Worker，PostgreSQL、Redis 和 MinIO 数据卷未删除或重建；API 健康检查通过。
- 浏览器验收通过：视频工作台已显示“文生视频 / 首帧图生 / 首尾帧图生 / 多模态参考”，并随模型能力显示码率、输出格式、生成声音和水印，不再是旧的两模式固定面板。
- 未执行真实 Token360 视频任务，避免在仅验证界面更新时产生费用；下一步需按生成方式做真实任务验收。

# 当前任务进度：全视频模型参数适配（2026-09-12）

- **状态：** 进行中
- 已建立五阶段执行计划；先审计 Token360 当前目录和既有能力编译链路。
- 本轮执行边界：修改与验证，不自动提交、推送或部署 ECS。
- 已读取公开目录：当前识别 25 个视频相关条目，确认模型系列和参数 Schema 结构存在明显差异。
- 已审计第一版实现：服务端与前端仅覆盖 7 个通用参数，尚未消费目录中的条件显示与扩展参数。
- 已记录首次按错误字段抽样目录导致空结果，后续改为读取条目真实键名。
- 已按 `modelType=VIDEO_GENERATION` 精确确认 22 个视频模型，并整理每个模型的时长、分辨率、比例、参考素材上限和扩展参数。
- 已确定改造主线：保留服务端白名单编译边界，但由目录自动生成安全标量参数和上游字段映射；模式由帧输入与多模态参考限制推导。
- 已完成配置与画布持久化审计：决定新增通用视频参数字典并兼容旧固定字段，避免未来每增加模型都修改类型和多个页面。
- 已定位模式显示错误：单帧模型和明确禁用参考素材的模型当前会多显示不支持的生成方式，将在能力编译层统一修正。
- 错误记录：第一轮 Web 类型检查发现泛型参数键无法让 TypeScript 同步收窄对应值类型；已在画布元数据边界显式区分参数字典与字符串字段，API 类型检查同期通过。
- 错误记录：首轮模型网关定向测试有两项旧断言失败：无 Schema 的兼容样本丢失多参考模式，且 limits 新增参数定义后旧期望未更新；已恢复“字段缺失时按 supported_parameters 兼容”的语义并把测试样本升级为真实目录结构。
- 已完成服务端通用安全标量参数编译：目录可见且属于模型参数的 boolean/string/integer/number 自动进入白名单、映射和范围校验。
- 已完成模式限制推导：单帧模型不再显示首尾帧，三类参考上限都为 0 的模型不再显示多模态参考。
- 已完成前端通用参数控件与按模型隔离存储；新增参数会自动渲染选择、开关、数字或文本控件。
- 已把扩展参数接入视频工作台日志恢复、画布节点元数据和托管视频提交负载。
- 错误记录：完整 API 测试剩余一项旧样本仍只在 supported_parameters 声明码率/格式，却未在 normalized Schema 提供定义；真实目录两处均有，已把测试夹具补成真实结构，不放宽生产安全白名单。
- 错误记录：首次运行实时 22 模型契约审计时，`tsx -e` 的 CJS 模式不支持顶层 await；已改用异步函数包装后成功运行，不重复原命令。
- 实时审计进一步发现空 `max_items` 被 JavaScript `Number(null)` 误判为 0，导致 MiniMax H3 系列少显示图生模式；已先判空再转换数字。
- 已审计视频工作台参考输入，发现旧的 7 张硬上限与界面 9 张文案不一致；将改为按模型和生成方式动态限制。
- 错误记录：尝试把扩展参数标签插入单行压缩的语言包时上下文不匹配，补丁整体未写入；改为在参数组件中提供中英双语标签映射，避免重写整行语言资源。
- 已把视频工作台参考图上限改为按模型/模式动态计算：0、1、2 或目录多参考上限，并同步按钮状态和提示文案。
- 格式收敛：两份语言包曾被格式化工具展开约两千行；已精确恢复到当前 HEAD 后，仅重新加入动态数量和“当前模式无需参考图”两处文案，不保留格式噪音。
- API/Web 类型检查通过；实时 Token360 目录的 22 个视频模型全部完成能力编译与参数映射检查。
- 兼容性复审发现并修复两项边界：模型原生 `2k`/`4k` 清晰度不再被旧逻辑降为 `720p`；多模态参考对明确为 0 的图片/视频/音频上限会严格拒绝，同时不误伤首帧模式。
- 验证记录：API 38 项测试通过、1 项按设计跳过；API/Web 类型检查和生产构建通过，前端仅有既有大包与混合导入警告。
- 本地 Docker 已用最新源码无卷重建 Web/API/Worker，三个应用容器运行正常，`/api/health/ready` 返回 HTTP 200，数据库中有 22 个启用且健康的视频模型。
- 浏览器验收通过：模型下拉完整显示 22 个视频模型；Vidu Q1 Text 仅显示文生模式与运动幅度，Berry Pro 正确显示 1080p/2K/4K 和提示词扩展开关，CogVideoX-3 显示输出尺寸与质量，并在首帧模式把参考图上限切为 1。
- 错误记录：首次手工健康检查误用了不存在的 `/api/health`，随后按 Compose 健康检查配置改用 `/api/health/ready` 并确认 HTTP 200。
- 本轮未提交真实模型生成，避免仅做契约和界面验收时产生费用；未自动 commit、push 或部署 ECS。

# 当前任务进度：Virtual Portrait 角色资产库

- 已建立六阶段实施计划并记录安全边界。
- 已从用户截图确认 `asset://{assetId}` 及首帧、参考图、多模态参考三类字段映射。
- 下一步：读取 Token360 官方文档并审计现有资产与视频编译链路。
- 已完成官方文档核对：建组、上传、状态轮询、`ta_` 身份限制、六类视频场景和两套媒体字段均已确认。
- 下一步：审计本项目现有资产表、资产 API、画布资产侧栏及视频请求编译结构，确定最小兼容实现。
- 已完成第一轮代码审计：确认可复用项目资产权限、服务端视频媒体字段编译和画布资产选择入口；当前缺少供应商分组/资产状态和 `asset://` 身份贯通。

---
# 2026-09-14 路径核验

- 初次按桌面线程 `cwd` 搜索时未命中目标文件；核验后确认本任务真实仓库仍为 `H:\CodexStorage\infinite-canvas-upgrade`。
- 已检查真实仓库 `main...origin/main`，当前仅 `task_plan.md`、`findings.md`、`progress.md` 含本次规划记录，尚未修改产品代码。

# 2026-09-14 Virtual Portrait 架构审计

- 已确认 Token360 provider 的视频请求编译层无需重复改造；新增引用只需在任务落库前解析为受信任的 `asset://ta_...`。
- 已确认对象存储支持服务端直接读取版本文件，能够在不暴露 Token360 密钥的情况下转发 multipart 上传。
- 已确定采用项目级持久化资产库、服务端权限校验、画布图片节点复用的最小方案；不复用仅保存在浏览器本地的普通资产 Store。
- 错误记录：迁移目录首次按仓库根 `db/migrations` 查找不存在，下一步将通过文件索引定位真实目录后再新增增量迁移。

# 2026-09-14 Virtual Portrait 实现

- 已新增增量迁移及结构回滚脚本，按项目保存 Token360 分组映射、角色 Asset ID、处理状态和源素材版本。
- 已新增服务端角色资产 API：列表/同步、创建、移除；Token360 密钥只在服务端使用，上传通过既有对象存储版本转发。
- 已扩展生成引用契约：画布提交站内 `virtualPortraitId`，Worker 在实际提交前校验项目归属和 active 状态，再转换为 `asset://ta_...`。
- 已新增画布“角色”资产库页签，支持上传、状态刷新、错误提示、移除、点击或拖拽插入；节点同时保留预览版本与角色身份。
- 错误记录：两次大补丁因目标行上下文与实际文件不一致被 `apply_patch` 完整拒绝，确认无半成品后按准确位置拆分应用。
- 安全复审收紧普通引用 URL 为 HTTP/HTTPS，客户端无法直接伪造 `asset://`；Token360 错误信息在落库/返回前脱敏。
- 结构复审把角色库 UI 从已有大型侧栏文件拆为同目录独立组件，避免继续堆积无关状态和请求逻辑。
- 验证通过：API 与 Web TypeScript 类型检查；API 全量测试 48 项中 47 项通过、1 项既有 PostgreSQL 环境测试跳过；Virtual Portrait/任务引用定向测试 8 项全部通过；`git diff --check` 无空白错误。
- 格式检查首次发现两份新增文件未符合 Prettier，已仅格式化本次新增文件后重新完成类型和定向测试。
- 未执行真实 Token360 上传或视频生成，避免创建外部资产与产生费用；已写入待测试清单。本轮未 commit、push 或部署 ECS。

## 账号管理后台与 Token360 消耗对账（2026-09-14）

- **状态：** 阶段 1 进行中
- 已读取 `planning-with-files` 与 `frontend-design` 技能说明。
- 已确认现有数据库、鉴权、运营接口和生成任务具备增量扩展基础。
- 已确认实施约束：独立 `/admin`、单管理员角色、开放注册、业务内容严格只读、实际账单对账、手动重试创建新任务。
- 错误：首次规划追加因历史 `findings.md` 结尾与模板不同被整体拒绝；已按实际末尾重新追加，未修改产品代码。
- 错误：账单服务大补丁首次上下文写错并被整体拒绝；改用拆分补丁，未留下半成品。
- 已完成 API/Worker 启动、配置、Provider、任务执行、队列与中断恢复链路审计。
- 已完成路由、Session 用户契约、素材下载权限、前端鉴权上下文与顶栏导航审计；阶段 1 主要决策已收敛。
- 服务端已增加账号状态、任务 Trace、账单流水迁移，手动重试改为新任务，并加入终态账单同步。
- 管理 API 已覆盖账号操作、消耗、任务、模型、项目只读排障与审计；Web 已增加独立 `/admin` 布局。
- 验证记录：根目录 pnpm 命令不适用于当前仓库，已切换到子项目 npm 脚本。
# 当前任务进度：视频结果快速回传与断点转存

- 已确认参数：30 秒低速窗口、64KB/s 阈值、4MB 分块、转存并发 4。
- 已完成生产只读诊断与两条官方下载路径 Range 探测。
- 下一步：审计现有 BullMQ Worker、恢复逻辑与画布任务事件，再做最小兼容实现。
- 已完成 4MB Range 断点下载、30 秒持续低速检测、直链与鉴权内容接口自动切换；切换只续传既有结果，不重新提交模型。
- 已把结果转存拆到独立 4 并发队列；Worker 重启时，下载中与写入中的任务回到转存队列恢复，不进入生成队列。
- 画布节点会显示“正在下载 x / y MB”“正在写入资产库”等真实阶段，完成后清理临时状态文案。
- 验证通过：API 类型检查、构建、全量测试 66 项（65 通过、1 项 PostgreSQL 环境测试跳过）；Web 类型检查与生产构建通过；`git diff --check` 通过。
- Web 构建仅保留既有动态导入和大包体积警告；本轮未 commit、push 或部署。

# 当前任务进度：海马云渠道接入与管理员渠道治理

- **状态：** 本地实现与验证完成，尚未 commit、push 或部署。
- **服务端：** 新增海马云目录同步、媒体预上传、模型提交、异步轮询、结果解析和 Provider 路由；新增全局托管渠道设置及管理员切换接口，密钥仅从 `RH_API_KEY` 读取。
- **目录：** 管理后台完整显示官方 383 个海马云模型；291 个可由当前画布精确映射的生成模型进入能力目录，其余 3D、上传、续写和分析等专用端点保持未启用，避免猜测参数。
- **前端：** 普通用户隐藏渠道页；管理后台模型页增加 Token360/海马云二级菜单、密钥配置状态和全局渠道切换，新目录同步后自动选择各能力首个可用模型。
- **验证：** API 类型检查通过；67 项测试通过、1 项 PostgreSQL 集成按环境跳过；新增海马云请求契约测试通过；Web 类型检查和生产构建通过；隔离 PostgreSQL 16 全量迁移成功并确认默认渠道和海马云配置记录。

# 当前任务进度：海马云 Seedance 2.5 与实际消耗入账

- 已建立五阶段计划并记录安全边界。
- 已确认本轮不提交真实海马云任务，避免产生费用；不自动 commit、push 或部署。
- 下一步：追踪 RunningHub Provider 终态响应到 `generation_usage` 的完整数据链路，再做最小增量实现。
- 已完成任务返回、目录刷新、转存恢复和管理员消耗页审计；确认无需新增数据库列。
- 已确认公开注册表暂缺 Seedance 2.5，需要在服务端目录刷新时合并三条受控补充定义。
- 错误记录：首次模型目录大补丁上下文不匹配，被完整拒绝；确认无半成品后改用小补丁继续。
- 已补充海马云 Seedance 2.5 文生、首帧/首尾帧和多模态视频三类服务端模型定义；公开注册表以后出现同 endpoint 时会自动覆盖补充定义。
- 多模态参考支持官方 30 图、10 视频、10 音频上限；客户端可获得的音视频时长会在提交前校验单段 2–30 秒和已知总时长不超过 30 秒。
- 海马云终态 `usage` 的第三方实际金额、平台金额、Token、计费秒数和原始字段会幂等写入统一消耗流水；同步异常不会把已成功生成改成失败。
- 管理后台消耗流水增加渠道列；金额存在但币种缺失时显示“原始单位”，不猜测 USD/CNY，也不误报为金额缺失。
- 验证通过：API 73 项测试通过、1 项 PostgreSQL 环境测试跳过；API 与 Web TypeScript 类型检查通过；本次修改文件通过格式检查及 `git diff --check`。
- 本轮未调用真实海马云计费任务，已将三类模型、参考限制和 `usage` 入账加入人工验收清单；未 commit、push 或部署。

# 当前任务进度：海马云国际区 GPT Image 2.5

- 已建立五阶段计划并记录密钥隔离、区域隔离、无计费调用和不自动发布边界。
- 已确认国际站 GPT Image 2.5 文生图和参考图模型入口，以及 Bearer 鉴权、异步任务查询的标准生命周期。
- 错误记录：首次三文件规划补丁因 `findings.md` 上下文不匹配被整体拒绝；确认产品代码无变化后按实际文件末尾补写。
- 已新增海马云国际区独立 Provider、环境变量和可回滚迁移；中国区 `.cn` 配置保持独立。
- 已接入 GPT Image 2.5 Sunburst 文生图与 1–16 张参考图编辑，画布尺寸会转换为官方画幅与 1k/2k/4k 分辨率字段。
- 管理后台模型二级菜单和消耗流水按 Token360、海马云中国区、海马云国际区分别展示；国际区账单 ID 使用独立前缀，避免与中国区任务号碰撞。
- 验证通过：API 与 Web TypeScript 类型检查通过；API 全量测试通过（77 项执行、76 项通过、1 项 PostgreSQL 环境测试跳过）；`git diff --check` 通过。
- 海外 Key 只做了脱敏格式和鉴权校验，未写入项目、日志或版本库；本轮未 commit、push、部署，也未创建真实生成任务。

# 当前任务进度：账号积分消费系统

- **状态：** 阶段 1 进行中，产品代码尚未修改。
- 已明确基础公式：人民币实际成本 × 100 × 1.2，示例 10 元 = 1200 积分。
- 已建立五阶段计划，计费链路按增量迁移、整数积分、`numeric` 金额、任务幂等扣款和不可删除流水设计。
- 已开始读取用户提供的积分服务协议，并审计现有积分账户、生成消耗与管理后台实现。
- 错误记录：workspace dependency 动态入口已下线，未获得 bundled runtime；后续改用本机已有 DOCX 读取能力。
- 错误记录：本机未安装 Pandoc 与 `python-docx`；已使用 DOCX 的 OOXML 正文只读解析完成协议提取，未安装依赖、未改原文件。
- 已读完协议：实现需支持 12 个月购买积分有效期、赠送积分独立有效期、按批次消费、7 日/5%退款核算、系统误扣冲正和不可删除流水。
- 已审计现有占位实现：积分账户、流水、套餐和支付订单表已经存在，但积分能力关闭，缺少批次/到期/分摊和真实任务扣费。
- 已确认扣费接入点：`generation_usage` 实际金额入账后立即尝试独立积分结算事务；失败保留待计费状态并由 Worker 恢复，使用任务 ID 作为幂等依据避免重复扣款。
- 用户授权同步修改积分服务协议；已按中国大陆格式条款与预付款服务规则完成中等风险初审，后续生成保留修订痕迹的新文件，不覆盖原协议。
- 已核对本地权威来源：《民法典》第496—498条、《消费者权益保护法实施条例》第10、17、22条及《合同行政监督管理办法》第6—9条。
- 已确定最小完整架构：现有账户/流水 + 新增积分批次/分摊 + `generation_usage` 积分结算字段；用户新增 `/credits` 页面，管理员详情支持发放和查看积分。
- 未明确的 USD/CNY 汇率不设猜测默认值，改由超级管理员配置；CNY 直接结算，未知币种保持待计费。
- 第一轮检查：Web 类型检查未报告错误；API 类型检查仅发现 402 不在项目状态码类型中，已改用 409；`git diff --check` 通过。
- 协议修订工具首次运行因插入段落复制了未绑定命名空间属性而停止，输出未保存；已改为标准正文段落属性，保留原文与修订追踪逻辑不变。
- 协议修订稿已生成到 `H:\CodexStorage\积分服务协议-积分消费系统修订稿-20260922.docx`；首次关键字校验误用“USD”，而正文采用“非人民币成本”，已调整验证词，不涉及文件内容错误。
- 已完成增量迁移、积分批次与分摊、不可变流水、任务结算、余额准入、管理员发放与汇率配置、用户积分页和后台消费展示。
- 隔离 PostgreSQL 16 已完成全量迁移；真实数据库测试验证 10 元扣 1200 积分、同任务只扣一次、最早到期批次先扣、余额可在已完成任务后暂时为负以及后续到账先抵欠额。
- API/Web 类型检查通过；API 全量测试新增至 82 项。测试曾发现 PostgreSQL 无法推断余额减法参数类型，已统一加 `bigint` 类型标注并通过回归。
- 支付与支付宝回调仍保持关闭；上线前需先配置 USD/CNY 汇率，并为需要继续生成的账号发放初始积分。本轮未 commit、push 或部署。
# 本轮：积分激活码与渠道计费

- 已补齐一次性激活码表、管理员生成与用户兑换接口、前端入口、本地生成脚本；海马云无币种用量按 CNY 结算。
- API 与 Web TypeScript 检查通过；API 单元测试 81 通过、3 个数据库集成用例按默认配置跳过。
- 隔离 PostgreSQL 刚启动时立即运行集成测试，连接被初始化过程关闭；需等数据库 ready 后重新运行（不是代码错误）。

## 资产重复与回收站调查（2026-09-23）

- 已确认正式仓库工作区无既有未提交改动；开始只读追踪数据链路。
- 已读上传/回收服务和前端资产页；回收属于软删除，继续检查生成链路、引用和线上分布。
- 线上只读 SQL 与磁盘统计完成；回收站很小，重复主要需按来源分解；继续核查迁移机制与磁盘其余占用。
- 完成按来源拆分与文件系统统计；未执行删除、配置变更、commit、push 或部署。业务永久删除及镜像清理均需先做备份/明确范围后实施。

## 双路径清理与同内容引用设计（2026-09-23）

- 用户明确需要手动永久删除和期限自动清理；已询问保留天数，继续核对版本与任务引用语义。
- 已确认版本 ID 被多个链路用作历史身份，需保留版本 ID，仅共享物理文件；继续明确清理策略。
- 形成设计：两种入口共用安全清理器；自动清理跳过画布活跃引用；历史任务产物过期后的展示方式与天数已向用户征询。未改业务代码或生产数据。
- 已实现增量迁移、永久删除 API/界面、14 天后台清理与失败重试；首次验证命令误用不存在的根 workspace，`git diff --check` 已通过，改从子项目继续验证。
- API 与 Web 类型检查通过；API 85 项测试在临时 PostgreSQL 16 中全部通过，覆盖画布引用阻止删除、解除引用后永久删除和 14 天过期清理；临时数据库容器已停止并自动移除。
- 已复审迁移、服务端事务、Worker 定时器、前端二次确认和文档；todo 无对应旧事项需要迁移。本轮未 commit、push、部署或删除生产资产。
