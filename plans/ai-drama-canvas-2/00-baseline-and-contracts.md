# 第 0 部分：基线冻结与契约定稿

状态：`complete`（2026-09-08）

## 目的

在改动鉴权、数据、任务和生产部署前，明确当前代码与 ECS 状态，建立后续所有任务包共享的数据契约。

## 范围

- 审计当前 Git 分支、提交、工作区、远端和现有部署版本。
- 对 ECS 应用目录、环境变量名称、容器配置和数据库做脱敏清单。
- 创建代码、配置和数据库可回滚备份；不在规划阶段执行生产替换。
- 定稿通用节点、`workflowKind`、端口、连线、资产版本、任务状态和模型能力结构。
- 定稿 Web/API/Worker/OSS 的边界和目录结构。
- 为后续迁移补充数据库 ER 草图、API 错误格式和事件格式。

## 关键产物

- 基线审计报告和备份位置。
- `NodeDefinition`、`PortDefinition`、`CanvasEdge` 契约。
- 任务状态机和允许转换表。
- 资产及资产版本规则。
- 模型能力声明与参数过滤规则。
- 服务端目录与 Docker 服务边界。

## 非目标

- 不实现登录、数据库表或生成任务。
- 不修改生产数据。
- 不部署 ECS。

## 验证

- 契约能表达文生视频、图生视频、首尾帧和多参考四种模式。
- 旧节点可映射到新定义，不要求删除旧数据。
- 每个任务状态只有明确允许的下一状态。
- 敏感配置清单只记录变量名，不记录变量值。
- 备份可读取且恢复步骤明确。

## 完成条件

- 后续任务包不再需要重新决定核心 ID、状态和字段语义。
- `task_plan.md` 将第 0 部分标记为 complete，第 1 部分成为下一任务。

## 完成记录

- 本地与 ECS 基线已完成脱敏审计，事实记录在 `findings.md`。
- ECS 与本地备份均已创建，并通过校验和、归档可读性和 Git bundle 验证。
- 跨层契约已定稿，索引位于 `contracts/README.md`。
- 四类视频模式、旧节点无损映射、任务状态转换、资产版本、模型白名单过滤、ER/API/SSE 均已覆盖。
- 本部分没有修改运行代码、生产数据或线上容器，也没有 commit、push 或部署。

## 回滚

- 本部分只增加文档、契约和备份，不替换运行代码；删除新草案即可回到原状态，备份保留。
