# 迁移进度

## 当前状态

- 已将用户 Fork 克隆到 `H:\CodexStorage\infinite-canvas-upgrade`。
- 已确认 `upstream/main` 指向原始上游仓库。
- 正在进行提交依赖与冲突审计。
- 首次选择性迁移尚未开始写入，原因是仓库缺少本地提交身份。
- 已在仓库级配置 GitHub noreply 提交身份。
- 已迁入：遮罩参考图、上游文本标记、多参考图片、渠道导入、素材下载、视频任务恢复、视频模式、媒体参数、节点分组、本地代理基础。
- 正在处理 WebDAV 代理增强的文档冲突。
- 12 个选定的上游功能提交已经全部迁入；广告、发行提交和 Agent 文档更新未迁入。
- 下一步：先验证上游迁移基线，再实现全局模型与 Seedance 请求适配。
- 基线验证第一次尝试因本机没有 Bun 终止，尚未得到代码层检查结果。
- 冻结安装又发现上游 npm 锁文件未同步；将改用不写锁文件的普通安装进行验证。
- 普通安装因 npm 网络连接重置失败，尚未完成基线验证；源码迁移提交保持完整。
- 已加入服务端托管模型渠道：浏览器仅保存占位凭据，真实密钥由容器环境变量注入。
- 已按模式实现 Seedance 2.5 JSON 请求映射，并保留上游远端任务恢复逻辑。
- 已加入 Seedance 2.0 漫剧模板库；仅复用提示词方法，不套用 2.0 的平台参数到 2.5。
- 已从公开目录核对 99 个模型：61 文本、14 图片、22 视频、2 音频；启动后自动同步到内置渠道。
- 类型检查通过，生产构建通过，Seedance 模板 JSON 通过解析，容器配置通过展开检查。
- 提交前敏感信息扫描通过；未发现 Token360 密钥或私钥。
- npm 冻结安装第一次因既有 peer 声明冲突终止，未生成可运行依赖；下一次使用兼容解析。

## 验证记录

- 待执行。
