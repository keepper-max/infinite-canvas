---
title: Pending Tests
description: Implemented changes that still need manual verification
---

# Pending Tests

- 积分账户前台：余额、待计费任务和积分流水保持可见；不再显示“成本 × 120”、供应商成本说明或 USD/CNY 汇率，管理员后台定价设置和实际扣费规则不变。

- Shoushou Canvas branding and currency: navigation, home, sign-in/up, browser title and favicon use the new name and supplied logo; new canvases use the new default name, while existing names and local storage remain intact. In admin summaries and usage rows, legacy RunningHub bills with unknown currency display as CNY and merge with new CNY bills. Unknown Token360 currency must remain unconfirmed and must not cause back-charging.

- HTTP admin page: opening `/admin`, viewing a user, and granting credits from the current HTTP IP origin must not crash with `crypto.randomUUID is not a function`; a retried grant should use a new request ID.

- Activation codes: an admin can issue a code with a credit amount and expiry; the full code appears only once. Redemption adds to the shared balance. Reuse, expiry, and invalid codes must fail. Codes made with the local `credits:code` command must redeem online. RunningHub CNY charges use no FX; Token360 USD charges use the admin-configured rate. Alipay stays disabled.

- Account credit charging: set the USD/CNY settlement rate in `/admin`, then grant credits to a test account. CNY bills must charge `actual amount × 100 × 1.2`; USD bills must first use the saved exchange rate, and fractional points must round up. Reconciliation of the same job must never charge twice. Credits should consume the earliest-expiring lot first. An insufficient balance blocks only new managed jobs; submitted jobs finish and may create a temporary negative balance, which later grants cover before creating spendable lot value. Historical usage must not be back-charged, while missing amount, currency, or exchange rate remains pending. Users can inspect lots and ledger entries in Credits; admin grants and rate changes must be audited. Alipay recharge remains disabled.

- 画布图片编辑重试：使用已同步到云端资产库的图片作为参考图生成失败节点后再次重试，新节点应保存参考图资产版本；历史节点只保存 `projects/...` 对象路径时，应从仍在画布上的原素材节点自动找回资产版本，不再请求创建 0 字节素材，也不再显示“提交内容格式不正确”。

- 海马云国际区 GPT Image 2.5：在服务器环境中分别配置中国区 `RH_API_KEY` 与国际区 `RH_GLOBAL_API_KEY` 后，管理后台“模型”二级菜单应独立显示“海马云 · 中国区”和“海马云 · 国际区”；国际区只显示 GPT Image 2.5 Sunburst 文生图与参考图编辑。切换到国际区后，画布和生图工作台应能使用原有分辨率、比例、质量与透明背景控件提交文生图；像素尺寸应转换为国际接口的 `aspectRatio` 与 `resolution`。参考图编辑应上传 1–16 张图片并调用国际站官方 `sunburst/edit` 路径，且既有画布保存的模型 ID 仍可继续使用，不再把合法上传地址误报为 `Invalid URL`。任务、消耗流水和管理员列表应标记为国际区，切回中国区后原有海马云模型与任务不受影响。

- 海马云 Seedance 2.5 与实际消耗：在管理员后台切换到海马云后，应出现文生视频、首帧/首尾帧和多模态视频三个 Seedance 2.5 Token 模型。分别验证纯文本、单首帧、首尾帧和多模态参考；多模态最多允许 30 张图片、10 段视频和 10 段音频，已知音视频片段应限制在 2–30 秒且已知总时长不超过 30 秒。任务进入终态后，管理员消耗流水应显示 `usage` 返回的第三方实际金额、Token 与计费秒数；上游未返回币种时显示“原始单位”，不得误标为 USD、CNY 或金额缺失。

- 海马云托管渠道：在服务器 `.env` 配置 `RH_API_KEY` 后，管理后台“模型”应显示 Token360/海马云二级菜单，并同步官方 383 个模型；当前画布能精确映射的 291 个生成模型启用，其余 3D、上传、续写和分析等专用端点显示为未启用。切换渠道后，新登录或刷新的普通用户只能看到所选渠道的可用文本、图片、视频和音频模型，且设置页不出现渠道或 API Key。分别验证文生图、图生视频和纯文本/音频任务的媒体预上传、异步状态轮询、结果转存、失败原因、本地手动停止与审计日志；海马云公开接口未提供上游取消端点，因此本地停止不应声称已取消供应商任务。切换渠道前已创建的任务必须继续使用原渠道完成。

- 海马云模型名称：官方目录中的“全能图片/全能视频”包装名应显示为真实模型家族，例如 GPT Image、Nano Banana、Grok、Sora、Veo 或 Runway，并保留文生图、图生视频、官方稳定版、低价渠道版和已下架等区别；Seedance 等本身已经明确的名称保持不变。

- Video result transfer queue: submit several videos concurrently and verify Token360 generation polling remains available while up to four completed results transfer independently. Each result should download in 4 MB Range chunks; a route held below 64 KB/s for 30 seconds should resume from the received byte offset through the alternate official content route without another model submission or charge. The canvas should change from model generation to `正在下载 x / y` and then `正在写入资产库`, while cancellation, Worker restart recovery, immutable asset SHA-256, and completed-node playback remain intact.

- Token360 video return latency: production API and Worker should both run with `VIDEO_POLL_INTERVAL_MS=2000`. After Token360 reaches a terminal video state, the Worker should detect it within roughly two seconds, retain the existing idle reconnect behavior while downloading, persist the immutable asset with its SHA-256, and publish the completed canvas event only after the asset version is available. Manual cancellation and provider rate limits must remain unaffected.

- Token360 incomplete billing: when the billing endpoint returns usage but `billed` is false, or omits currency/final amount, the job must remain pending and retry with the documented backoff instead of becoming settled. Admin overview, user detail, usage breakdowns, and ledgers must show “金额未返回” rather than `UNKNOWN 0`; a later finalized bill must replace the provisional record with its actual currency and decimal amount. Existing incomplete settled records should re-enter reconciliation after migration and reach settled/mismatch only after a finalized amount arrives, or become not billed after 24 hours.

- 管理后台：概览应正常显示用户、项目、素材、任务、近 30 天趋势和消耗汇总；任务与消耗列表中的长任务、Trace、资源及账单 ID 应以短格式展示，悬停可查看且点击可复制完整值；`completed` 且没有真实错误的任务不应在失败原因列显示“任务失败”。

- Seedance video extension ratio: connect a source video to a Seedance 2.0 or 2.5 video node using the `video_input` role. The canvas settings summary and ratio panel should show Adaptive and explain that the output follows the input video; submitting with an old fixed ratio such as `9:16` must send `adaptive` upstream and complete without the provider ratio rejection. A `motion_reference` video must retain the user's selected ratio, and non-Seedance models must remain unchanged.

- Admin and Token360 reconciliation: verify that only administrators can access `/admin`; disabling an account revokes its sessions and blocks new requests without cancelling jobs already submitted. Run text, image, audio, and video jobs and confirm the local job UUID is the initial Token360 Trace, video polling never overwrites it, actual usage and currency-separated string amounts are reconciled, and a manual retry creates a new linked job. Exercise delayed 404 billing responses, duplicate reconciliation, Trace mismatches, the 24-hour cutoff, read-only project inspection, asset downloads, and immutable audit logs.

- Model-aware video duration slider: open the canvas video settings for Seedance 2.5, Seedance 2.0, Seedance 1.5, and a model with non-contiguous duration options. Continuous models should show a compact range slider using their own minimum, maximum, and step; dragging must immediately replace Smart mode with the selected second value, persist after closing/reopening the panel and refresh, and submit that exact duration. Smart must remain selectable only for models that advertise `-1`, while non-contiguous models must retain a working dropdown whose popup stays above the canvas panel.

- Virtual Portrait virtual character assets: in two browsers signed into the same project, upload a character image from the canvas Virtual Characters tab and wait for Token360 status to become Ready. Existing projects whose library stored the provider's numeric internal record ID should transparently repair it to the real asset group ID and complete the upload instead of returning “Asset group not found”. The asset should appear in both browsers, insert or drag as a proportional image node, survive refresh through its cloud asset version, and submit `asset://ta_...` as the first-frame or multimodal image reference. A different project or account must not list, resolve, or submit that virtual character asset; processing/failed states and sanitized provider errors must remain visible without exposing the Token360 key. Removing a virtual character should hide it from the project library without deleting its source cloud asset.

- Interrupted managed-video recovery: while a Token360 video has completed and the Worker is downloading or persisting it, restart the Worker. The existing provider task must be resumed without submitting a new generation even if its model is no longer present in the latest catalog. If the original signed result URL stalls or expires, the Worker must query that same provider task for a refreshed URL and continue transferring without a second generation or charge; the result must enter the asset library and return to the canvas, and the node must not remain permanently at “Generating”.

- Managed failure details: submit a video prompt or references that the provider rejects for visual copyright restrictions. The failed canvas node and Get task status action should show the specific safe Chinese explanation instead of only "视频生成失败"; generated-audio copyright, parameter mismatch, authentication, rate limit, and internal-error messages must remain correctly classified without exposing upstream URLs, headers, or credentials. Re-querying a historical failed job with a stored sanitized provider message should receive the improved explanation.

- Directional connection motion: selecting one or more canvas nodes should animate only their directly connected lines with clearly separated, high-contrast dashes moving from source to target; selecting a line should animate that line. Hover-only highlights and unrelated lines must remain static, the effect should stay legible in both themes and at different zoom levels, and reduced-motion preference must disable movement.

- Managed video attachment: complete a server-managed video task and verify the canvas immediately binds its cloud asset version and playable URL without downloading a second browser-local copy. Until that binding finishes, the node must remain loading rather than showing success with an empty video; refreshing another browser should renew the asset URL from the saved version.

- HTTP asset hashing fallback: from the production HTTP address, submit image and video generation with local reference images. The browser should stage the files as cloud assets without a `crypto.subtle.digest` error; HTTPS/localhost should continue using native Web Crypto, and genuine hashing failures should show a clear Chinese message.

- Video model parameter persistence: in a Seedance 2.5 node, switch among the catalog-provided 4–10 second durations and change resolution, ratio, audio, bitrate, or output options. Every selection should remain visible after closing and reopening the panel and should be submitted as the selected model-specific value.

- Large video references: generate Seedance video from a local image larger than 1 MB and verify the browser uploads it as an immutable cloud asset before creating the job, the request survives refresh, and manual Stop cancels an in-progress upload. A provider rejection mentioning generated-audio copyright must be shown as an actionable Chinese message instructing the user to disable generated audio.

- Dynamic parameters for all video models: switch among representative Vidu Q1 text/image, Veo 3.1, Berry, Seedance, CogVideoX, and MiniMax models. Modes, duration, resolution, ratio, and extended controls must follow the catalog schema. T2V must reject references, first/last-frame modes must cap images at 1/2, multimodal mode must enforce separate image/video/audio limits including explicit zeroes, and native `2k` / `4k` values must never be converted to `720p`.

- Canvas reference picker: clicking Add reference with no eligible canvas asset should show a warning without hiding the node editor. With an eligible image, video, audio, text, or resource group, selecting it should create one connection and immediately return to the node editor; clicking the target node, the top hint, or ESC should cancel selection.

- Managed canvas result recovery: Seedance asynchronous submissions should retain the provider task ID instead of failing on legacy databases. Canvas image jobs should identify their target node and job in the inspector, remain successful when browser image caching is unavailable, and refresh from a fresh cloud asset URL on another browser.

- Cross-device model catalog: on a browser with no local site data, sign in as an existing or new user and open the image, video, and text workbenches. The server catalog should load only after authentication and expose all enabled models for each capability without requiring a second refresh or a previously warmed browser cache.

- Provider wait and manual cancellation: text, image, audio, and video submissions must not be aborted by a client, proxy, worker, or overall runtime deadline. They should keep waiting for the provider's terminal response. Clicking Stop must cancel the browser wait and managed backend job, abort an in-flight synchronous provider request, and prevent a later completion from overwriting the cancelled state. Failed requests create only one upstream attempt and remain available for explicit manual retry. FFmpeg composition keeps its independent retry policy.

- Fractional canvas persistence: freely resize a node to fractional dimensions, wait for synchronized status, and refresh. The node must restore at the same precise size without a 422 or an unsynchronized state; integer helper columns in PostgreSQL must not reject the save.

- Model catalog and prompt sources: after API startup, Settings should list every currently supported text, image, video, and text-to-speech model returned by the public catalog, grouped by capability; speech-to-text must not appear under text-to-speech until a transcription node exists. The public top bar and canvas menu should have no documentation entry. Prompt source cards should show concise purpose descriptions rather than URLs, while refresh and custom-source editing continue to work.

- Cloud project lifecycle: automated API/PostgreSQL coverage now passes for create, rename, soft delete, access blocking, fallback workspace, canvas save, and a fresh login session restoring the same canvas revision. Manual UI verification remains for project-list and in-canvas controls, redirects, shared-viewer visibility, and confirming IndexedDB-only legacy projects never reappear as active cloud projects.

- Production release orchestration: preflight and deployment must stop before building or switching services when `.env` permissions are too broad, secure cookies/HTTPS origins are missing, the worktree is dirty, or the explicit confirmation token is absent, without printing secrets. The release must build separate commit-tagged Web and API images, migrate and switch API/Worker before Web, retain a verified backup and manifest on failure, and restore previous images from that manifest without dropping database structures.

- Platform operations and permissions: new users should get a zero-balance credit account; SMS and Alipay payments must remain disabled. A team `editor` can edit an attached project, while a `viewer` can read it but cannot save the canvas, mutate assets, or create/retry/cancel generation and composition jobs. Non-admin users cannot access platform metrics, failures, or model health; admin-visible errors must not expose API keys. Before release, create and verify backups of source, environment, PostgreSQL, containers, and image identifiers, then exercise migrations and rollback in an isolated environment.

- Drama composition: generate at least two video shots plus dialogue, effects, and music in the default template. Build subtitles and a timeline, edit clip duration/fades, audio start/volume, subtitle timing, and output size, then submit the episode. The dedicated worker must continue after the page closes, recover or retry after refresh, store a playable/downloadable immutable asset with a cover, honor cancellation, clean temporary files, and reject cross-project asset references.

- Canvas productization: verify six-way alignment, horizontal/vertical distribution, grouping, and bulk locking all participate in undo/redo; locked nodes must resist dragging, resizing, auto layout, and bulk deletion. Alignment guides should appear near peer edges and centers. Selecting a node or edge should open the inspector with generation metadata, references, task/version details, or edge roles; incompatible roles must be rejected, while disconnect and reconnect work. Assets should drag to an exact canvas position, focus mode should hide surrounding chrome, and performance mode should keep 80+ media nodes navigable.

The current release needs manual verification in these areas:

- Managed image references: connect two or more locally stored high-resolution images to a GPT Image 2 edit node and generate. Each local reference should upload to cloud assets before the generation job is created, the job request should contain asset version IDs instead of Base64 image bodies, and Nginx should no longer return 413. A genuine oversized JSON request should display an explicit request-too-large message in both the toast and failed node instead of the generic all-images-failed message.
- Managed video result recovery: allow an upstream video result download to take longer than 60 seconds and verify the canvas keeps waiting until the asset is persisted. Manual stop must still cancel the wait. If persistence fails after upstream completion, Retry should reuse the existing provider task instead of submitting and charging for a second generation. Duration and frame-rate Select menus must render above the canvas settings panel.
- Managed generation jobs: after signing in, start text, image, audio, and each Seedance video mode from the canvas. Progress should survive page refresh, duplicate clicks should not create duplicate jobs, successful files should enter cloud assets as immutable versions, failed jobs should allow retry, and a running cancellation must remain cancelled instead of later becoming completed. The browser must not expose the built-in API key or provider address.
- Local real-provider smoke is complete: the latest Docker stack loaded the secret into API/Worker, completed one GPT-5.5 job, and restored the same canvas revision and completed job from a fresh login session. Broader image, audio, and Seedance mode coverage remains under Managed generation jobs above.

- Cloud assets and versions: after signing in, upload images, video, audio, subtitles, or a project archive from My Assets and verify that they remain after refresh; image cards should use thumbnails. Uploading another file to the same asset should create an immutable version that can be compared side by side, selected as the current version, used as the parent of a new version, downloaded, moved to trash, and restored. Switching the current version must not change an exact version already pinned by a canvas. Migrating local assets should transfer old IndexedDB images and videos item by item, allow failed items to retry, and keep the original local files. A different account must not be able to list, download, or reference another account's assets.

- Local proxy: Settings should show a dedicated Local proxy tab between Providers and Preferences, with a switch that is off by default. Turning it on should reveal the `npx @basketikun/canvas-proxy` command (with `--port` appended when a non-default port is configured), a copy action, and an address field defaulting to `http://127.0.0.1:23210` that normalizes a missing scheme and trailing slashes on blur. Test connection should succeed only while the command is running. With the proxy on, fetching model lists, generating images, videos, text (including streamed output), and audio, running custom call scripts, polling video tasks, and downloading generated images and videos should all route through the proxy and keep working; turning it off should restore direct requests. The proxy itself should forward request methods, headers, query strings, and bodies unchanged, stream SSE responses without buffering, and keep `data:` and `blob:` URLs untouched.
- Image and video size settings: Image settings should split resolution (`1k` / `2k` / `4k` / auto) from aspect ratio (`1:1`, `2:3`, `3:2`, `4:3`, `3:4`, `16:9`, `9:16`, `21:9`, `9:21`, auto) and fill W/H from that pair. Managed video settings should use the selected model's catalog values and defaults; custom browser channels should retain their legacy resolution, ratio and W/H controls.
- Built-in video requests: without a custom call script, OpenAI-format channels should send `mode`, `first_frame`/`last_frame` or `image[]`, `generate_audio`, `watermark`, and reference videos/audio like the insertable template, and must not use `input_reference[]`. Gemini-format channels should use `predictLongRunning` with `image`/`lastFrame` or `referenceImages`, and must not report that video is unsupported. Refreshing the page should continue polling a Gemini task.
- Video generation mode: managed video settings should offer only modes supported by the selected model and submit the explicitly selected `t2v`, `i2v`, `flf2v`, or `multiref` mode. Legacy saved `frames`/`reference` values and custom request scripts remain compatible.
- Gemini image generation: Gemini-format channels and the Gemini image call-script template should send `generationConfig.responseModalities: ["TEXT", "IMAGE"]` with `imageConfig.aspectRatio` / `imageConfig.imageSize`, and must not send `responseFormat`.
- Video request scripts: opening Call Script for a Video model should open a full-screen stepped editor. The left panel should follow Read the rules → Let another AI write it → Available variables, and list `videos` (File[]) and `audios` (File[]). Copy script brief should include the capability, variables, return requirements, and writing rules. Inserted templates should be async functions whose parameter lists unpack `params` (image templates must include `quality`, `size`, `count`, and `background`). The OpenAI video template should use FormData with `mode`, duration, size, resolution, audio, watermark, and reference images/videos/audios; `mode=frames` should send `first_frame` / `last_frame`, and `mode=reference` should send `image[]`, then poll and download `/content`. After pasting a script from another AI and saving, generating from the canvas with connected reference videos or audio should pass those files into the script.
- URL credential import: opening the app with a new valid `baseUrl` and `apiKey` should append a provider without changing existing providers; importing the same Base URL again, including trailing-slash or `/v1` variants, should update only that provider and remain idempotent; an API key without a Base URL or an invalid Base URL should leave all settings unchanged.
- Documentation navigation and page contents: at phone width, opening the documentation sidebar should show the current section and its description at the top. Expanding it should allow switching among Overview, Canvas Guide, Development, Progress, Business, and Support; the selected section and page list should update together while the desktop top tabs remain unchanged. At widths of 960px and above, a table of contents with hierarchical track lines should appear to the right of the article, support heading navigation, and highlight the current heading while scrolling; narrower layouts should keep the top collapsible contents control.
- Canvas Agent generation-flow references: when a generation prompt contains only `@[node:...]` references already passed through `referenceNodeIds`, the existing text, image, video, audio, or group nodes should connect directly to the generated config node without creating a duplicate prompt text node; prompts containing ordinary prose should continue creating their own prompt text node.
- Video frame capture: right-clicking a video node with content should show Capture first frame, Capture last frame, and Capture current frame. Each action should create and select a proportional image node to the right, connect the video to the image, use the player's visible position for the current frame, and leave the original playback position unchanged.
- Text nodes: the Generate button should no longer appear in the upper-right corner of the node body, and text should use the full content area. The image-generation action in the hover toolbar should continue to create a generation configuration.
- Multi-text generation: requesting more than one result should create one text-result node and one source connection. Its upper-right count should expand all successful alternatives like a multi-image node; setting an alternative as primary should immediately replace the body while retaining the previous primary, and edits to any primary text should remain when switching away and back.
- Group references: group nodes should use a transparent, lightweight border and title treatment and expose a source handle on the right. Connecting a group to a generation node should include every populated image, video, audio, text, and reference-capable extension node inside it. Regular prompt mentions should list the group's resources, while the generation-config composer should allow selecting the whole group with one `@`; resources referenced both individually and through the group must be sent only once.
- Group selected nodes: after selecting two or more nodes, a rounded dashed bound should wrap the whole selection, with Group above it. It wraps those nodes in a group without moving them, then selects the new group. Selecting a group should allow Ungroup from the hover toolbar, context menu, or `Ctrl/Cmd+Shift+G`. Nodes already in the same group should show Ungroup instead of grouping again. Dragging into a group, group references, and the left-panel tree should keep working.
- Node reference bar: regular prompt panels, expanded editors, and generation-config inputs should show their current input nodes above the editor. Images and videos should use thumbnails with hover previews, while text, audio, and extension nodes show their matching type. A group connection should expand into every valid content node inside the group, and removing any expanded item should disconnect the whole group. The plus button should enter canvas selection for adding multiple valid references. Connected nodes, the target itself, children already referenced through a group, and unsupported nodes must not be selectable again; ESC or the top hint should return to the input, and new connections should immediately update the `@` list.
- Multi-image transparency: when a multi-image node containing transparent PNG or WebP assets is expanded, transparent areas in both the primary and alternatives should reveal the canvas; switching primary images must not introduce a white or panel-colored background.
- Canvas navigation: the canvas should open in Move mode by default, and the bottom toolbar should switch between Select and Move modes and reflect the active mode with its icon; Select should draw a clearly spaced dashed range whose dash length, gap, and stroke width remain visually constant at every zoom level, while Move should drag elements directly and pan only when dragging the blank background. Clicking blank canvas space in either mode should deselect the current node and close its top toolbar and lower prompt panel. Holding Control or Space should temporarily invert the active tool without changing the toolbar mode: Select becomes Move, and Move becomes range selection. After clicking any canvas-page button, pressing Space should only invert the tool and must not trigger that button again. Middle-button panning from any position and additive node selection with Shift/Cmd should continue to work, while Space remains available inside text inputs and editable content.
- English routes without a locale prefix should load directly without a redirect loop; switching between English and Simplified Chinese should keep navigation, settings, titles, descriptions, Ant Design components, and persisted preferences in the selected language.
- Global Dropdown, Menu, Select, Cascader, and TreeSelect popup backgrounds, hover states, and selected states in both light and dark themes.
- Canvas node resize stability: importing an image should open its prompt panel, resizing it immediately from any corner should temporarily hide the panel, remain smooth without flashing or triggering a maximum update depth error, and restore the panel after release. Prompt scrolling, generated-prompt restoration, edit-prompt persistence after closing an existing image or text node panel, expanded prompt editing with live synchronization and `@` references, image editing, drag-and-drop references, and generation configuration should continue to work.
- Text output count: both a text node's lower prompt panel and a generation configuration node in Text mode should expose an independent generation count from 1 to 15, default to one, and create that many text nodes. Changing the text count must not alter the image count, and switching a generation configuration node back to Image should continue to use the configured image count.
- Node editing entry points: the image toolbar should default to icons without labels and no longer include Edit; the text toolbar should no longer include Edit or Edit text. Clicking an image, text, or video node without dragging should open its lower prompt panel directly, while image toolbar labels can still be enabled explicitly from the toolbar settings.
- Canvas side panel: clicking an element row should smoothly center and select its node without zooming above 100%; image elements with content should still open the large preview without triggering canvas focus.
- 画布左侧树形列表：组内节点应缩进显示在所属组下方，并带有清晰的层级连接线；组箭头可以收起和展开子节点，搜索或类型筛选时仍应保留匹配节点所属的组。
- Multi-image canvas generation: starting N images should immediately create one image node with N expandable slots and one incoming connection; each empty slot should independently show generating or failure while requests are running, every slot with image content should always display that image, and the first successful image should appear as the primary image immediately. The collapsed state should use a compact stack with at most three closely spaced backplates and a clearly legible count control; only clicking the count control should expand or collapse the slots upward and to the right, while double-clicking the image should open its large preview. Every completed secondary image should keep a clearly legible primary-image action visible in both themes. Expanded groups should remain expanded when clicking elsewhere, dragging, resizing, or previewing, and multiple groups may remain expanded together.
- Multi-image primary selection: choosing another primary image should preserve the node center and current maximum edge while adapting to the new image ratio; free-resize nodes should keep their exact dimensions, and the viewport zoom indicator must not change.
- Failed multi-image slots: every failed slot, including the current primary slot, should expose retry and delete actions. Retry should update only that slot without replacing an existing successful primary image; delete should remove only that failed slot and update the displayed count.
- Multi-image resize: resizing the root node while child images are expanded should keep the group expanded and resize normally without triggering nested React updates or an application error.
- Multi-image alternatives: expanded images should use up to four columns so larger batches stay compact; double-clicking any completed child image should preview that exact image without changing the primary image. Every completed image, including the primary image, should provide an independent download action, while alternatives should also keep clearly legible create-copy and set-primary actions in both themes. Create-copy should add an independent image node to the right of the group, select it, and leave the original image group unchanged.
- Canvas Agent startup, MCP status, model and permission controls, approvals, version reporting, diagnostics, streaming responses, history consistency, multi-tab isolation, and local Skill management.
- Canvas Agent credential redaction: Codex and Claude CLI stderr containing Bearer tokens, `sk-` keys, explicit API key/token/authorization assignments, ANSI formatting, or credentials split across output chunks should reach neither debug logs nor browser diagnostic events in plaintext; ordinary stderr text should remain intact.
- Image API model parameters: `gpt-image-*` generation and edit requests should omit `response_format`, while `dall-e-*` requests should continue sending `response_format: "b64_json"`.
- Image URL persistence: canvas and image-workbench generation should download each remote API result only once, validate it as a decodable image, and persist it in IndexedDB; `200` responses containing non-image data should fail, stopping canvas generation during download should not save or apply the result, CORS-blocked but displayable images should keep their original URL without an empty storage key, and plugin calls should still receive Data URLs whenever the remote image is readable.
- Multi-reference image editing: OpenAI-format edits with more than one reference image should submit files as repeated `image[]` form fields; a single reference should continue using `image`. Newly created OpenAI image scripts should use the same field names. Gemini-format channels and already saved custom scripts are unchanged.
- Agent header tabs in English and Chinese at different panel widths; labels should collapse to icons and counts before either edge is clipped.
- Agent message layout, Markdown, code blocks, attachments, token statistics, progress timelines, command groups, and scroll-follow behavior.
- Agent inline references: type `/` to select an enabled Skill or `@` to select a canvas asset at the caret; tokens should remain aligned with text, support keyboard navigation and hover previews, and preserve their positions after sending or refreshing.
- Agent message metadata: image attachments, Skills, canvas references, and local previews should survive page refreshes and Agent restarts through exact `clientMessageId`, `threadId`, and `turnId` ownership; protocol upgrades must not reset the independent storage format, unknown or damaged storage must refuse writes without overwriting files, no records may be silently pruned by count or size, and deleting a thread should remove its metadata and preview assets without relying on browser IndexedDB or text matching.
- Prompt Center layout, source synchronization, search, asset insertion, prompt detail dialogs, and compact split-layout prompt pickers in the image and video workbenches.
- Workbench history image cleanup: after generating content in the image or video workbench, deleting an asset, canvas image node, canvas content, or canvas project must not remove thumbnails, results, or references from generation history; deleting a generation record should still remove its dedicated local files.
- Prompt text labels: when a regular node or generation configuration is connected to several text nodes, every upstream text block appended to the prompt should start with its label such as `【Text 1】` or `【Text 2】`, numbered in connection order and matching both the `@` references in the prompt and the reference bar. A single text input should still be labeled, and image, video, and audio numbering must stay unchanged.
- Local mask editing: the dialog footer should offer Reset, Export to canvas, and Generate now. Both actions require a painted area and an edit request; Export to canvas should only write the nodes and connections while prefilling the composed prompt in the result node's prompt panel for manual generation, and Generate now should start the request right after writing the nodes. After painting an area and entering an edit request, confirming should add a mask annotation node below the source image showing the original picture with a translucent blue overlay on the painted area that matches the color and opacity used while painting in the dialog, create the result node to the right, and connect both the source image and the mask annotation to it. The request should be sent as two ordinary reference images with a prompt that names them by index, without the `mask` API parameter, and it should also work for Gemini-format channels; only the marked area should change, and the result must not keep any blue overlay.
- Settings import/export and WebDAV-related configuration.
- Local storage settings: the Settings page should provide a Local storage tab that reads only the `infinite-canvas` database and shows IndexedDB usage, total site usage, browser quota, quota percentage, and per-object-store record counts and content size; refreshing after local data changes should update the figures without blocking the rest of the page.
- Asset image download: opening an image that still previews in My Assets and clicking Download image should save the file. Titles with Chinese characters, spaces, or brackets such as `【】` should remain the downloaded filename and must not need renaming. Images saved from the canvas in the same session should download as well.
- Canvas video task resume: starting an OpenAI-style video generation should store the remote task ID on the video node. After a page refresh, a still-pending video should keep polling automatically; if polling stopped, **Get task status** should resume with that task ID until the video is ready, the task fails, or the status request errors. Task failure should then allow a normal retry that creates a new generation.

The Chinese version contains the complete item-by-item acceptance checklist.

## 图片分辨率尺寸表

选择分辨率和宽高比后，W/H 按下表填写，单位为像素。预设尺寸不受「16 倍数对齐」开关影响；该开关只控制手动输入尺寸时的对齐。最终生成尺寸以所选模型的实际输出为准。

| 宽高比 | 1K | 2K | 4K |
| --- | ---: | ---: | ---: |
| **1:1** | **1024 × 1024** | **2048 × 2048** | **2880 × 2880** |
| **2:3** | 1024 × 1536 | 1360 × 2048 | 2336 × 3520 |
| **3:2** | 1536 × 1024 | 2048 × 1360 | 3520 × 2336 |
| **4:3** | 1024 × 768 | 2048 × 1536 | 3312 × 2480 |
| **3:4** | 768 × 1024 | 1536 × 2048 | 2480 × 3312 |
| **16:9** | 1536 × 864 | 2048 × 1152 | **3840 × 2160** |
| **9:16** | 864 × 1536 | 1152 × 2048 | **2160 × 3840** |
| **21:9** | 2016 × 864 | 2688 × 1152 | **3840 × 1648** |
| **9:21** | 864 × 2016 | 1152 × 2688 | **1648 × 3840** |

待验收：

- 在画布和图像工作台逐项切换 3 档分辨率与 9 种比例，W/H 应与表格一致，重新打开设置后仍选中正确档位；重点检查 1K 的 2:3、16:9、21:9 及对应竖图，以及 4K 的 1:1。
- 所有 4K 预设提交时均不应触发本地总像素或最长边限制；生成和编辑请求、调用脚本应收到所选像素尺寸。
- 分辨率为自动时保留比例选择，比例为自动时禁用 W/H；手动输入 W/H 后，对齐开关仍应按原有方式工作。
- Gemini 在质量为自动时，应将表中尺寸识别为对应的 1K / 2K / 4K；显式选择质量时维持原有优先级。视频尺寸设置应保持原有行为。
- 视频工作台长任务：提交一个超过五分钟完成的托管视频任务，页面应持续显示生成中且只允许手动停止，不再自动报超时；旧版本已显示“视频生成超时”的记录在刷新后应恢复查询，并在服务端任务完成后显示视频结果。
